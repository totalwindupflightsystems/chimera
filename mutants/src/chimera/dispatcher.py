"""The Dispatcher — designs the entire deliberation in ONE model call.

Reads the user prompt + the model catalog (with category-weighted scores) and
produces a :class:`DispatchResult`: a formation DAG, a custom prompt for every
worker, and merge instructions for the aggregator.

Two operating modes:
* ``auto`` — the dispatcher designs the DAG structure *and* writes all prompts.
* named preset (``simple`` / ``debate`` / ``audit`` / ...) — the DAG structure is
  derived from the config preset, and the dispatcher writes the per-worker
  prompts and aggregator/audit instructions for that fixed structure.

Either way Chimera makes exactly one dispatcher model call.
"""

from __future__ import annotations

import json
import re
import time
from dataclasses import dataclass
from typing import Any

import structlog
from pydantic import BaseModel, Field

from chimera.config import ChimeraConfig, FormationPreset
from chimera.gateway import Gateway, GatewayError, GatewayResponse

log = structlog.get_logger("chimera.dispatcher")

#: JSON schema description embedded in the dispatcher prompt.
DISPATCH_SCHEMA_HINT = {
    "formation": {
        "stages": [
            {"id": "worker_1", "kind": "worker", "model": "<model name>", "depends_on": []}
        ],
        "edges": [["worker_1", "aggregator"]],
    },
    "worker_prompts": [
        {
            "stage_id": "worker_1",
            "model": "<model name>",
            "prompt": "<custom subtask prompt>",
            "expected_output_schema": None,
        }
    ],
    "aggregator_instructions": "<how to merge the worker outputs into the final answer>",
    "stage_instructions": {"<audit/merge stage id>": "<instructions>"},
    "output_schema": {
        "type": "object",
        "properties": {
            "answer": {"type": "string", "description": "The final merged answer for the user"},
            "sources": {"type": "array", "items": {"type": "string"}, "description": "Worker IDs that contributed"},
        },
        "required": ["answer"],
    },
}


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


class Stage(BaseModel):
    """A single node in the formation DAG."""

    id: str
    kind: str = "worker"  # worker | aggregator | audit | merge
    model: str
    depends_on: list[str] = Field(default_factory=list)
mutants_xǁFormationDAGǁstage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁFormationDAGǁterminals__mutmut: MutantDict = {}  # type: ignore
mutants_xǁFormationDAGǁtopo_order__mutmut: MutantDict = {}  # type: ignore


class FormationDAG(BaseModel):
    """The dispatcher-designed execution graph."""

    stages: list[Stage]
    edges: list[tuple[str, str]] = Field(default_factory=list)

    @_mutmut_mutated(mutants_xǁFormationDAGǁstage__mutmut)
    def stage(self, stage_id: str) -> Stage:
        for s in self.stages:
            if s.id == stage_id:
                return s
        raise KeyError(stage_id)

    def xǁFormationDAGǁstage__mutmut_orig(self, stage_id: str) -> Stage:
        for s in self.stages:
            if s.id == stage_id:
                return s
        raise KeyError(stage_id)

    def xǁFormationDAGǁstage__mutmut_1(self, stage_id: str) -> Stage:
        for s in self.stages:
            if s.id != stage_id:
                return s
        raise KeyError(stage_id)

    def xǁFormationDAGǁstage__mutmut_2(self, stage_id: str) -> Stage:
        for s in self.stages:
            if s.id == stage_id:
                return s
        raise KeyError(None)

    def stage_ids(self) -> list[str]:
        return [s.id for s in self.stages]

    def kinds(self) -> set[str]:
        return {s.kind for s in self.stages}

    @_mutmut_mutated(mutants_xǁFormationDAGǁterminals__mutmut)
    def terminals(self) -> list[Stage]:
        """Stages that nothing else depends on (the answer producers)."""
        depended = {src for src, _ in self.edges}
        return [s for s in self.stages if s.id not in depended]

    def xǁFormationDAGǁterminals__mutmut_orig(self) -> list[Stage]:
        """Stages that nothing else depends on (the answer producers)."""
        depended = {src for src, _ in self.edges}
        return [s for s in self.stages if s.id not in depended]

    def xǁFormationDAGǁterminals__mutmut_1(self) -> list[Stage]:
        """Stages that nothing else depends on (the answer producers)."""
        depended = None
        return [s for s in self.stages if s.id not in depended]

    def xǁFormationDAGǁterminals__mutmut_2(self) -> list[Stage]:
        """Stages that nothing else depends on (the answer producers)."""
        depended = {src for src, _ in self.edges}
        return [s for s in self.stages if s.id in depended]

    @_mutmut_mutated(mutants_xǁFormationDAGǁtopo_order__mutmut)
    def topo_order(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_orig(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_1(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = None
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_2(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = None
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_3(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = None

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_4(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id not in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_5(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id not in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_6(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(None)
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_7(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(None)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_8(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep not in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_9(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(None, stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_10(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], None)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_11(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_12(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], )
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_13(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(None)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_14(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(None)
            ordered.append(node)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_15(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(None)

        for s in self.stages:
            visit(s, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_16(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(None, set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_17(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, None)
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_18(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(set())
        return ordered

    def xǁFormationDAGǁtopo_order__mutmut_19(self) -> list[Stage]:
        """Return stages ordered so dependencies come first."""
        by_id = {s.id: s for s in self.stages}
        ordered: list[Stage] = []
        done: set[str] = set()

        def visit(node: Stage, stack: set[str]) -> None:
            if node.id in done:
                return
            if node.id in stack:
                raise ValueError(f"Cycle detected at {node.id}")
            stack.add(node.id)
            for dep in node.depends_on:
                if dep in by_id:
                    visit(by_id[dep], stack)
            stack.discard(node.id)
            done.add(node.id)
            ordered.append(node)

        for s in self.stages:
            visit(s, )
        return ordered

mutants_xǁFormationDAGǁstage__mutmut['_mutmut_orig'] = FormationDAG.xǁFormationDAGǁstage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁFormationDAGǁstage__mutmut['xǁFormationDAGǁstage__mutmut_1'] = FormationDAG.xǁFormationDAGǁstage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁstage__mutmut['xǁFormationDAGǁstage__mutmut_2'] = FormationDAG.xǁFormationDAGǁstage__mutmut_2 # type: ignore # mutmut generated

mutants_xǁFormationDAGǁterminals__mutmut['_mutmut_orig'] = FormationDAG.xǁFormationDAGǁterminals__mutmut_orig # type: ignore # mutmut generated
mutants_xǁFormationDAGǁterminals__mutmut['xǁFormationDAGǁterminals__mutmut_1'] = FormationDAG.xǁFormationDAGǁterminals__mutmut_1 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁterminals__mutmut['xǁFormationDAGǁterminals__mutmut_2'] = FormationDAG.xǁFormationDAGǁterminals__mutmut_2 # type: ignore # mutmut generated

mutants_xǁFormationDAGǁtopo_order__mutmut['_mutmut_orig'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_orig # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_1'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_1 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_2'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_2 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_3'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_3 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_4'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_4 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_5'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_5 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_6'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_6 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_7'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_7 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_8'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_8 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_9'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_9 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_10'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_10 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_11'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_11 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_12'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_12 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_13'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_13 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_14'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_14 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_15'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_15 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_16'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_16 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_17'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_17 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_18'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_18 # type: ignore # mutmut generated
mutants_xǁFormationDAGǁtopo_order__mutmut['xǁFormationDAGǁtopo_order__mutmut_19'] = FormationDAG.xǁFormationDAGǁtopo_order__mutmut_19 # type: ignore # mutmut generated


class WorkerPrompt(BaseModel):
    stage_id: str
    model: str
    prompt: str
    expected_output_schema: dict[str, Any] | None = None
mutants_xǁDispatchResultǁworker_prompt_for__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatchResultǁstage_instruction__mutmut: MutantDict = {}  # type: ignore


class DispatchResult(BaseModel):
    """What the dispatcher produces in one call."""

    formation: FormationDAG
    worker_prompts: list[WorkerPrompt] = Field(default_factory=list)
    aggregator_instructions: str = ""
    stage_instructions: dict[str, str] = Field(default_factory=dict)
    output_schema: dict[str, Any] | None = None  # JSON Schema for the final answer
    source: str = "auto"  # auto | preset | fallback

    @_mutmut_mutated(mutants_xǁDispatchResultǁworker_prompt_for__mutmut)
    def worker_prompt_for(self, stage_id: str) -> WorkerPrompt | None:
        for wp in self.worker_prompts:
            if wp.stage_id == stage_id:
                return wp
        return None

    def xǁDispatchResultǁworker_prompt_for__mutmut_orig(self, stage_id: str) -> WorkerPrompt | None:
        for wp in self.worker_prompts:
            if wp.stage_id == stage_id:
                return wp
        return None

    def xǁDispatchResultǁworker_prompt_for__mutmut_1(self, stage_id: str) -> WorkerPrompt | None:
        for wp in self.worker_prompts:
            if wp.stage_id != stage_id:
                return wp
        return None

    @_mutmut_mutated(mutants_xǁDispatchResultǁstage_instruction__mutmut)
    def stage_instruction(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_orig(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_1(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id not in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_2(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" or not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_3(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind != "aggregator" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_4(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "XXaggregatorXX" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_5(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "AGGREGATOR" and not self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_6(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" and self.stage_instructions:
            return self.aggregator_instructions
        return ""

    def xǁDispatchResultǁstage_instruction__mutmut_7(self, stage_id: str, kind: str) -> str:
        """Prompt instructions for a non-worker stage."""
        if stage_id in self.stage_instructions:
            return self.stage_instructions[stage_id]
        if kind == "aggregator" and not self.stage_instructions:
            return self.aggregator_instructions
        return "XXXX"

mutants_xǁDispatchResultǁworker_prompt_for__mutmut['_mutmut_orig'] = DispatchResult.xǁDispatchResultǁworker_prompt_for__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatchResultǁworker_prompt_for__mutmut['xǁDispatchResultǁworker_prompt_for__mutmut_1'] = DispatchResult.xǁDispatchResultǁworker_prompt_for__mutmut_1 # type: ignore # mutmut generated

mutants_xǁDispatchResultǁstage_instruction__mutmut['_mutmut_orig'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_1'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_2'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_3'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_4'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_5'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_6'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatchResultǁstage_instruction__mutmut['xǁDispatchResultǁstage_instruction__mutmut_7'] = DispatchResult.xǁDispatchResultǁstage_instruction__mutmut_7 # type: ignore # mutmut generated


@dataclass(slots=True)
class DispatchOutcome:
    """The dispatcher model call result + parsed dispatch plan."""

    result: DispatchResult
    messages: list[dict[str, str]]
    response: GatewayResponse
    latency_ms: int
mutants_x_build_preset_dag__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


@_mutmut_mutated(mutants_x_build_preset_dag__mutmut)
def build_preset_dag(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_orig(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_1(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag or preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_2(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_3(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(None, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_4(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, None)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_5(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_6(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, )

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_7(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto and preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_8(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is not None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_9(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError(None)

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_10(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("XXCannot build a structural DAG from an auto presetXX")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_11(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("cannot build a structural dag from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_12(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("CANNOT BUILD A STRUCTURAL DAG FROM AN AUTO PRESET")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_13(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = None
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_14(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(None, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_15(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, None)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_16(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_17(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, )
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_18(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = None
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_19(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = None
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_20(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = None

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_21(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = None
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_22(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(None):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_23(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = None
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_24(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i - 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_25(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 2}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_26(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(None)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_27(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(None)

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_28(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=None, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_29(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind=None, model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_30(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=None))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_31(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_32(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_33(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", ))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_34(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="XXworkerXX", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_35(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="WORKER", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_36(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i / len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_37(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = None
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_38(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(None, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_39(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, None)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_40(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_41(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, )
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_42(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = None
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_43(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(None):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_44(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = None
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_45(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "XXaggregatorXX" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_46(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "AGGREGATOR" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_47(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i != 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_48(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 1 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_49(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i - 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_50(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 2}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_51(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(None)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_52(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(None)
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_53(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=None, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_54(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind=None, model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_55(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=None, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_56(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=None))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_57(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_58(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_59(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_60(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, ))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_61(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="XXaggregatorXX", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_62(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="AGGREGATOR", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_63(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(None)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_64(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append(None)

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_65(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = None
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_66(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(None)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_67(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(None)
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_68(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id=None, kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_69(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind=None, model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_70(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=None, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_71(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=None))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_72(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_73(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_74(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_75(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, ))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_76(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="XXauditXX", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_77(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="AUDIT", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_78(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="XXauditXX", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_79(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="AUDIT", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_80(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[1]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_81(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append(None)

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_82(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[1], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_83(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "XXauditXX"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_84(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "AUDIT"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_85(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) >= 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_86(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 2:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_87(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = None
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_88(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = None
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_89(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "XXmergeXX"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_90(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "MERGE"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_91(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(None)
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_92(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=None, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_93(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind=None, model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_94(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=None, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_95(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=None))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_96(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_97(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_98(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_99(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, ))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_100(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="XXmergeXX", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_101(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="MERGE", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_102(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(None)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_103(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append(None)

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_104(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = None
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_105(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=None, edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_106(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=None)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_107(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(edges=edges)
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_108(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, )
    _validate_dag(dag, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_109(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(None, config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_110(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, None)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_111(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(config)
    return dag


# --------------------------------------------------------------------------- #
# Preset → DAG construction (deterministic, testable without an LLM)
# --------------------------------------------------------------------------- #


def x_build_preset_dag__mutmut_112(preset: FormationPreset, config: ChimeraConfig) -> FormationDAG:
    """Construct a :class:`FormationDAG` from a config preset.

    Resolution order:
    1. ``preset.dag`` — an explicit client/config-defined DAG. Returned (validated)
       directly. This is the modern path; the legacy ``simple``/``debate``/``audit``
       presets can be redefined this way in config.
    2. ``preset.workers`` / ``aggregator`` — the legacy structural fallback.
    3. Otherwise (e.g. an ``auto`` preset) → ``ValueError``.
    """
    if preset.has_dag and preset.dag is not None:
        return build_dag_from_dict(preset.dag, config)

    if preset.is_auto or preset.workers is None:
        raise ValueError("Cannot build a structural DAG from an auto preset")

    worker_models = _resolve_worker_models(preset, config)
    n_workers = preset.workers
    stages: list[Stage] = []
    edges: list[tuple[str, str]] = []

    worker_ids: list[str] = []
    for i in range(n_workers):
        wid = f"worker_{i + 1}"
        worker_ids.append(wid)
        stages.append(Stage(id=wid, kind="worker", model=worker_models[i % len(worker_models)]))

    aggregator_models = _resolve_aggregator_models(preset, config)
    aggregator_ids: list[str] = []
    for i, jm in enumerate(aggregator_models):
        jid = "aggregator" if i == 0 else f"aggregator_{i + 1}"
        aggregator_ids.append(jid)
        stages.append(Stage(id=jid, kind="aggregator", model=jm, depends_on=list(worker_ids)))
        for wid in worker_ids:
            edges.append((wid, jid))

    if preset.audit:
        audit_model = config.resolve_model_alias(preset.audit)
        stages.append(Stage(id="audit", kind="audit", model=audit_model, depends_on=[aggregator_ids[0]]))
        edges.append((aggregator_ids[0], "audit"))

    if len(aggregator_ids) > 1:
        merge_model = config.defaults.default_aggregator
        merge_id = "merge"
        stages.append(Stage(id=merge_id, kind="merge", model=merge_model, depends_on=list(aggregator_ids)))
        for jid in aggregator_ids:
            edges.append((jid, merge_id))

    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, )
    return dag

mutants_x_build_preset_dag__mutmut['_mutmut_orig'] = x_build_preset_dag__mutmut_orig # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_1'] = x_build_preset_dag__mutmut_1 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_2'] = x_build_preset_dag__mutmut_2 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_3'] = x_build_preset_dag__mutmut_3 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_4'] = x_build_preset_dag__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_5'] = x_build_preset_dag__mutmut_5 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_6'] = x_build_preset_dag__mutmut_6 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_7'] = x_build_preset_dag__mutmut_7 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_8'] = x_build_preset_dag__mutmut_8 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_9'] = x_build_preset_dag__mutmut_9 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_10'] = x_build_preset_dag__mutmut_10 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_11'] = x_build_preset_dag__mutmut_11 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_12'] = x_build_preset_dag__mutmut_12 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_13'] = x_build_preset_dag__mutmut_13 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_14'] = x_build_preset_dag__mutmut_14 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_15'] = x_build_preset_dag__mutmut_15 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_16'] = x_build_preset_dag__mutmut_16 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_17'] = x_build_preset_dag__mutmut_17 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_18'] = x_build_preset_dag__mutmut_18 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_19'] = x_build_preset_dag__mutmut_19 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_20'] = x_build_preset_dag__mutmut_20 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_21'] = x_build_preset_dag__mutmut_21 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_22'] = x_build_preset_dag__mutmut_22 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_23'] = x_build_preset_dag__mutmut_23 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_24'] = x_build_preset_dag__mutmut_24 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_25'] = x_build_preset_dag__mutmut_25 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_26'] = x_build_preset_dag__mutmut_26 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_27'] = x_build_preset_dag__mutmut_27 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_28'] = x_build_preset_dag__mutmut_28 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_29'] = x_build_preset_dag__mutmut_29 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_30'] = x_build_preset_dag__mutmut_30 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_31'] = x_build_preset_dag__mutmut_31 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_32'] = x_build_preset_dag__mutmut_32 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_33'] = x_build_preset_dag__mutmut_33 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_34'] = x_build_preset_dag__mutmut_34 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_35'] = x_build_preset_dag__mutmut_35 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_36'] = x_build_preset_dag__mutmut_36 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_37'] = x_build_preset_dag__mutmut_37 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_38'] = x_build_preset_dag__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_39'] = x_build_preset_dag__mutmut_39 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_40'] = x_build_preset_dag__mutmut_40 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_41'] = x_build_preset_dag__mutmut_41 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_42'] = x_build_preset_dag__mutmut_42 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_43'] = x_build_preset_dag__mutmut_43 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_44'] = x_build_preset_dag__mutmut_44 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_45'] = x_build_preset_dag__mutmut_45 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_46'] = x_build_preset_dag__mutmut_46 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_47'] = x_build_preset_dag__mutmut_47 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_48'] = x_build_preset_dag__mutmut_48 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_49'] = x_build_preset_dag__mutmut_49 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_50'] = x_build_preset_dag__mutmut_50 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_51'] = x_build_preset_dag__mutmut_51 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_52'] = x_build_preset_dag__mutmut_52 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_53'] = x_build_preset_dag__mutmut_53 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_54'] = x_build_preset_dag__mutmut_54 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_55'] = x_build_preset_dag__mutmut_55 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_56'] = x_build_preset_dag__mutmut_56 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_57'] = x_build_preset_dag__mutmut_57 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_58'] = x_build_preset_dag__mutmut_58 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_59'] = x_build_preset_dag__mutmut_59 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_60'] = x_build_preset_dag__mutmut_60 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_61'] = x_build_preset_dag__mutmut_61 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_62'] = x_build_preset_dag__mutmut_62 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_63'] = x_build_preset_dag__mutmut_63 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_64'] = x_build_preset_dag__mutmut_64 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_65'] = x_build_preset_dag__mutmut_65 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_66'] = x_build_preset_dag__mutmut_66 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_67'] = x_build_preset_dag__mutmut_67 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_68'] = x_build_preset_dag__mutmut_68 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_69'] = x_build_preset_dag__mutmut_69 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_70'] = x_build_preset_dag__mutmut_70 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_71'] = x_build_preset_dag__mutmut_71 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_72'] = x_build_preset_dag__mutmut_72 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_73'] = x_build_preset_dag__mutmut_73 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_74'] = x_build_preset_dag__mutmut_74 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_75'] = x_build_preset_dag__mutmut_75 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_76'] = x_build_preset_dag__mutmut_76 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_77'] = x_build_preset_dag__mutmut_77 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_78'] = x_build_preset_dag__mutmut_78 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_79'] = x_build_preset_dag__mutmut_79 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_80'] = x_build_preset_dag__mutmut_80 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_81'] = x_build_preset_dag__mutmut_81 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_82'] = x_build_preset_dag__mutmut_82 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_83'] = x_build_preset_dag__mutmut_83 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_84'] = x_build_preset_dag__mutmut_84 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_85'] = x_build_preset_dag__mutmut_85 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_86'] = x_build_preset_dag__mutmut_86 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_87'] = x_build_preset_dag__mutmut_87 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_88'] = x_build_preset_dag__mutmut_88 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_89'] = x_build_preset_dag__mutmut_89 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_90'] = x_build_preset_dag__mutmut_90 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_91'] = x_build_preset_dag__mutmut_91 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_92'] = x_build_preset_dag__mutmut_92 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_93'] = x_build_preset_dag__mutmut_93 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_94'] = x_build_preset_dag__mutmut_94 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_95'] = x_build_preset_dag__mutmut_95 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_96'] = x_build_preset_dag__mutmut_96 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_97'] = x_build_preset_dag__mutmut_97 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_98'] = x_build_preset_dag__mutmut_98 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_99'] = x_build_preset_dag__mutmut_99 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_100'] = x_build_preset_dag__mutmut_100 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_101'] = x_build_preset_dag__mutmut_101 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_102'] = x_build_preset_dag__mutmut_102 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_103'] = x_build_preset_dag__mutmut_103 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_104'] = x_build_preset_dag__mutmut_104 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_105'] = x_build_preset_dag__mutmut_105 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_106'] = x_build_preset_dag__mutmut_106 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_107'] = x_build_preset_dag__mutmut_107 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_108'] = x_build_preset_dag__mutmut_108 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_109'] = x_build_preset_dag__mutmut_109 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_110'] = x_build_preset_dag__mutmut_110 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_111'] = x_build_preset_dag__mutmut_111 # type: ignore # mutmut generated
mutants_x_build_preset_dag__mutmut['x_build_preset_dag__mutmut_112'] = x_build_preset_dag__mutmut_112 # type: ignore # mutmut generated
mutants_x__resolve_worker_models__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__resolve_worker_models__mutmut)
def _resolve_worker_models(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(m) for m in preset.worker_models]
    return [config.defaults.default_worker] * (preset.workers or 1)


def x__resolve_worker_models__mutmut_orig(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(m) for m in preset.worker_models]
    return [config.defaults.default_worker] * (preset.workers or 1)


def x__resolve_worker_models__mutmut_1(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(None) for m in preset.worker_models]
    return [config.defaults.default_worker] * (preset.workers or 1)


def x__resolve_worker_models__mutmut_2(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(m) for m in preset.worker_models]
    return [config.defaults.default_worker] / (preset.workers or 1)


def x__resolve_worker_models__mutmut_3(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(m) for m in preset.worker_models]
    return [config.defaults.default_worker] * (preset.workers and 1)


def x__resolve_worker_models__mutmut_4(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.worker_models:
        return [config.resolve_model_alias(m) for m in preset.worker_models]
    return [config.defaults.default_worker] * (preset.workers or 2)

mutants_x__resolve_worker_models__mutmut['_mutmut_orig'] = x__resolve_worker_models__mutmut_orig # type: ignore # mutmut generated
mutants_x__resolve_worker_models__mutmut['x__resolve_worker_models__mutmut_1'] = x__resolve_worker_models__mutmut_1 # type: ignore # mutmut generated
mutants_x__resolve_worker_models__mutmut['x__resolve_worker_models__mutmut_2'] = x__resolve_worker_models__mutmut_2 # type: ignore # mutmut generated
mutants_x__resolve_worker_models__mutmut['x__resolve_worker_models__mutmut_3'] = x__resolve_worker_models__mutmut_3 # type: ignore # mutmut generated
mutants_x__resolve_worker_models__mutmut['x__resolve_worker_models__mutmut_4'] = x__resolve_worker_models__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_build_dag_from_dict__mutmut)
def build_dag_from_dict(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_orig(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_1(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = None
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_2(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") and []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_3(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get(None) or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_4(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("XXstagesXX") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_5(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("STAGES") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_6(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) and not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_7(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_8(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_9(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError(None)
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_10(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("XXClient DAG must define a non-empty 'stages' listXX")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_11(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("client dag must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_12(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("CLIENT DAG MUST DEFINE A NON-EMPTY 'STAGES' LIST")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_13(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = None
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_14(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) and "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_15(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_16(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "XXidXX" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_17(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "ID" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_18(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_19(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError(None)
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_20(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("XXEach DAG stage must be a mapping with an 'id'XX")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_21(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("each dag stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_22(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("EACH DAG STAGE MUST BE A MAPPING WITH AN 'ID'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_23(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            None
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_24(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=None,
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_25(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=None,
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_26(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=None,
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_27(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=None,
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_28(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_29(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_30(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_31(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_32(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["XXidXX"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_33(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["ID"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_34(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get(None, "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_35(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", None),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_36(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_37(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", ),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_38(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("XXkindXX", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_39(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("KIND", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_40(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "XXworkerXX"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_41(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "WORKER"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_42(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(None),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_43(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get(None, "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_44(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", None)),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_45(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_46(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", )),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_47(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("XXmodelXX", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_48(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("MODEL", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_49(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "XXXX")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_50(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(None),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_51(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get(None, [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_52(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", None)),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_53(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get([])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_54(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", )),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_55(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("XXdepends_onXX", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_56(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("DEPENDS_ON", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_57(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = None
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_58(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(None) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_59(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") and [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_60(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get(None) or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_61(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("XXedgesXX") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_62(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("EDGES") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_63(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = None
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_64(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=None, edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_65(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=None)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_66(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(edges=edges)
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_67(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, )
    _validate_dag(dag, config)
    return dag


def x_build_dag_from_dict__mutmut_68(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(None, config)
    return dag


def x_build_dag_from_dict__mutmut_69(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, None)
    return dag


def x_build_dag_from_dict__mutmut_70(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(config)
    return dag


def x_build_dag_from_dict__mutmut_71(dag_dict: dict[str, Any], config: ChimeraConfig) -> FormationDAG:
    """Parse a client/config-defined DAG mapping into a validated :class:`FormationDAG`.

    Expected shape::

        {"stages": [{"id", "kind", "model", "depends_on"}, ...], "edges": [["a","b"], ...]}

    Validation (via :func:`_validate_dag`):
    * every ``depends_on`` references a known stage id,
    * every stage ``model`` exists in the catalog (``"default"`` alias resolved),
    * at least one ``worker`` stage,
    * at least one ``aggregator``/``merge``/``audit`` stage,
    * no cycles.
    """
    stages_raw = dag_dict.get("stages") or []
    if not isinstance(stages_raw, list) or not stages_raw:
        raise ValueError("Client DAG must define a non-empty 'stages' list")
    stages: list[Stage] = []
    for s in stages_raw:
        if not isinstance(s, dict) or "id" not in s:
            raise ValueError("Each DAG stage must be a mapping with an 'id'")
        stages.append(
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=config.resolve_model_alias(s.get("model", "")),
                depends_on=list(s.get("depends_on", [])),
            )
        )
    edges = [tuple(e) for e in (dag_dict.get("edges") or [])]
    dag = FormationDAG(stages=stages, edges=edges)
    _validate_dag(dag, )
    return dag

mutants_x_build_dag_from_dict__mutmut['_mutmut_orig'] = x_build_dag_from_dict__mutmut_orig # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_1'] = x_build_dag_from_dict__mutmut_1 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_2'] = x_build_dag_from_dict__mutmut_2 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_3'] = x_build_dag_from_dict__mutmut_3 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_4'] = x_build_dag_from_dict__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_5'] = x_build_dag_from_dict__mutmut_5 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_6'] = x_build_dag_from_dict__mutmut_6 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_7'] = x_build_dag_from_dict__mutmut_7 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_8'] = x_build_dag_from_dict__mutmut_8 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_9'] = x_build_dag_from_dict__mutmut_9 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_10'] = x_build_dag_from_dict__mutmut_10 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_11'] = x_build_dag_from_dict__mutmut_11 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_12'] = x_build_dag_from_dict__mutmut_12 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_13'] = x_build_dag_from_dict__mutmut_13 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_14'] = x_build_dag_from_dict__mutmut_14 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_15'] = x_build_dag_from_dict__mutmut_15 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_16'] = x_build_dag_from_dict__mutmut_16 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_17'] = x_build_dag_from_dict__mutmut_17 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_18'] = x_build_dag_from_dict__mutmut_18 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_19'] = x_build_dag_from_dict__mutmut_19 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_20'] = x_build_dag_from_dict__mutmut_20 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_21'] = x_build_dag_from_dict__mutmut_21 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_22'] = x_build_dag_from_dict__mutmut_22 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_23'] = x_build_dag_from_dict__mutmut_23 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_24'] = x_build_dag_from_dict__mutmut_24 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_25'] = x_build_dag_from_dict__mutmut_25 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_26'] = x_build_dag_from_dict__mutmut_26 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_27'] = x_build_dag_from_dict__mutmut_27 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_28'] = x_build_dag_from_dict__mutmut_28 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_29'] = x_build_dag_from_dict__mutmut_29 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_30'] = x_build_dag_from_dict__mutmut_30 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_31'] = x_build_dag_from_dict__mutmut_31 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_32'] = x_build_dag_from_dict__mutmut_32 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_33'] = x_build_dag_from_dict__mutmut_33 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_34'] = x_build_dag_from_dict__mutmut_34 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_35'] = x_build_dag_from_dict__mutmut_35 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_36'] = x_build_dag_from_dict__mutmut_36 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_37'] = x_build_dag_from_dict__mutmut_37 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_38'] = x_build_dag_from_dict__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_39'] = x_build_dag_from_dict__mutmut_39 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_40'] = x_build_dag_from_dict__mutmut_40 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_41'] = x_build_dag_from_dict__mutmut_41 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_42'] = x_build_dag_from_dict__mutmut_42 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_43'] = x_build_dag_from_dict__mutmut_43 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_44'] = x_build_dag_from_dict__mutmut_44 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_45'] = x_build_dag_from_dict__mutmut_45 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_46'] = x_build_dag_from_dict__mutmut_46 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_47'] = x_build_dag_from_dict__mutmut_47 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_48'] = x_build_dag_from_dict__mutmut_48 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_49'] = x_build_dag_from_dict__mutmut_49 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_50'] = x_build_dag_from_dict__mutmut_50 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_51'] = x_build_dag_from_dict__mutmut_51 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_52'] = x_build_dag_from_dict__mutmut_52 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_53'] = x_build_dag_from_dict__mutmut_53 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_54'] = x_build_dag_from_dict__mutmut_54 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_55'] = x_build_dag_from_dict__mutmut_55 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_56'] = x_build_dag_from_dict__mutmut_56 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_57'] = x_build_dag_from_dict__mutmut_57 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_58'] = x_build_dag_from_dict__mutmut_58 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_59'] = x_build_dag_from_dict__mutmut_59 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_60'] = x_build_dag_from_dict__mutmut_60 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_61'] = x_build_dag_from_dict__mutmut_61 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_62'] = x_build_dag_from_dict__mutmut_62 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_63'] = x_build_dag_from_dict__mutmut_63 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_64'] = x_build_dag_from_dict__mutmut_64 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_65'] = x_build_dag_from_dict__mutmut_65 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_66'] = x_build_dag_from_dict__mutmut_66 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_67'] = x_build_dag_from_dict__mutmut_67 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_68'] = x_build_dag_from_dict__mutmut_68 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_69'] = x_build_dag_from_dict__mutmut_69 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_70'] = x_build_dag_from_dict__mutmut_70 # type: ignore # mutmut generated
mutants_x_build_dag_from_dict__mutmut['x_build_dag_from_dict__mutmut_71'] = x_build_dag_from_dict__mutmut_71 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__resolve_aggregator_models__mutmut)
def _resolve_aggregator_models(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator or "default"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_orig(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator or "default"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_1(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(None) for j in preset.aggregators]
    agg_name = preset.aggregator or "default"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_2(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = None
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_3(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator and "default"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_4(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator or "XXdefaultXX"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_5(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator or "DEFAULT"
    return [config.resolve_model_alias(agg_name)]


def x__resolve_aggregator_models__mutmut_6(preset: FormationPreset, config: ChimeraConfig) -> list[str]:
    if preset.aggregators:
        return [config.resolve_model_alias(j) for j in preset.aggregators]
    agg_name = preset.aggregator or "default"
    return [config.resolve_model_alias(None)]

mutants_x__resolve_aggregator_models__mutmut['_mutmut_orig'] = x__resolve_aggregator_models__mutmut_orig # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_1'] = x__resolve_aggregator_models__mutmut_1 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_2'] = x__resolve_aggregator_models__mutmut_2 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_3'] = x__resolve_aggregator_models__mutmut_3 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_4'] = x__resolve_aggregator_models__mutmut_4 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_5'] = x__resolve_aggregator_models__mutmut_5 # type: ignore # mutmut generated
mutants_x__resolve_aggregator_models__mutmut['x__resolve_aggregator_models__mutmut_6'] = x__resolve_aggregator_models__mutmut_6 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__has_cycle__mutmut)
def _has_cycle(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_orig(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_1(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = None
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_2(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = None  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_3(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = None
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_4(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(None)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_5(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st != 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_6(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 2:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_7(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return True
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_8(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st != 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_9(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 1:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_10(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return False
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_11(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = None
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_12(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 1
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_13(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = None
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_14(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(None)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_15(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_16(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id or visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_17(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep not in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_18(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(None):
                    return True
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_19(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return False
        state[node_id] = 1
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_20(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = None
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_21(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 2
        return False

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_22(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return True

    return any(visit(s.id) for s in dag.stages)


def x__has_cycle__mutmut_23(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(None)


def x__has_cycle__mutmut_24(dag: FormationDAG) -> bool:
    """Return True if the DAG's dependency graph contains a cycle."""
    by_id = {s.id: s for s in dag.stages}
    state: dict[str, int] = {}  # 0=visiting, 1=done

    def visit(node_id: str) -> bool:
        st = state.get(node_id)
        if st == 1:
            return False
        if st == 0:
            return True
        state[node_id] = 0
        node = by_id.get(node_id)
        if node is not None:
            for dep in node.depends_on:
                if dep in by_id and visit(dep):
                    return True
        state[node_id] = 1
        return False

    return any(visit(None) for s in dag.stages)

mutants_x__has_cycle__mutmut['_mutmut_orig'] = x__has_cycle__mutmut_orig # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_1'] = x__has_cycle__mutmut_1 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_2'] = x__has_cycle__mutmut_2 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_3'] = x__has_cycle__mutmut_3 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_4'] = x__has_cycle__mutmut_4 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_5'] = x__has_cycle__mutmut_5 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_6'] = x__has_cycle__mutmut_6 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_7'] = x__has_cycle__mutmut_7 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_8'] = x__has_cycle__mutmut_8 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_9'] = x__has_cycle__mutmut_9 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_10'] = x__has_cycle__mutmut_10 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_11'] = x__has_cycle__mutmut_11 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_12'] = x__has_cycle__mutmut_12 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_13'] = x__has_cycle__mutmut_13 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_14'] = x__has_cycle__mutmut_14 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_15'] = x__has_cycle__mutmut_15 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_16'] = x__has_cycle__mutmut_16 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_17'] = x__has_cycle__mutmut_17 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_18'] = x__has_cycle__mutmut_18 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_19'] = x__has_cycle__mutmut_19 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_20'] = x__has_cycle__mutmut_20 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_21'] = x__has_cycle__mutmut_21 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_22'] = x__has_cycle__mutmut_22 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_23'] = x__has_cycle__mutmut_23 # type: ignore # mutmut generated
mutants_x__has_cycle__mutmut['x__has_cycle__mutmut_24'] = x__has_cycle__mutmut_24 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_dag__mutmut)
def _validate_dag(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_orig(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_1(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = None
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_2(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(None)
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_3(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) == len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_4(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError(None)
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_5(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("XXFormation DAG has duplicate stage idsXX")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_6(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("formation dag has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_7(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("FORMATION DAG HAS DUPLICATE STAGE IDS")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_8(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_9(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(None)
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_10(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_11(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(None)
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_12(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_13(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(None):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_14(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind != "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_15(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "XXworkerXX" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_16(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "WORKER" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_17(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError(None)
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_18(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("XXFormation has no worker stagesXX")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_19(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_20(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("FORMATION HAS NO WORKER STAGES")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_21(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_22(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(None):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_23(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind not in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_24(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"XXaggregatorXX", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_25(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"AGGREGATOR", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_26(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "XXmergeXX", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_27(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "MERGE", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_28(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "XXauditXX"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_29(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "AUDIT"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_30(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError(None)
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_31(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("XXFormation has no aggregator/merge/audit stagesXX")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_32(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_33(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("FORMATION HAS NO AGGREGATOR/MERGE/AUDIT STAGES")
    if _has_cycle(dag):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_34(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(None):
        raise ValueError("Formation DAG contains a cycle")


def x__validate_dag__mutmut_35(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError(None)


def x__validate_dag__mutmut_36(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("XXFormation DAG contains a cycleXX")


def x__validate_dag__mutmut_37(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("formation dag contains a cycle")


def x__validate_dag__mutmut_38(dag: FormationDAG, config: ChimeraConfig) -> None:
    ids = set(dag.stage_ids())
    if len(ids) != len(dag.stages):
        raise ValueError("Formation DAG has duplicate stage ids")
    for stage in dag.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"Stage {stage.id!r} depends on unknown {dep!r}")
        if stage.model not in config.models:
            raise ValueError(f"Stage {stage.id!r} uses unknown model {stage.model!r}")
    if not any(s.kind == "worker" for s in dag.stages):
        raise ValueError("Formation has no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in dag.stages):
        raise ValueError("Formation has no aggregator/merge/audit stages")
    if _has_cycle(dag):
        raise ValueError("FORMATION DAG CONTAINS A CYCLE")

mutants_x__validate_dag__mutmut['_mutmut_orig'] = x__validate_dag__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_1'] = x__validate_dag__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_2'] = x__validate_dag__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_3'] = x__validate_dag__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_4'] = x__validate_dag__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_5'] = x__validate_dag__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_6'] = x__validate_dag__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_7'] = x__validate_dag__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_8'] = x__validate_dag__mutmut_8 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_9'] = x__validate_dag__mutmut_9 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_10'] = x__validate_dag__mutmut_10 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_11'] = x__validate_dag__mutmut_11 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_12'] = x__validate_dag__mutmut_12 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_13'] = x__validate_dag__mutmut_13 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_14'] = x__validate_dag__mutmut_14 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_15'] = x__validate_dag__mutmut_15 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_16'] = x__validate_dag__mutmut_16 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_17'] = x__validate_dag__mutmut_17 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_18'] = x__validate_dag__mutmut_18 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_19'] = x__validate_dag__mutmut_19 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_20'] = x__validate_dag__mutmut_20 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_21'] = x__validate_dag__mutmut_21 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_22'] = x__validate_dag__mutmut_22 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_23'] = x__validate_dag__mutmut_23 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_24'] = x__validate_dag__mutmut_24 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_25'] = x__validate_dag__mutmut_25 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_26'] = x__validate_dag__mutmut_26 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_27'] = x__validate_dag__mutmut_27 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_28'] = x__validate_dag__mutmut_28 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_29'] = x__validate_dag__mutmut_29 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_30'] = x__validate_dag__mutmut_30 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_31'] = x__validate_dag__mutmut_31 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_32'] = x__validate_dag__mutmut_32 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_33'] = x__validate_dag__mutmut_33 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_34'] = x__validate_dag__mutmut_34 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_35'] = x__validate_dag__mutmut_35 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_36'] = x__validate_dag__mutmut_36 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_37'] = x__validate_dag__mutmut_37 # type: ignore # mutmut generated
mutants_x__validate_dag__mutmut['x__validate_dag__mutmut_38'] = x__validate_dag__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


@_mutmut_mutated(mutants_x_build_dispatcher_prompt__mutmut)
def build_dispatcher_prompt(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_orig(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_1(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = None
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_2(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = None

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_3(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(None, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_4(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=None)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_5(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_6(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, )

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_7(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=3)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_8(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = None

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_9(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "XX\n## Worker Prompt Rules (STRICT — failures degrade the output)\nXX"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_10(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## worker prompt rules (strict — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_11(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## WORKER PROMPT RULES (STRICT — FAILURES DEGRADE THE OUTPUT)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_12(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "XX- EVERY entry in worker_prompts[] MUST have a non-empty, specific, XX"
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_13(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- every entry in worker_prompts[] must have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_14(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY ENTRY IN WORKER_PROMPTS[] MUST HAVE A NON-EMPTY, SPECIFIC, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_15(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "XXdomain-scoped `prompt`.\nXX"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_16(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "DOMAIN-SCOPED `PROMPT`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_17(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "XX- Do NOT leave worker_prompts[].prompt empty.\nXX"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_18(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- do not leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_19(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- DO NOT LEAVE WORKER_PROMPTS[].PROMPT EMPTY.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_20(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "XX- Do NOT copy the user's request verbatim into a worker prompt.\nXX"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_21(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- do not copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_22(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- DO NOT COPY THE USER'S REQUEST VERBATIM INTO A WORKER PROMPT.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_23(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "XX- Each worker must receive a DISTINCT subtask focused on what THAT model XX"
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_24(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- each worker must receive a distinct subtask focused on what that model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_25(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- EACH WORKER MUST RECEIVE A DISTINCT SUBTASK FOCUSED ON WHAT THAT MODEL "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_26(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "XXis best at (per the category weights above).\nXX"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_27(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "IS BEST AT (PER THE CATEGORY WEIGHTS ABOVE).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_28(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "XX- Each worker_prompts[].stage_id MUST match a formation worker stage id XX"
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_29(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- each worker_prompts[].stage_id must match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_30(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- EACH WORKER_PROMPTS[].STAGE_ID MUST MATCH A FORMATION WORKER STAGE ID "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_31(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "XXexactly (case-sensitive).XX"
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_32(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "EXACTLY (CASE-SENSITIVE)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_33(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is not None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_34(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = None
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_35(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "XXYou are in AUTO mode. Design the entire formation yourself.\nXX"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_36(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "you are in auto mode. design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_37(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "YOU ARE IN AUTO MODE. DESIGN THE ENTIRE FORMATION YOURSELF.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_38(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "XX1. Analyze the task and the domains it touches XX"
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_39(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_40(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. ANALYZE THE TASK AND THE DOMAINS IT TOUCHES "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_41(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "XX(code, analysis, design, audit, reasoning, ...).\nXX"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_42(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(CODE, ANALYSIS, DESIGN, AUDIT, REASONING, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_43(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "XX2. Pick the best model for each subtask using the category weights above.\nXX"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_44(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_45(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. PICK THE BEST MODEL FOR EACH SUBTASK USING THE CATEGORY WEIGHTS ABOVE.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_46(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "XX3. Design a formation DAG: parallel workers, then an aggregator that merges. XX"
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_47(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. design a formation dag: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_48(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. DESIGN A FORMATION DAG: PARALLEL WORKERS, THEN AN AGGREGATOR THAT MERGES. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_49(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "XXYou may add an audit stage or multiple aggregators with a merge stage.\nXX"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_50(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "you may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_51(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "YOU MAY ADD AN AUDIT STAGE OR MULTIPLE AGGREGATORS WITH A MERGE STAGE.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_52(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "XX4. Write a CUSTOM prompt for each worker, scoped to what that model is XX"
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_53(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. write a custom prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_54(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. WRITE A CUSTOM PROMPT FOR EACH WORKER, SCOPED TO WHAT THAT MODEL IS "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_55(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "XXgood at. Do NOT give every worker the same prompt unless consensus is XX"
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_56(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. do not give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_57(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "GOOD AT. DO NOT GIVE EVERY WORKER THE SAME PROMPT UNLESS CONSENSUS IS "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_58(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "XXrequired.\nXX"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_59(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "REQUIRED.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_60(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "XX5. Write aggregator_instructions telling the aggregator exactly what each worker was XX"
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_61(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_62(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. WRITE AGGREGATOR_INSTRUCTIONS TELLING THE AGGREGATOR EXACTLY WHAT EACH WORKER WAS "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_63(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "XXasked to do and how to combine their outputs into the final answer.\nXX"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_64(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "ASKED TO DO AND HOW TO COMBINE THEIR OUTPUTS INTO THE FINAL ANSWER.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_65(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "XX6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\nXX"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_66(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. use only models from the catalog. keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_67(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. USE ONLY MODELS FROM THE CATALOG. KEEP IT SMALL: 2-4 WORKERS IS IDEAL.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_68(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = None
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_69(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "XXDesign the `formation` field yourself.XX"
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_70(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_71(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "DESIGN THE `FORMATION` FIELD YOURSELF."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_72(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = None
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_73(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "XXA fixed formation has already been chosen. Do NOT change the structure.\nXX"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_74(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "a fixed formation has already been chosen. do not change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_75(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A FIXED FORMATION HAS ALREADY BEEN CHOSEN. DO NOT CHANGE THE STRUCTURE.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_76(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "XXYour only job: write a CUSTOM prompt for each worker and the merge XX"
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_77(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "your only job: write a custom prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_78(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "YOUR ONLY JOB: WRITE A CUSTOM PROMPT FOR EACH WORKER AND THE MERGE "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_79(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "XXinstructions for the aggregator, tailored to this specific task and to each XX"
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_80(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "INSTRUCTIONS FOR THE AGGREGATOR, TAILORED TO THIS SPECIFIC TASK AND TO EACH "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_81(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "XXmodel's strengths from the category weights above.\nXX"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_82(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "MODEL'S STRENGTHS FROM THE CATEGORY WEIGHTS ABOVE.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_83(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = None
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_84(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "XXUse this EXACT formation (return it unchanged in the `formation` field):\nXX"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_85(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "use this exact formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_86(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "USE THIS EXACT FORMATION (RETURN IT UNCHANGED IN THE `FORMATION` FIELD):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_87(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(None, indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_88(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=None)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_89(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_90(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), )}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_91(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode=None), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_92(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='XXjsonXX'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_93(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='JSON'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_94(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=3)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_95(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = None
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_96(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind != "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_97(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "XXworkerXX"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_98(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "WORKER"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_99(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block = (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_100(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block -= (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_101(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "XX\nThe worker stage ids you MUST address in worker_prompts[] XX"
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_102(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nthe worker stage ids you must address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_103(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nTHE WORKER STAGE IDS YOU MUST ADDRESS IN WORKER_PROMPTS[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_104(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = None
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_105(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "XXYou are the Chimera dispatcher. Your job: design (or fill in) a XX"
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_106(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "you are the chimera dispatcher. your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_107(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "YOU ARE THE CHIMERA DISPATCHER. YOUR JOB: DESIGN (OR FILL IN) A "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_108(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "XXmulti-model deliberation that solves the user's task by splitting it XX"
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_109(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "MULTI-MODEL DELIBERATION THAT SOLVES THE USER'S TASK BY SPLITTING IT "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_110(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "XXacross models according to their strengths.\n\nXX"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_111(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "ACROSS MODELS ACCORDING TO THEIR STRENGTHS.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_112(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "XX## Output\nXX"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_113(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_114(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## OUTPUT\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_115(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "XXRespond with ONLY a JSON object (no markdown fences) matching:\nXX"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_116(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "respond with only a json object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_117(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "RESPOND WITH ONLY A JSON OBJECT (NO MARKDOWN FENCES) MATCHING:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_118(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"XXroleXX": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_119(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"ROLE": "system", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_120(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "XXsystemXX", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_121(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "SYSTEM", "content": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_122(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "XXcontentXX": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_123(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "CONTENT": system},
        {"role": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_124(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"XXroleXX": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_125(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"ROLE": "user", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_126(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "XXuserXX", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_127(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "USER", "content": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_128(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "XXcontentXX": f"User's request:\n{user_prompt}"},
    ]


# --------------------------------------------------------------------------- #
# Dispatcher prompt construction
# --------------------------------------------------------------------------- #


def x_build_dispatcher_prompt__mutmut_129(
    user_prompt: str,
    config: ChimeraConfig,
    *,
    fixed_dag: FormationDAG | None = None,
) -> list[dict[str, str]]:
    """Build the message list for the dispatcher model call."""
    catalog = config.catalog_description()
    schema_str = json.dumps(DISPATCH_SCHEMA_HINT, indent=2)

    # STRICT worker-prompt rules — prevent empty / verbatim / generic prompts.
    enforcement = (
        "\n## Worker Prompt Rules (STRICT — failures degrade the output)\n"
        "- EVERY entry in worker_prompts[] MUST have a non-empty, specific, "
        "domain-scoped `prompt`.\n"
        "- Do NOT leave worker_prompts[].prompt empty.\n"
        "- Do NOT copy the user's request verbatim into a worker prompt.\n"
        "- Each worker must receive a DISTINCT subtask focused on what THAT model "
        "is best at (per the category weights above).\n"
        "- Each worker_prompts[].stage_id MUST match a formation worker stage id "
        "exactly (case-sensitive)."
    )

    if fixed_dag is None:
        task_block = (
            "You are in AUTO mode. Design the entire formation yourself.\n"
            "1. Analyze the task and the domains it touches "
            "(code, analysis, design, audit, reasoning, ...).\n"
            "2. Pick the best model for each subtask using the category weights above.\n"
            "3. Design a formation DAG: parallel workers, then an aggregator that merges. "
            "You may add an audit stage or multiple aggregators with a merge stage.\n"
            "4. Write a CUSTOM prompt for each worker, scoped to what that model is "
            "good at. Do NOT give every worker the same prompt unless consensus is "
            "required.\n"
            "5. Write aggregator_instructions telling the aggregator exactly what each worker was "
            "asked to do and how to combine their outputs into the final answer.\n"
            "6. Use only models from the catalog. Keep it small: 2-4 workers is ideal.\n"
        )
        formation_block = "Design the `formation` field yourself."
    else:
        task_block = (
            "A fixed formation has already been chosen. Do NOT change the structure.\n"
            "Your only job: write a CUSTOM prompt for each worker and the merge "
            "instructions for the aggregator, tailored to this specific task and to each "
            "model's strengths from the category weights above.\n"
        )
        formation_block = (
            "Use this EXACT formation (return it unchanged in the `formation` field):\n"
            f"{json.dumps(fixed_dag.model_dump(mode='json'), indent=2)}"
        )
        # Surface the exact worker stage ids so the dispatcher addresses them
        # with the correct (case-sensitive) stage_id values.
        worker_ids = [s.id for s in fixed_dag.stages if s.kind == "worker"]
        if worker_ids:
            formation_block += (
                "\nThe worker stage ids you MUST address in worker_prompts[] "
                f"(use these EXACT stage_id values): {worker_ids}"
            )

    system = (
        "You are the Chimera dispatcher. Your job: design (or fill in) a "
        "multi-model deliberation that solves the user's task by splitting it "
        "across models according to their strengths.\n\n"
        f"## Available Models\n{catalog}\n\n"
        f"## Your Task\n{task_block}\n"
        f"{formation_block}\n"
        f"{enforcement}\n\n"
        "## Output\n"
        "Respond with ONLY a JSON object (no markdown fences) matching:\n"
        f"{schema_str}"
    )
    return [
        {"role": "system", "content": system},
        {"role": "user", "CONTENT": f"User's request:\n{user_prompt}"},
    ]

mutants_x_build_dispatcher_prompt__mutmut['_mutmut_orig'] = x_build_dispatcher_prompt__mutmut_orig # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_1'] = x_build_dispatcher_prompt__mutmut_1 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_2'] = x_build_dispatcher_prompt__mutmut_2 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_3'] = x_build_dispatcher_prompt__mutmut_3 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_4'] = x_build_dispatcher_prompt__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_5'] = x_build_dispatcher_prompt__mutmut_5 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_6'] = x_build_dispatcher_prompt__mutmut_6 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_7'] = x_build_dispatcher_prompt__mutmut_7 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_8'] = x_build_dispatcher_prompt__mutmut_8 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_9'] = x_build_dispatcher_prompt__mutmut_9 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_10'] = x_build_dispatcher_prompt__mutmut_10 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_11'] = x_build_dispatcher_prompt__mutmut_11 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_12'] = x_build_dispatcher_prompt__mutmut_12 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_13'] = x_build_dispatcher_prompt__mutmut_13 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_14'] = x_build_dispatcher_prompt__mutmut_14 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_15'] = x_build_dispatcher_prompt__mutmut_15 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_16'] = x_build_dispatcher_prompt__mutmut_16 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_17'] = x_build_dispatcher_prompt__mutmut_17 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_18'] = x_build_dispatcher_prompt__mutmut_18 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_19'] = x_build_dispatcher_prompt__mutmut_19 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_20'] = x_build_dispatcher_prompt__mutmut_20 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_21'] = x_build_dispatcher_prompt__mutmut_21 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_22'] = x_build_dispatcher_prompt__mutmut_22 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_23'] = x_build_dispatcher_prompt__mutmut_23 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_24'] = x_build_dispatcher_prompt__mutmut_24 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_25'] = x_build_dispatcher_prompt__mutmut_25 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_26'] = x_build_dispatcher_prompt__mutmut_26 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_27'] = x_build_dispatcher_prompt__mutmut_27 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_28'] = x_build_dispatcher_prompt__mutmut_28 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_29'] = x_build_dispatcher_prompt__mutmut_29 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_30'] = x_build_dispatcher_prompt__mutmut_30 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_31'] = x_build_dispatcher_prompt__mutmut_31 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_32'] = x_build_dispatcher_prompt__mutmut_32 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_33'] = x_build_dispatcher_prompt__mutmut_33 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_34'] = x_build_dispatcher_prompt__mutmut_34 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_35'] = x_build_dispatcher_prompt__mutmut_35 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_36'] = x_build_dispatcher_prompt__mutmut_36 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_37'] = x_build_dispatcher_prompt__mutmut_37 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_38'] = x_build_dispatcher_prompt__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_39'] = x_build_dispatcher_prompt__mutmut_39 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_40'] = x_build_dispatcher_prompt__mutmut_40 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_41'] = x_build_dispatcher_prompt__mutmut_41 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_42'] = x_build_dispatcher_prompt__mutmut_42 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_43'] = x_build_dispatcher_prompt__mutmut_43 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_44'] = x_build_dispatcher_prompt__mutmut_44 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_45'] = x_build_dispatcher_prompt__mutmut_45 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_46'] = x_build_dispatcher_prompt__mutmut_46 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_47'] = x_build_dispatcher_prompt__mutmut_47 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_48'] = x_build_dispatcher_prompt__mutmut_48 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_49'] = x_build_dispatcher_prompt__mutmut_49 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_50'] = x_build_dispatcher_prompt__mutmut_50 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_51'] = x_build_dispatcher_prompt__mutmut_51 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_52'] = x_build_dispatcher_prompt__mutmut_52 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_53'] = x_build_dispatcher_prompt__mutmut_53 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_54'] = x_build_dispatcher_prompt__mutmut_54 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_55'] = x_build_dispatcher_prompt__mutmut_55 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_56'] = x_build_dispatcher_prompt__mutmut_56 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_57'] = x_build_dispatcher_prompt__mutmut_57 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_58'] = x_build_dispatcher_prompt__mutmut_58 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_59'] = x_build_dispatcher_prompt__mutmut_59 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_60'] = x_build_dispatcher_prompt__mutmut_60 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_61'] = x_build_dispatcher_prompt__mutmut_61 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_62'] = x_build_dispatcher_prompt__mutmut_62 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_63'] = x_build_dispatcher_prompt__mutmut_63 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_64'] = x_build_dispatcher_prompt__mutmut_64 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_65'] = x_build_dispatcher_prompt__mutmut_65 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_66'] = x_build_dispatcher_prompt__mutmut_66 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_67'] = x_build_dispatcher_prompt__mutmut_67 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_68'] = x_build_dispatcher_prompt__mutmut_68 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_69'] = x_build_dispatcher_prompt__mutmut_69 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_70'] = x_build_dispatcher_prompt__mutmut_70 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_71'] = x_build_dispatcher_prompt__mutmut_71 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_72'] = x_build_dispatcher_prompt__mutmut_72 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_73'] = x_build_dispatcher_prompt__mutmut_73 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_74'] = x_build_dispatcher_prompt__mutmut_74 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_75'] = x_build_dispatcher_prompt__mutmut_75 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_76'] = x_build_dispatcher_prompt__mutmut_76 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_77'] = x_build_dispatcher_prompt__mutmut_77 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_78'] = x_build_dispatcher_prompt__mutmut_78 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_79'] = x_build_dispatcher_prompt__mutmut_79 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_80'] = x_build_dispatcher_prompt__mutmut_80 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_81'] = x_build_dispatcher_prompt__mutmut_81 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_82'] = x_build_dispatcher_prompt__mutmut_82 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_83'] = x_build_dispatcher_prompt__mutmut_83 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_84'] = x_build_dispatcher_prompt__mutmut_84 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_85'] = x_build_dispatcher_prompt__mutmut_85 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_86'] = x_build_dispatcher_prompt__mutmut_86 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_87'] = x_build_dispatcher_prompt__mutmut_87 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_88'] = x_build_dispatcher_prompt__mutmut_88 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_89'] = x_build_dispatcher_prompt__mutmut_89 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_90'] = x_build_dispatcher_prompt__mutmut_90 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_91'] = x_build_dispatcher_prompt__mutmut_91 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_92'] = x_build_dispatcher_prompt__mutmut_92 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_93'] = x_build_dispatcher_prompt__mutmut_93 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_94'] = x_build_dispatcher_prompt__mutmut_94 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_95'] = x_build_dispatcher_prompt__mutmut_95 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_96'] = x_build_dispatcher_prompt__mutmut_96 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_97'] = x_build_dispatcher_prompt__mutmut_97 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_98'] = x_build_dispatcher_prompt__mutmut_98 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_99'] = x_build_dispatcher_prompt__mutmut_99 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_100'] = x_build_dispatcher_prompt__mutmut_100 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_101'] = x_build_dispatcher_prompt__mutmut_101 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_102'] = x_build_dispatcher_prompt__mutmut_102 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_103'] = x_build_dispatcher_prompt__mutmut_103 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_104'] = x_build_dispatcher_prompt__mutmut_104 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_105'] = x_build_dispatcher_prompt__mutmut_105 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_106'] = x_build_dispatcher_prompt__mutmut_106 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_107'] = x_build_dispatcher_prompt__mutmut_107 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_108'] = x_build_dispatcher_prompt__mutmut_108 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_109'] = x_build_dispatcher_prompt__mutmut_109 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_110'] = x_build_dispatcher_prompt__mutmut_110 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_111'] = x_build_dispatcher_prompt__mutmut_111 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_112'] = x_build_dispatcher_prompt__mutmut_112 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_113'] = x_build_dispatcher_prompt__mutmut_113 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_114'] = x_build_dispatcher_prompt__mutmut_114 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_115'] = x_build_dispatcher_prompt__mutmut_115 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_116'] = x_build_dispatcher_prompt__mutmut_116 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_117'] = x_build_dispatcher_prompt__mutmut_117 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_118'] = x_build_dispatcher_prompt__mutmut_118 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_119'] = x_build_dispatcher_prompt__mutmut_119 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_120'] = x_build_dispatcher_prompt__mutmut_120 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_121'] = x_build_dispatcher_prompt__mutmut_121 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_122'] = x_build_dispatcher_prompt__mutmut_122 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_123'] = x_build_dispatcher_prompt__mutmut_123 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_124'] = x_build_dispatcher_prompt__mutmut_124 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_125'] = x_build_dispatcher_prompt__mutmut_125 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_126'] = x_build_dispatcher_prompt__mutmut_126 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_127'] = x_build_dispatcher_prompt__mutmut_127 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_128'] = x_build_dispatcher_prompt__mutmut_128 # type: ignore # mutmut generated
mutants_x_build_dispatcher_prompt__mutmut['x_build_dispatcher_prompt__mutmut_129'] = x_build_dispatcher_prompt__mutmut_129 # type: ignore # mutmut generated


# --------------------------------------------------------------------------- #
# Output parsing
# --------------------------------------------------------------------------- #


_FENCE_RE = re.compile(r"^```(?:json)?\s*|\s*```$", re.MULTILINE)
mutants_x__strip_fences__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__strip_fences__mutmut)
def _strip_fences(text: str) -> str:
    return _FENCE_RE.sub("", text.strip())


def x__strip_fences__mutmut_orig(text: str) -> str:
    return _FENCE_RE.sub("", text.strip())


def x__strip_fences__mutmut_1(text: str) -> str:
    return _FENCE_RE.sub(None, text.strip())


def x__strip_fences__mutmut_2(text: str) -> str:
    return _FENCE_RE.sub("", None)


def x__strip_fences__mutmut_3(text: str) -> str:
    return _FENCE_RE.sub(text.strip())


def x__strip_fences__mutmut_4(text: str) -> str:
    return _FENCE_RE.sub("", )


def x__strip_fences__mutmut_5(text: str) -> str:
    return _FENCE_RE.sub("XXXX", text.strip())

mutants_x__strip_fences__mutmut['_mutmut_orig'] = x__strip_fences__mutmut_orig # type: ignore # mutmut generated
mutants_x__strip_fences__mutmut['x__strip_fences__mutmut_1'] = x__strip_fences__mutmut_1 # type: ignore # mutmut generated
mutants_x__strip_fences__mutmut['x__strip_fences__mutmut_2'] = x__strip_fences__mutmut_2 # type: ignore # mutmut generated
mutants_x__strip_fences__mutmut['x__strip_fences__mutmut_3'] = x__strip_fences__mutmut_3 # type: ignore # mutmut generated
mutants_x__strip_fences__mutmut['x__strip_fences__mutmut_4'] = x__strip_fences__mutmut_4 # type: ignore # mutmut generated
mutants_x__strip_fences__mutmut['x__strip_fences__mutmut_5'] = x__strip_fences__mutmut_5 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__extract_json_object__mutmut)
def _extract_json_object(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_orig(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_1(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = None
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_2(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(None)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_3(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(None)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_4(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = None
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_5(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find(None)
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_6(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.rfind("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_7(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("XX{XX")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_8(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start != -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_9(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == +1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_10(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -2:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_11(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError(None)
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_12(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("XXNo JSON object found in dispatcher outputXX")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_13(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("no json object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_14(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("NO JSON OBJECT FOUND IN DISPATCHER OUTPUT")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_15(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = None
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_16(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 1
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_17(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(None, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_18(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, None):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_19(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_20(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, ):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_21(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = None
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_22(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch != "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_23(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "XX{XX":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_24(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth = 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_25(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth -= 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_26(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 2
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_27(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch != "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_28(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "XX}XX":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_29(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth = 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_30(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth += 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_31(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 2
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_32(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth != 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_33(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 1:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_34(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(None)
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_35(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i - 1])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_36(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 2])
    raise ValueError("Unbalanced JSON object in dispatcher output")


def x__extract_json_object__mutmut_37(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError(None)


def x__extract_json_object__mutmut_38(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("XXUnbalanced JSON object in dispatcher outputXX")


def x__extract_json_object__mutmut_39(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("unbalanced json object in dispatcher output")


def x__extract_json_object__mutmut_40(text: str) -> Any:
    """Pull the first balanced JSON object out of ``text``."""
    cleaned = _strip_fences(text)
    try:
        return json.loads(cleaned)
    except json.JSONDecodeError:
        pass
    start = cleaned.find("{")
    if start == -1:
        raise ValueError("No JSON object found in dispatcher output")
    depth = 0
    for i in range(start, len(cleaned)):
        ch = cleaned[i]
        if ch == "{":
            depth += 1
        elif ch == "}":
            depth -= 1
            if depth == 0:
                return json.loads(cleaned[start : i + 1])
    raise ValueError("UNBALANCED JSON OBJECT IN DISPATCHER OUTPUT")

mutants_x__extract_json_object__mutmut['_mutmut_orig'] = x__extract_json_object__mutmut_orig # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_1'] = x__extract_json_object__mutmut_1 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_2'] = x__extract_json_object__mutmut_2 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_3'] = x__extract_json_object__mutmut_3 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_4'] = x__extract_json_object__mutmut_4 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_5'] = x__extract_json_object__mutmut_5 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_6'] = x__extract_json_object__mutmut_6 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_7'] = x__extract_json_object__mutmut_7 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_8'] = x__extract_json_object__mutmut_8 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_9'] = x__extract_json_object__mutmut_9 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_10'] = x__extract_json_object__mutmut_10 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_11'] = x__extract_json_object__mutmut_11 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_12'] = x__extract_json_object__mutmut_12 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_13'] = x__extract_json_object__mutmut_13 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_14'] = x__extract_json_object__mutmut_14 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_15'] = x__extract_json_object__mutmut_15 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_16'] = x__extract_json_object__mutmut_16 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_17'] = x__extract_json_object__mutmut_17 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_18'] = x__extract_json_object__mutmut_18 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_19'] = x__extract_json_object__mutmut_19 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_20'] = x__extract_json_object__mutmut_20 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_21'] = x__extract_json_object__mutmut_21 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_22'] = x__extract_json_object__mutmut_22 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_23'] = x__extract_json_object__mutmut_23 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_24'] = x__extract_json_object__mutmut_24 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_25'] = x__extract_json_object__mutmut_25 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_26'] = x__extract_json_object__mutmut_26 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_27'] = x__extract_json_object__mutmut_27 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_28'] = x__extract_json_object__mutmut_28 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_29'] = x__extract_json_object__mutmut_29 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_30'] = x__extract_json_object__mutmut_30 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_31'] = x__extract_json_object__mutmut_31 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_32'] = x__extract_json_object__mutmut_32 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_33'] = x__extract_json_object__mutmut_33 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_34'] = x__extract_json_object__mutmut_34 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_35'] = x__extract_json_object__mutmut_35 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_36'] = x__extract_json_object__mutmut_36 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_37'] = x__extract_json_object__mutmut_37 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_38'] = x__extract_json_object__mutmut_38 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_39'] = x__extract_json_object__mutmut_39 # type: ignore # mutmut generated
mutants_x__extract_json_object__mutmut['x__extract_json_object__mutmut_40'] = x__extract_json_object__mutmut_40 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__normalize_model_name__mutmut)
def _normalize_model_name(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_orig(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_1(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = None
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_2(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name and "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_3(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "XXXX").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_4(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned and cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_5(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_6(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned not in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_7(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"XXdefaultXX", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_8(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"DEFAULT", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_9(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "XXdefault_workerXX"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_10(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "DEFAULT_WORKER"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_11(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = None
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_12(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned not in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_13(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = None
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_14(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.upper()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_15(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.upper() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_16(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() != lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_17(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = None
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_18(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith(None)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_19(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.upper().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_20(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" - lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_21(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("XX/XX" + lower)]
    if len(candidates) == 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_22(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) != 1:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_23(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 2:
        return candidates[0]
    return cleaned


def x__normalize_model_name__mutmut_24(name: str, config: ChimeraConfig) -> str:
    """Resolve a dispatcher-emitted model name to a known catalog model.

    Dispatcher models sometimes emit partial or differently-cased names
    (e.g. ``claude-sonnet-4`` instead of ``openrouter/anthropic/claude-sonnet-4``).
    Matching strategy, in order:

    * exact match in the catalog → returned as-is,
    * case-insensitive exact match,
    * unique suffix match (``"x/y/foo"`` ends with ``"/foo"``),
    * otherwise the original (unknown models are later replaced by the default).

    The ``"default"`` / ``"default_worker"`` aliases are passed through unchanged
    so :meth:`ChimeraConfig.resolve_model_alias` can handle them upstream.
    """
    cleaned = (name or "").strip()
    if not cleaned or cleaned in {"default", "default_worker"}:
        return cleaned
    models = config.models
    if cleaned in models:
        return cleaned
    lower = cleaned.lower()
    for m in models:
        if m.lower() == lower:
            return m
    # Unique suffix match: dispatcher's name is the tail of a catalog entry.
    candidates = [m for m in models if m.lower().endswith("/" + lower)]
    if len(candidates) == 1:
        return candidates[1]
    return cleaned

mutants_x__normalize_model_name__mutmut['_mutmut_orig'] = x__normalize_model_name__mutmut_orig # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_1'] = x__normalize_model_name__mutmut_1 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_2'] = x__normalize_model_name__mutmut_2 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_3'] = x__normalize_model_name__mutmut_3 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_4'] = x__normalize_model_name__mutmut_4 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_5'] = x__normalize_model_name__mutmut_5 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_6'] = x__normalize_model_name__mutmut_6 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_7'] = x__normalize_model_name__mutmut_7 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_8'] = x__normalize_model_name__mutmut_8 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_9'] = x__normalize_model_name__mutmut_9 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_10'] = x__normalize_model_name__mutmut_10 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_11'] = x__normalize_model_name__mutmut_11 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_12'] = x__normalize_model_name__mutmut_12 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_13'] = x__normalize_model_name__mutmut_13 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_14'] = x__normalize_model_name__mutmut_14 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_15'] = x__normalize_model_name__mutmut_15 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_16'] = x__normalize_model_name__mutmut_16 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_17'] = x__normalize_model_name__mutmut_17 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_18'] = x__normalize_model_name__mutmut_18 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_19'] = x__normalize_model_name__mutmut_19 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_20'] = x__normalize_model_name__mutmut_20 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_21'] = x__normalize_model_name__mutmut_21 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_22'] = x__normalize_model_name__mutmut_22 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_23'] = x__normalize_model_name__mutmut_23 # type: ignore # mutmut generated
mutants_x__normalize_model_name__mutmut['x__normalize_model_name__mutmut_24'] = x__normalize_model_name__mutmut_24 # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__resolve_and_normalize_model__mutmut)
def _resolve_and_normalize_model(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config.resolve_model_alias(name), config)


def x__resolve_and_normalize_model__mutmut_orig(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config.resolve_model_alias(name), config)


def x__resolve_and_normalize_model__mutmut_1(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(None, config)


def x__resolve_and_normalize_model__mutmut_2(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config.resolve_model_alias(name), None)


def x__resolve_and_normalize_model__mutmut_3(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config)


def x__resolve_and_normalize_model__mutmut_4(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config.resolve_model_alias(name), )


def x__resolve_and_normalize_model__mutmut_5(name: str, config: ChimeraConfig) -> str:
    """Resolve ``default`` aliases then fuzzy-match against the catalog."""
    return _normalize_model_name(config.resolve_model_alias(None), config)

mutants_x__resolve_and_normalize_model__mutmut['_mutmut_orig'] = x__resolve_and_normalize_model__mutmut_orig # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut['x__resolve_and_normalize_model__mutmut_1'] = x__resolve_and_normalize_model__mutmut_1 # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut['x__resolve_and_normalize_model__mutmut_2'] = x__resolve_and_normalize_model__mutmut_2 # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut['x__resolve_and_normalize_model__mutmut_3'] = x__resolve_and_normalize_model__mutmut_3 # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut['x__resolve_and_normalize_model__mutmut_4'] = x__resolve_and_normalize_model__mutmut_4 # type: ignore # mutmut generated
mutants_x__resolve_and_normalize_model__mutmut['x__resolve_and_normalize_model__mutmut_5'] = x__resolve_and_normalize_model__mutmut_5 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_parse_dispatch_result__mutmut)
def parse_dispatch_result(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_orig(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_1(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug(None, text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_2(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=None)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_3(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug(text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_4(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", )
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_5(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("XXdispatcher_raw_outputXX", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_6(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("DISPATCHER_RAW_OUTPUT", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_7(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = None
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_8(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = None
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_9(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(None)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_10(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning(None, error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_11(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=None)
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_12(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning(error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_13(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", )
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_14(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("XXdispatcher_bad_jsonXX", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_15(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("DISPATCHER_BAD_JSON", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_16(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(None))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_17(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(None, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_18(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, None, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_19(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt=None)

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_20(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_21(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_22(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, )

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_23(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="XXXX")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_24(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = None
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_25(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") and {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_26(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get(None) or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_27(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("XXformationXX") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_28(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("FORMATION") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_29(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = None
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_30(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") and []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_31(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get(None) or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_32(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("XXstagesXX") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_33(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("STAGES") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_34(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = None
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_35(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=None,
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_36(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=None,
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_37(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=None,
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_38(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=None,
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_39(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_40(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_41(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_42(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_43(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["XXidXX"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_44(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["ID"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_45(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get(None, "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_46(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", None),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_47(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_48(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", ),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_49(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("XXkindXX", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_50(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("KIND", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_51(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "XXworkerXX"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_52(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "WORKER"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_53(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(None, config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_54(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), None),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_55(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_56(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), ),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_57(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get(None, ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_58(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", None), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_59(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get(""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_60(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_61(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("XXmodelXX", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_62(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("MODEL", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_63(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", "XXXX"), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_64(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(None),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_65(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get(None, [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_66(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", None)),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_67(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get([])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_68(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", )),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_69(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("XXdepends_onXX", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_70(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("DEPENDS_ON", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_71(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = None
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_72(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(None) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_73(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") and [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_74(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get(None) or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_75(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("XXedgesXX") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_76(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("EDGES") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_77(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = None

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_78(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=None, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_79(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=None)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_80(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_81(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, )

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_82(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = None
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_83(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=None,
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_84(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=None,
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_85(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=None,
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_86(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=None,
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_87(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_88(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_89(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_90(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_91(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get(None, ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_92(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", None),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_93(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get(""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_94(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_95(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("XXstage_idXX", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_96(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("STAGE_ID", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_97(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", "XXXX"),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_98(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(None, config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_99(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), None),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_100(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_101(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), ),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_102(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get(None, ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_103(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", None), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_104(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get(""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_105(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_106(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("XXmodelXX", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_107(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("MODEL", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_108(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", "XXXX"), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_109(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get(None, ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_110(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", None),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_111(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get(""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_112(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_113(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("XXpromptXX", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_114(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("PROMPT", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_115(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", "XXXX"),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_116(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get(None),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_117(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("XXexpected_output_schemaXX"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_118(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("EXPECTED_OUTPUT_SCHEMA"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_119(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") and [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_120(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get(None) or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_121(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("XXworker_promptsXX") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_122(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("WORKER_PROMPTS") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_123(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = None
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_124(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=None,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_125(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=None,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_126(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=None,
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_127(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=None,
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_128(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=None,
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_129(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source=None,
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_130(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_131(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_132(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_133(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_134(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_135(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_136(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") and "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_137(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get(None, "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_138(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", None) or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_139(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_140(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", ) or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_141(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("XXaggregator_instructionsXX", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_142(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("AGGREGATOR_INSTRUCTIONS", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_143(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "XXXX") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_144(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "XXXX",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_145(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) and {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_146(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get(None, {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_147(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", None) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_148(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get({}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_149(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", ) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_150(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("XXstage_instructionsXX", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_151(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("STAGE_INSTRUCTIONS", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_152(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get(None),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_153(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("XXoutput_schemaXX"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_154(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("OUTPUT_SCHEMA"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_155(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="XXautoXX",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_156(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="AUTO",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_157(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(None, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_158(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, None)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_159(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_160(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, )
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_161(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(None)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_162(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning(None, error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_163(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=None)
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_164(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning(error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_165(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", )
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_166(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("XXdispatcher_parse_failedXX", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_167(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("DISPATCHER_PARSE_FAILED", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_168(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(None))
        return _fallback_result(config, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_169(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(None, fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_170(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, None, user_prompt="")


def x_parse_dispatch_result__mutmut_171(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt=None)


def x_parse_dispatch_result__mutmut_172(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(fallback_dag, user_prompt="")


def x_parse_dispatch_result__mutmut_173(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, user_prompt="")


def x_parse_dispatch_result__mutmut_174(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, )


def x_parse_dispatch_result__mutmut_175(
    raw: str | dict[str, Any],
    config: ChimeraConfig,
    *,
    fallback_dag: FormationDAG | None = None,
) -> DispatchResult:
    """Parse dispatcher output into a validated :class:`DispatchResult`.

    If the output is malformed or references unknown models, fall back to
    ``fallback_dag`` (or a minimal 1-worker formation) with templated prompts.
    """
    if isinstance(raw, str):
        # Visible at debug level so malformed/strict-JSON failures can be diagnosed.
        log.debug("dispatcher_raw_output", text=raw)
    data: dict[str, Any]
    if isinstance(raw, dict):
        data = raw
    else:
        try:
            data = _extract_json_object(raw)
        except (json.JSONDecodeError, ValueError) as exc:
            log.warning("dispatcher_bad_json", error=str(exc))
            return _fallback_result(config, fallback_dag, user_prompt="")

    try:
        formation_data = data.get("formation") or {}
        stages_raw = formation_data.get("stages") or []
        stages = [
            Stage(
                id=s["id"],
                kind=s.get("kind", "worker"),
                model=_resolve_and_normalize_model(s.get("model", ""), config),
                depends_on=list(s.get("depends_on", [])),
            )
            for s in stages_raw
        ]
        edges = [tuple(e) for e in (formation_data.get("edges") or [])]
        formation = FormationDAG(stages=stages, edges=edges)

        worker_prompts = [
            WorkerPrompt(
                stage_id=wp.get("stage_id", ""),
                model=_resolve_and_normalize_model(wp.get("model", ""), config),
                prompt=wp.get("prompt", ""),
                expected_output_schema=wp.get("expected_output_schema"),
            )
            for wp in (data.get("worker_prompts") or [])
        ]
        result = DispatchResult(
            formation=formation,
            worker_prompts=worker_prompts,
            aggregator_instructions=data.get("aggregator_instructions", "") or "",
            stage_instructions=data.get("stage_instructions", {}) or {},
            output_schema=data.get("output_schema"),
            source="auto",
        )
        _normalize_result(result, config)
        _validate_result(result)
        return result
    except (KeyError, TypeError, ValueError, IndexError) as exc:
        log.warning("dispatcher_parse_failed", error=str(exc))
        return _fallback_result(config, fallback_dag, user_prompt="XXXX")

mutants_x_parse_dispatch_result__mutmut['_mutmut_orig'] = x_parse_dispatch_result__mutmut_orig # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_1'] = x_parse_dispatch_result__mutmut_1 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_2'] = x_parse_dispatch_result__mutmut_2 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_3'] = x_parse_dispatch_result__mutmut_3 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_4'] = x_parse_dispatch_result__mutmut_4 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_5'] = x_parse_dispatch_result__mutmut_5 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_6'] = x_parse_dispatch_result__mutmut_6 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_7'] = x_parse_dispatch_result__mutmut_7 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_8'] = x_parse_dispatch_result__mutmut_8 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_9'] = x_parse_dispatch_result__mutmut_9 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_10'] = x_parse_dispatch_result__mutmut_10 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_11'] = x_parse_dispatch_result__mutmut_11 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_12'] = x_parse_dispatch_result__mutmut_12 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_13'] = x_parse_dispatch_result__mutmut_13 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_14'] = x_parse_dispatch_result__mutmut_14 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_15'] = x_parse_dispatch_result__mutmut_15 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_16'] = x_parse_dispatch_result__mutmut_16 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_17'] = x_parse_dispatch_result__mutmut_17 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_18'] = x_parse_dispatch_result__mutmut_18 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_19'] = x_parse_dispatch_result__mutmut_19 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_20'] = x_parse_dispatch_result__mutmut_20 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_21'] = x_parse_dispatch_result__mutmut_21 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_22'] = x_parse_dispatch_result__mutmut_22 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_23'] = x_parse_dispatch_result__mutmut_23 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_24'] = x_parse_dispatch_result__mutmut_24 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_25'] = x_parse_dispatch_result__mutmut_25 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_26'] = x_parse_dispatch_result__mutmut_26 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_27'] = x_parse_dispatch_result__mutmut_27 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_28'] = x_parse_dispatch_result__mutmut_28 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_29'] = x_parse_dispatch_result__mutmut_29 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_30'] = x_parse_dispatch_result__mutmut_30 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_31'] = x_parse_dispatch_result__mutmut_31 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_32'] = x_parse_dispatch_result__mutmut_32 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_33'] = x_parse_dispatch_result__mutmut_33 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_34'] = x_parse_dispatch_result__mutmut_34 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_35'] = x_parse_dispatch_result__mutmut_35 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_36'] = x_parse_dispatch_result__mutmut_36 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_37'] = x_parse_dispatch_result__mutmut_37 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_38'] = x_parse_dispatch_result__mutmut_38 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_39'] = x_parse_dispatch_result__mutmut_39 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_40'] = x_parse_dispatch_result__mutmut_40 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_41'] = x_parse_dispatch_result__mutmut_41 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_42'] = x_parse_dispatch_result__mutmut_42 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_43'] = x_parse_dispatch_result__mutmut_43 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_44'] = x_parse_dispatch_result__mutmut_44 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_45'] = x_parse_dispatch_result__mutmut_45 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_46'] = x_parse_dispatch_result__mutmut_46 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_47'] = x_parse_dispatch_result__mutmut_47 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_48'] = x_parse_dispatch_result__mutmut_48 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_49'] = x_parse_dispatch_result__mutmut_49 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_50'] = x_parse_dispatch_result__mutmut_50 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_51'] = x_parse_dispatch_result__mutmut_51 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_52'] = x_parse_dispatch_result__mutmut_52 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_53'] = x_parse_dispatch_result__mutmut_53 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_54'] = x_parse_dispatch_result__mutmut_54 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_55'] = x_parse_dispatch_result__mutmut_55 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_56'] = x_parse_dispatch_result__mutmut_56 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_57'] = x_parse_dispatch_result__mutmut_57 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_58'] = x_parse_dispatch_result__mutmut_58 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_59'] = x_parse_dispatch_result__mutmut_59 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_60'] = x_parse_dispatch_result__mutmut_60 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_61'] = x_parse_dispatch_result__mutmut_61 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_62'] = x_parse_dispatch_result__mutmut_62 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_63'] = x_parse_dispatch_result__mutmut_63 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_64'] = x_parse_dispatch_result__mutmut_64 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_65'] = x_parse_dispatch_result__mutmut_65 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_66'] = x_parse_dispatch_result__mutmut_66 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_67'] = x_parse_dispatch_result__mutmut_67 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_68'] = x_parse_dispatch_result__mutmut_68 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_69'] = x_parse_dispatch_result__mutmut_69 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_70'] = x_parse_dispatch_result__mutmut_70 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_71'] = x_parse_dispatch_result__mutmut_71 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_72'] = x_parse_dispatch_result__mutmut_72 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_73'] = x_parse_dispatch_result__mutmut_73 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_74'] = x_parse_dispatch_result__mutmut_74 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_75'] = x_parse_dispatch_result__mutmut_75 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_76'] = x_parse_dispatch_result__mutmut_76 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_77'] = x_parse_dispatch_result__mutmut_77 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_78'] = x_parse_dispatch_result__mutmut_78 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_79'] = x_parse_dispatch_result__mutmut_79 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_80'] = x_parse_dispatch_result__mutmut_80 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_81'] = x_parse_dispatch_result__mutmut_81 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_82'] = x_parse_dispatch_result__mutmut_82 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_83'] = x_parse_dispatch_result__mutmut_83 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_84'] = x_parse_dispatch_result__mutmut_84 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_85'] = x_parse_dispatch_result__mutmut_85 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_86'] = x_parse_dispatch_result__mutmut_86 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_87'] = x_parse_dispatch_result__mutmut_87 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_88'] = x_parse_dispatch_result__mutmut_88 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_89'] = x_parse_dispatch_result__mutmut_89 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_90'] = x_parse_dispatch_result__mutmut_90 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_91'] = x_parse_dispatch_result__mutmut_91 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_92'] = x_parse_dispatch_result__mutmut_92 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_93'] = x_parse_dispatch_result__mutmut_93 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_94'] = x_parse_dispatch_result__mutmut_94 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_95'] = x_parse_dispatch_result__mutmut_95 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_96'] = x_parse_dispatch_result__mutmut_96 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_97'] = x_parse_dispatch_result__mutmut_97 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_98'] = x_parse_dispatch_result__mutmut_98 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_99'] = x_parse_dispatch_result__mutmut_99 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_100'] = x_parse_dispatch_result__mutmut_100 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_101'] = x_parse_dispatch_result__mutmut_101 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_102'] = x_parse_dispatch_result__mutmut_102 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_103'] = x_parse_dispatch_result__mutmut_103 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_104'] = x_parse_dispatch_result__mutmut_104 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_105'] = x_parse_dispatch_result__mutmut_105 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_106'] = x_parse_dispatch_result__mutmut_106 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_107'] = x_parse_dispatch_result__mutmut_107 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_108'] = x_parse_dispatch_result__mutmut_108 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_109'] = x_parse_dispatch_result__mutmut_109 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_110'] = x_parse_dispatch_result__mutmut_110 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_111'] = x_parse_dispatch_result__mutmut_111 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_112'] = x_parse_dispatch_result__mutmut_112 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_113'] = x_parse_dispatch_result__mutmut_113 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_114'] = x_parse_dispatch_result__mutmut_114 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_115'] = x_parse_dispatch_result__mutmut_115 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_116'] = x_parse_dispatch_result__mutmut_116 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_117'] = x_parse_dispatch_result__mutmut_117 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_118'] = x_parse_dispatch_result__mutmut_118 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_119'] = x_parse_dispatch_result__mutmut_119 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_120'] = x_parse_dispatch_result__mutmut_120 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_121'] = x_parse_dispatch_result__mutmut_121 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_122'] = x_parse_dispatch_result__mutmut_122 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_123'] = x_parse_dispatch_result__mutmut_123 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_124'] = x_parse_dispatch_result__mutmut_124 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_125'] = x_parse_dispatch_result__mutmut_125 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_126'] = x_parse_dispatch_result__mutmut_126 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_127'] = x_parse_dispatch_result__mutmut_127 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_128'] = x_parse_dispatch_result__mutmut_128 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_129'] = x_parse_dispatch_result__mutmut_129 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_130'] = x_parse_dispatch_result__mutmut_130 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_131'] = x_parse_dispatch_result__mutmut_131 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_132'] = x_parse_dispatch_result__mutmut_132 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_133'] = x_parse_dispatch_result__mutmut_133 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_134'] = x_parse_dispatch_result__mutmut_134 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_135'] = x_parse_dispatch_result__mutmut_135 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_136'] = x_parse_dispatch_result__mutmut_136 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_137'] = x_parse_dispatch_result__mutmut_137 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_138'] = x_parse_dispatch_result__mutmut_138 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_139'] = x_parse_dispatch_result__mutmut_139 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_140'] = x_parse_dispatch_result__mutmut_140 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_141'] = x_parse_dispatch_result__mutmut_141 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_142'] = x_parse_dispatch_result__mutmut_142 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_143'] = x_parse_dispatch_result__mutmut_143 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_144'] = x_parse_dispatch_result__mutmut_144 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_145'] = x_parse_dispatch_result__mutmut_145 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_146'] = x_parse_dispatch_result__mutmut_146 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_147'] = x_parse_dispatch_result__mutmut_147 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_148'] = x_parse_dispatch_result__mutmut_148 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_149'] = x_parse_dispatch_result__mutmut_149 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_150'] = x_parse_dispatch_result__mutmut_150 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_151'] = x_parse_dispatch_result__mutmut_151 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_152'] = x_parse_dispatch_result__mutmut_152 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_153'] = x_parse_dispatch_result__mutmut_153 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_154'] = x_parse_dispatch_result__mutmut_154 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_155'] = x_parse_dispatch_result__mutmut_155 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_156'] = x_parse_dispatch_result__mutmut_156 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_157'] = x_parse_dispatch_result__mutmut_157 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_158'] = x_parse_dispatch_result__mutmut_158 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_159'] = x_parse_dispatch_result__mutmut_159 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_160'] = x_parse_dispatch_result__mutmut_160 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_161'] = x_parse_dispatch_result__mutmut_161 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_162'] = x_parse_dispatch_result__mutmut_162 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_163'] = x_parse_dispatch_result__mutmut_163 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_164'] = x_parse_dispatch_result__mutmut_164 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_165'] = x_parse_dispatch_result__mutmut_165 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_166'] = x_parse_dispatch_result__mutmut_166 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_167'] = x_parse_dispatch_result__mutmut_167 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_168'] = x_parse_dispatch_result__mutmut_168 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_169'] = x_parse_dispatch_result__mutmut_169 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_170'] = x_parse_dispatch_result__mutmut_170 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_171'] = x_parse_dispatch_result__mutmut_171 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_172'] = x_parse_dispatch_result__mutmut_172 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_173'] = x_parse_dispatch_result__mutmut_173 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_174'] = x_parse_dispatch_result__mutmut_174 # type: ignore # mutmut generated
mutants_x_parse_dispatch_result__mutmut['x_parse_dispatch_result__mutmut_175'] = x_parse_dispatch_result__mutmut_175 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_result__mutmut)
def _validate_result(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_orig(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_1(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_2(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError(None)
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_3(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("XXdispatch produced no stagesXX")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_4(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("DISPATCH PRODUCED NO STAGES")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_5(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_6(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(None):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_7(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind != "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_8(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "XXworkerXX" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_9(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "WORKER" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_10(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError(None)
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_11(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("XXdispatch produced no worker stagesXX")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_12(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("DISPATCH PRODUCED NO WORKER STAGES")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_13(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_14(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(None):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_15(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind not in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_16(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"XXaggregatorXX", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_17(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"AGGREGATOR", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_18(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "XXmergeXX", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_19(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "MERGE", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_20(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "XXauditXX"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_21(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "AUDIT"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_22(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError(None)
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_23(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("XXdispatch produced no aggregator/merge/audit stageXX")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_24(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("DISPATCH PRODUCED NO AGGREGATOR/MERGE/AUDIT STAGE")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_25(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = None
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_26(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(None)
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_27(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep in ids:
                raise ValueError(f"stage {stage.id!r} depends on unknown {dep!r}")


def x__validate_result__mutmut_28(result: DispatchResult) -> None:
    """Raise ``ValueError`` if the dispatch plan is structurally unusable."""
    if not result.formation.stages:
        raise ValueError("dispatch produced no stages")
    if not any(s.kind == "worker" for s in result.formation.stages):
        raise ValueError("dispatch produced no worker stages")
    if not any(s.kind in {"aggregator", "merge", "audit"} for s in result.formation.stages):
        raise ValueError("dispatch produced no aggregator/merge/audit stage")
    ids = set(result.formation.stage_ids())
    for stage in result.formation.stages:
        for dep in stage.depends_on:
            if dep not in ids:
                raise ValueError(None)

mutants_x__validate_result__mutmut['_mutmut_orig'] = x__validate_result__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_1'] = x__validate_result__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_2'] = x__validate_result__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_3'] = x__validate_result__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_4'] = x__validate_result__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_5'] = x__validate_result__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_6'] = x__validate_result__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_7'] = x__validate_result__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_8'] = x__validate_result__mutmut_8 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_9'] = x__validate_result__mutmut_9 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_10'] = x__validate_result__mutmut_10 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_11'] = x__validate_result__mutmut_11 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_12'] = x__validate_result__mutmut_12 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_13'] = x__validate_result__mutmut_13 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_14'] = x__validate_result__mutmut_14 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_15'] = x__validate_result__mutmut_15 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_16'] = x__validate_result__mutmut_16 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_17'] = x__validate_result__mutmut_17 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_18'] = x__validate_result__mutmut_18 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_19'] = x__validate_result__mutmut_19 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_20'] = x__validate_result__mutmut_20 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_21'] = x__validate_result__mutmut_21 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_22'] = x__validate_result__mutmut_22 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_23'] = x__validate_result__mutmut_23 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_24'] = x__validate_result__mutmut_24 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_25'] = x__validate_result__mutmut_25 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_26'] = x__validate_result__mutmut_26 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_27'] = x__validate_result__mutmut_27 # type: ignore # mutmut generated
mutants_x__validate_result__mutmut['x__validate_result__mutmut_28'] = x__validate_result__mutmut_28 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__normalize_result__mutmut)
def _normalize_result(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_orig(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_1(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = None
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_2(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(None)
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_3(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = None
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_4(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = None

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_5(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = None
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_6(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = None
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_7(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.upper(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_8(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_9(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = None
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_10(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(None)
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_11(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.upper())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_12(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_13(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = None

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_14(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp or stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_15(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind != "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_16(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "XXdispatcherXX":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_17(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "DISPATCHER":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_18(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = None
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_19(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg or stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_20(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind not in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_21(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"XXaggregatorXX", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_22(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"AGGREGATOR", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_23(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "XXmergeXX", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_24(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "MERGE", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_25(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "XXauditXX"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_26(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "AUDIT"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_27(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = None

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_28(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_29(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = None
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_30(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind != "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_31(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "XXworkerXX"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_32(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "WORKER"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_33(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind != "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_34(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "XXworkerXX":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_35(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "WORKER":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_36(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = None
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_37(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(None)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_38(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is not None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_39(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    None
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_40(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=None, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_41(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=None, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_42(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt=None)
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_43(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_44(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_45(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, )
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_46(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="XXXX")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_47(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_48(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = None
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_49(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_50(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                None,
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_51(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=None,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_52(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason=None,
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_53(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_54(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_55(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_56(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "XXworker_prompt_templatedXX",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_57(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "WORKER_PROMPT_TEMPLATED",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_58(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="XXempty_or_missingXX",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_59(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="EMPTY_OR_MISSING",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, config)


def x__normalize_result__mutmut_60(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = None


def x__normalize_result__mutmut_61(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(None, config)


def x__normalize_result__mutmut_62(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, None)


def x__normalize_result__mutmut_63(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(config)


def x__normalize_result__mutmut_64(result: DispatchResult, config: ChimeraConfig) -> None:
    """Resolve model aliases, fill missing worker prompts, validate the DAG.

    * Reconciles ``worker_prompts[].stage_id`` against the formation using a
      case-insensitive fallback so dispatcher case mismatches still line up.
    * Honors model locking: when ``lock_aggregator``/``lock_dispatcher`` are set,
      the dispatcher's model choice for those roles is overridden by the
      configured defaults.
    * Emits a ``worker_prompt_templated`` WARNING whenever a worker prompt had
      to be filled from the generic template (the dispatcher left it empty).
    """
    valid_models = set(config.models.keys())
    lock_agg = config.defaults.lock_aggregator
    lock_disp = config.defaults.lock_dispatcher

    # Fuzzy (case-insensitive) stage_id reconciliation for worker prompts.
    formation_ids = {s.id for s in result.formation.stages}
    lower_to_id = {s.id.lower(): s.id for s in result.formation.stages}
    for wp in result.worker_prompts:
        if wp.stage_id not in formation_ids:
            matched = lower_to_id.get(wp.stage_id.lower())
            if matched is not None:
                wp.stage_id = matched

    for stage in result.formation.stages:
        # Model locking: force configured defaults for locked roles.
        if lock_disp and stage.kind == "dispatcher":
            stage.model = config.defaults.dispatcher
        if lock_agg and stage.kind in {"aggregator", "merge", "audit"}:
            stage.model = config.defaults.default_aggregator

        if stage.model not in valid_models:
            stage.model = (
                config.defaults.default_worker
                if stage.kind == "worker"
                else config.defaults.default_aggregator
            )
        if stage.kind == "worker":
            existing = result.worker_prompt_for(stage.id)
            if existing is None:
                result.worker_prompts.append(
                    WorkerPrompt(stage_id=stage.id, model=stage.model, prompt="")
                )
            elif not existing.prompt:
                existing.model = stage.model
    for wp in result.worker_prompts:
        if not wp.prompt:
            log.warning(
                "worker_prompt_templated",
                stage_id=wp.stage_id,
                reason="empty_or_missing",
            )
            wp.prompt = _templated_worker_prompt(wp.stage_id, )

mutants_x__normalize_result__mutmut['_mutmut_orig'] = x__normalize_result__mutmut_orig # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_1'] = x__normalize_result__mutmut_1 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_2'] = x__normalize_result__mutmut_2 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_3'] = x__normalize_result__mutmut_3 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_4'] = x__normalize_result__mutmut_4 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_5'] = x__normalize_result__mutmut_5 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_6'] = x__normalize_result__mutmut_6 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_7'] = x__normalize_result__mutmut_7 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_8'] = x__normalize_result__mutmut_8 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_9'] = x__normalize_result__mutmut_9 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_10'] = x__normalize_result__mutmut_10 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_11'] = x__normalize_result__mutmut_11 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_12'] = x__normalize_result__mutmut_12 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_13'] = x__normalize_result__mutmut_13 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_14'] = x__normalize_result__mutmut_14 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_15'] = x__normalize_result__mutmut_15 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_16'] = x__normalize_result__mutmut_16 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_17'] = x__normalize_result__mutmut_17 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_18'] = x__normalize_result__mutmut_18 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_19'] = x__normalize_result__mutmut_19 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_20'] = x__normalize_result__mutmut_20 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_21'] = x__normalize_result__mutmut_21 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_22'] = x__normalize_result__mutmut_22 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_23'] = x__normalize_result__mutmut_23 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_24'] = x__normalize_result__mutmut_24 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_25'] = x__normalize_result__mutmut_25 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_26'] = x__normalize_result__mutmut_26 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_27'] = x__normalize_result__mutmut_27 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_28'] = x__normalize_result__mutmut_28 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_29'] = x__normalize_result__mutmut_29 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_30'] = x__normalize_result__mutmut_30 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_31'] = x__normalize_result__mutmut_31 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_32'] = x__normalize_result__mutmut_32 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_33'] = x__normalize_result__mutmut_33 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_34'] = x__normalize_result__mutmut_34 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_35'] = x__normalize_result__mutmut_35 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_36'] = x__normalize_result__mutmut_36 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_37'] = x__normalize_result__mutmut_37 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_38'] = x__normalize_result__mutmut_38 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_39'] = x__normalize_result__mutmut_39 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_40'] = x__normalize_result__mutmut_40 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_41'] = x__normalize_result__mutmut_41 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_42'] = x__normalize_result__mutmut_42 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_43'] = x__normalize_result__mutmut_43 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_44'] = x__normalize_result__mutmut_44 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_45'] = x__normalize_result__mutmut_45 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_46'] = x__normalize_result__mutmut_46 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_47'] = x__normalize_result__mutmut_47 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_48'] = x__normalize_result__mutmut_48 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_49'] = x__normalize_result__mutmut_49 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_50'] = x__normalize_result__mutmut_50 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_51'] = x__normalize_result__mutmut_51 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_52'] = x__normalize_result__mutmut_52 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_53'] = x__normalize_result__mutmut_53 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_54'] = x__normalize_result__mutmut_54 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_55'] = x__normalize_result__mutmut_55 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_56'] = x__normalize_result__mutmut_56 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_57'] = x__normalize_result__mutmut_57 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_58'] = x__normalize_result__mutmut_58 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_59'] = x__normalize_result__mutmut_59 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_60'] = x__normalize_result__mutmut_60 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_61'] = x__normalize_result__mutmut_61 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_62'] = x__normalize_result__mutmut_62 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_63'] = x__normalize_result__mutmut_63 # type: ignore # mutmut generated
mutants_x__normalize_result__mutmut['x__normalize_result__mutmut_64'] = x__normalize_result__mutmut_64 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fallback_result__mutmut)
def _fallback_result(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_orig(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_1(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_2(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = None
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_3(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = None
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_4(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=None,
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_5(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=None,
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_6(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_7(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_8(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id=None, kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_9(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind=None, model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_10(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=None),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_11(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_12(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_13(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", ),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_14(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="XXworker_1XX", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_15(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="WORKER_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_16(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="XXworkerXX", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_17(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="WORKER", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_18(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id=None,
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_19(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind=None,
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_20(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=None,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_21(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=None,
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_22(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_23(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_24(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_25(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_26(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="XXaggregatorXX",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_27(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="AGGREGATOR",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_28(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="XXaggregatorXX",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_29(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="AGGREGATOR",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_30(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["XXworker_1XX"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_31(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["WORKER_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_32(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("XXworker_1XX", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_33(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("WORKER_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_34(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "XXaggregatorXX")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_35(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "AGGREGATOR")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_36(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = None
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_37(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=None, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_38(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=None, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_39(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=None)
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_40(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_41(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_42(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, )
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_43(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(None, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_44(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, None))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_45(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_46(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, ))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_47(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind != "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_48(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "XXworkerXX"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_49(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "WORKER"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_50(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=None,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_51(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=None,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_52(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=None,
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_53(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions=None,
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_54(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema=None,
        source="fallback",
    )


def x__fallback_result__mutmut_55(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source=None,
    )


def x__fallback_result__mutmut_56(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_57(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_58(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_59(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_60(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        source="fallback",
    )


def x__fallback_result__mutmut_61(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        )


def x__fallback_result__mutmut_62(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "XXMerge the worker outputs into a single, coherent final answer XX"
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_63(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_64(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "MERGE THE WORKER OUTPUTS INTO A SINGLE, COHERENT FINAL ANSWER "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_65(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "XXfor the user's request.XX"
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_66(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "FOR THE USER'S REQUEST."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_67(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "XXtypeXX": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_68(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "TYPE": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_69(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "XXobjectXX",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_70(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "OBJECT",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_71(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "XXpropertiesXX": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_72(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "PROPERTIES": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_73(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "XXanswerXX": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_74(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "ANSWER": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_75(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"XXtypeXX": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_76(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"TYPE": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_77(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "XXstringXX", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_78(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "STRING", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_79(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "XXdescriptionXX": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_80(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "DESCRIPTION": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_81(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "XXThe final merged answer for the userXX"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_82(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "the final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_83(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "THE FINAL MERGED ANSWER FOR THE USER"},
            },
            "required": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_84(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "XXrequiredXX": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_85(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "REQUIRED": ["answer"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_86(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["XXanswerXX"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_87(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["ANSWER"],
        },
        source="fallback",
    )


def x__fallback_result__mutmut_88(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="XXfallbackXX",
    )


def x__fallback_result__mutmut_89(
    config: ChimeraConfig,
    fallback_dag: FormationDAG | None,
    *,
    user_prompt: str,
) -> DispatchResult:
    """Build a safe 1-worker + 1-aggregator result when parsing fails."""
    if fallback_dag is not None:
        dag = fallback_dag
    else:
        dag = FormationDAG(
            stages=[
                Stage(id="worker_1", kind="worker", model=config.defaults.default_worker),
                Stage(
                    id="aggregator",
                    kind="aggregator",
                    model=config.defaults.default_aggregator,
                    depends_on=["worker_1"],
                ),
            ],
            edges=[("worker_1", "aggregator")],
        )
    worker_prompts = [
        WorkerPrompt(stage_id=s.id, model=s.model, prompt=_templated_worker_prompt(s.id, config))
        for s in dag.stages
        if s.kind == "worker"
    ]
    return DispatchResult(
        formation=dag,
        worker_prompts=worker_prompts,
        aggregator_instructions=(
            "Merge the worker outputs into a single, coherent final answer "
            "for the user's request."
        ),
        stage_instructions={},
        output_schema={
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final merged answer for the user"},
            },
            "required": ["answer"],
        },
        source="FALLBACK",
    )

mutants_x__fallback_result__mutmut['_mutmut_orig'] = x__fallback_result__mutmut_orig # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_1'] = x__fallback_result__mutmut_1 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_2'] = x__fallback_result__mutmut_2 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_3'] = x__fallback_result__mutmut_3 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_4'] = x__fallback_result__mutmut_4 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_5'] = x__fallback_result__mutmut_5 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_6'] = x__fallback_result__mutmut_6 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_7'] = x__fallback_result__mutmut_7 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_8'] = x__fallback_result__mutmut_8 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_9'] = x__fallback_result__mutmut_9 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_10'] = x__fallback_result__mutmut_10 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_11'] = x__fallback_result__mutmut_11 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_12'] = x__fallback_result__mutmut_12 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_13'] = x__fallback_result__mutmut_13 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_14'] = x__fallback_result__mutmut_14 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_15'] = x__fallback_result__mutmut_15 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_16'] = x__fallback_result__mutmut_16 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_17'] = x__fallback_result__mutmut_17 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_18'] = x__fallback_result__mutmut_18 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_19'] = x__fallback_result__mutmut_19 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_20'] = x__fallback_result__mutmut_20 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_21'] = x__fallback_result__mutmut_21 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_22'] = x__fallback_result__mutmut_22 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_23'] = x__fallback_result__mutmut_23 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_24'] = x__fallback_result__mutmut_24 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_25'] = x__fallback_result__mutmut_25 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_26'] = x__fallback_result__mutmut_26 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_27'] = x__fallback_result__mutmut_27 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_28'] = x__fallback_result__mutmut_28 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_29'] = x__fallback_result__mutmut_29 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_30'] = x__fallback_result__mutmut_30 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_31'] = x__fallback_result__mutmut_31 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_32'] = x__fallback_result__mutmut_32 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_33'] = x__fallback_result__mutmut_33 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_34'] = x__fallback_result__mutmut_34 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_35'] = x__fallback_result__mutmut_35 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_36'] = x__fallback_result__mutmut_36 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_37'] = x__fallback_result__mutmut_37 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_38'] = x__fallback_result__mutmut_38 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_39'] = x__fallback_result__mutmut_39 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_40'] = x__fallback_result__mutmut_40 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_41'] = x__fallback_result__mutmut_41 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_42'] = x__fallback_result__mutmut_42 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_43'] = x__fallback_result__mutmut_43 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_44'] = x__fallback_result__mutmut_44 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_45'] = x__fallback_result__mutmut_45 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_46'] = x__fallback_result__mutmut_46 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_47'] = x__fallback_result__mutmut_47 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_48'] = x__fallback_result__mutmut_48 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_49'] = x__fallback_result__mutmut_49 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_50'] = x__fallback_result__mutmut_50 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_51'] = x__fallback_result__mutmut_51 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_52'] = x__fallback_result__mutmut_52 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_53'] = x__fallback_result__mutmut_53 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_54'] = x__fallback_result__mutmut_54 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_55'] = x__fallback_result__mutmut_55 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_56'] = x__fallback_result__mutmut_56 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_57'] = x__fallback_result__mutmut_57 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_58'] = x__fallback_result__mutmut_58 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_59'] = x__fallback_result__mutmut_59 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_60'] = x__fallback_result__mutmut_60 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_61'] = x__fallback_result__mutmut_61 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_62'] = x__fallback_result__mutmut_62 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_63'] = x__fallback_result__mutmut_63 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_64'] = x__fallback_result__mutmut_64 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_65'] = x__fallback_result__mutmut_65 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_66'] = x__fallback_result__mutmut_66 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_67'] = x__fallback_result__mutmut_67 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_68'] = x__fallback_result__mutmut_68 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_69'] = x__fallback_result__mutmut_69 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_70'] = x__fallback_result__mutmut_70 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_71'] = x__fallback_result__mutmut_71 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_72'] = x__fallback_result__mutmut_72 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_73'] = x__fallback_result__mutmut_73 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_74'] = x__fallback_result__mutmut_74 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_75'] = x__fallback_result__mutmut_75 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_76'] = x__fallback_result__mutmut_76 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_77'] = x__fallback_result__mutmut_77 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_78'] = x__fallback_result__mutmut_78 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_79'] = x__fallback_result__mutmut_79 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_80'] = x__fallback_result__mutmut_80 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_81'] = x__fallback_result__mutmut_81 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_82'] = x__fallback_result__mutmut_82 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_83'] = x__fallback_result__mutmut_83 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_84'] = x__fallback_result__mutmut_84 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_85'] = x__fallback_result__mutmut_85 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_86'] = x__fallback_result__mutmut_86 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_87'] = x__fallback_result__mutmut_87 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_88'] = x__fallback_result__mutmut_88 # type: ignore # mutmut generated
mutants_x__fallback_result__mutmut['x__fallback_result__mutmut_89'] = x__fallback_result__mutmut_89 # type: ignore # mutmut generated
mutants_x__templated_worker_prompt__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__templated_worker_prompt__mutmut)
def _templated_worker_prompt(stage_id: str, config: ChimeraConfig) -> str:
    return (
        f"You are worker '{stage_id}'. Solve the user's request as completely as you can. "
        "Be precise and concise."
    )


def x__templated_worker_prompt__mutmut_orig(stage_id: str, config: ChimeraConfig) -> str:
    return (
        f"You are worker '{stage_id}'. Solve the user's request as completely as you can. "
        "Be precise and concise."
    )


def x__templated_worker_prompt__mutmut_1(stage_id: str, config: ChimeraConfig) -> str:
    return (
        f"You are worker '{stage_id}'. Solve the user's request as completely as you can. "
        "XXBe precise and concise.XX"
    )


def x__templated_worker_prompt__mutmut_2(stage_id: str, config: ChimeraConfig) -> str:
    return (
        f"You are worker '{stage_id}'. Solve the user's request as completely as you can. "
        "be precise and concise."
    )


def x__templated_worker_prompt__mutmut_3(stage_id: str, config: ChimeraConfig) -> str:
    return (
        f"You are worker '{stage_id}'. Solve the user's request as completely as you can. "
        "BE PRECISE AND CONCISE."
    )

mutants_x__templated_worker_prompt__mutmut['_mutmut_orig'] = x__templated_worker_prompt__mutmut_orig # type: ignore # mutmut generated
mutants_x__templated_worker_prompt__mutmut['x__templated_worker_prompt__mutmut_1'] = x__templated_worker_prompt__mutmut_1 # type: ignore # mutmut generated
mutants_x__templated_worker_prompt__mutmut['x__templated_worker_prompt__mutmut_2'] = x__templated_worker_prompt__mutmut_2 # type: ignore # mutmut generated
mutants_x__templated_worker_prompt__mutmut['x__templated_worker_prompt__mutmut_3'] = x__templated_worker_prompt__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁdispatch__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁ_resolve_preset__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁ_dispatch_auto__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁ_dispatch_custom__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁ_dispatch_preset__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDispatcherǁ_call_dispatcher__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# Dispatcher
# --------------------------------------------------------------------------- #


class Dispatcher:
    """Runs the one dispatcher model call and returns a :class:`DispatchResult`."""

    @_mutmut_mutated(mutants_xǁDispatcherǁ__init____mutmut)
    def __init__(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self.config = config
        self.gateway = gateway
        self.model = config.defaults.dispatcher

    def xǁDispatcherǁ__init____mutmut_orig(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self.config = config
        self.gateway = gateway
        self.model = config.defaults.dispatcher

    def xǁDispatcherǁ__init____mutmut_1(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self.config = None
        self.gateway = gateway
        self.model = config.defaults.dispatcher

    def xǁDispatcherǁ__init____mutmut_2(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self.config = config
        self.gateway = None
        self.model = config.defaults.dispatcher

    def xǁDispatcherǁ__init____mutmut_3(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self.config = config
        self.gateway = gateway
        self.model = None

    @_mutmut_mutated(mutants_xǁDispatcherǁdispatch__mutmut)
    async def dispatch(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_orig(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_1(
        self,
        user_prompt: str,
        formation: str = "XXautoXX",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_2(
        self,
        user_prompt: str,
        formation: str = "AUTO",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_3(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = None
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_4(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_5(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = None
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_6(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(None, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_7(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, None)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_8(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_9(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, )
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_10(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = None
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_11(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(None)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_12(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None or not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_13(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_14(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_15(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = None
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_16(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    None, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_17(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, None, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_18(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, None
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_19(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_20(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_21(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_22(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = None
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_23(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(None)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_24(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = None
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_25(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int(None)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_26(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) / 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_27(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() + start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_28(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1001)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_29(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=None, messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_30(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=None, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_31(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=None, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_32(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, latency_ms=None
        )

    async def xǁDispatcherǁdispatch__mutmut_33(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            messages=messages, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_34(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, response=response, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_35(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, latency_ms=latency_ms
        )

    async def xǁDispatcherǁdispatch__mutmut_36(
        self,
        user_prompt: str,
        formation: str = "auto",
        *,
        custom_dag: FormationDAG | None = None,
    ) -> DispatchOutcome:
        """Run the single dispatcher model call.

        When ``custom_dag`` is supplied (a validated client/config-defined DAG),
        the DESIGN phase is skipped: the dispatcher only performs the FILL-IN pass
        (writing per-worker prompts + merge instructions) against that fixed DAG.
        Otherwise the preset (``formation``) or ``auto`` path is used.
        """
        start = time.monotonic()
        if custom_dag is not None:
            messages, response, result = await self._dispatch_custom(user_prompt, custom_dag)
        else:
            preset = self._resolve_preset(formation)
            if preset is not None and not preset.is_auto:
                messages, response, result = await self._dispatch_preset(
                    user_prompt, formation, preset
                )
            else:
                messages, response, result = await self._dispatch_auto(user_prompt)
        latency_ms = int((time.monotonic() - start) * 1000)
        return DispatchOutcome(
            result=result, messages=messages, response=response, )

    @_mutmut_mutated(mutants_xǁDispatcherǁ_resolve_preset__mutmut)
    def _resolve_preset(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_orig(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_1(self, formation: str) -> FormationPreset | None:
        if formation in self.config.formations:
            log.warning("unknown_formation", formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_2(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning(None, formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_3(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=None, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_4(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, using=None)
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_5(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning(formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_6(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_7(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, )
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_8(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("XXunknown_formationXX", formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_9(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("UNKNOWN_FORMATION", formation=formation, using="auto")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_10(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, using="XXautoXX")
            return None
        return self.config.formations[formation]

    def xǁDispatcherǁ_resolve_preset__mutmut_11(self, formation: str) -> FormationPreset | None:
        if formation not in self.config.formations:
            log.warning("unknown_formation", formation=formation, using="AUTO")
            return None
        return self.config.formations[formation]

    @_mutmut_mutated(mutants_xǁDispatcherǁ_dispatch_auto__mutmut)
    async def _dispatch_auto(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_orig(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_1(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = None
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_2(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(None, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_3(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, None, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_4(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_5(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_6(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, )
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_7(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = None
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_8(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(None)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_9(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = None
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_10(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(None, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_11(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, None, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_12(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_13(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_14(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, )
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_15(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            None,
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_16(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=None,
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_17(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            source=None,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_18(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_19(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_20(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "dispatch_auto_done",
            stages=result.formation.stage_ids(),
            )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_21(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "XXdispatch_auto_doneXX",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_auto__mutmut_22(
        self, user_prompt: str
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        log.info(
            "DISPATCH_AUTO_DONE",
            stages=result.formation.stage_ids(),
            source=result.source,
        )
        return messages, response, result

    @_mutmut_mutated(mutants_xǁDispatcherǁ_dispatch_custom__mutmut)
    async def _dispatch_custom(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_orig(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_1(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = None
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_2(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(None, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_3(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, None, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_4(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_5(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_6(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_7(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, )
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_8(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = None
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_9(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(None)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_10(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = None
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_11(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(None, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_12(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, None, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_13(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_14(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_15(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_16(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, )
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_17(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = None
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_18(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = None
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_19(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "XXcustomXX"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_20(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "CUSTOM"
        log.info(
            "dispatch_custom_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_21(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            None,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_22(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            stages=None,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_23(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_24(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "dispatch_custom_done",
            )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_25(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "XXdispatch_custom_doneXX",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_custom__mutmut_26(
        self, user_prompt: str, dag: FormationDAG
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        """Fill-in pass for a client/config-defined DAG (no design phase)."""
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the client-provided structure exactly.
        result.formation = dag
        result.source = "custom"
        log.info(
            "DISPATCH_CUSTOM_DONE",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    @_mutmut_mutated(mutants_xǁDispatcherǁ_dispatch_preset__mutmut)
    async def _dispatch_preset(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_orig(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_1(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = None
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_2(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(None, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_3(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, None)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_4(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_5(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, )
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_6(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = None
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_7(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(None, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_8(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, None, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_9(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=None)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_10(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_11(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_12(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, )
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_13(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = None
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_14(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(None)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_15(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = None
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_16(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(None, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_17(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, None, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_18(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=None)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_19(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_20(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_21(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, )
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_22(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = None
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_23(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = None
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_24(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "XXpresetXX"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_25(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "PRESET"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_26(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            None,
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_27(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=None,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_28(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            stages=None,
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_29(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_30(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_31(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "dispatch_preset_done",
            formation=formation_name,
            )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_32(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "XXdispatch_preset_doneXX",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    async def xǁDispatcherǁ_dispatch_preset__mutmut_33(
        self, user_prompt: str, formation_name: str, preset: FormationPreset
    ) -> tuple[list[dict[str, str]], GatewayResponse, DispatchResult]:
        dag = build_preset_dag(preset, self.config)
        messages = build_dispatcher_prompt(user_prompt, self.config, fixed_dag=dag)
        response = await self._call_dispatcher(messages)
        result = parse_dispatch_result(response.text, self.config, fallback_dag=dag)
        # Always honor the preset structure exactly.
        result.formation = dag
        result.source = "preset"
        log.info(
            "DISPATCH_PRESET_DONE",
            formation=formation_name,
            stages=dag.stage_ids(),
        )
        return messages, response, result

    @_mutmut_mutated(mutants_xǁDispatcherǁ_call_dispatcher__mutmut)
    async def _call_dispatcher(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_orig(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_1(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                None,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_2(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                None,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_3(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=None,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_4(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format=None,
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_5(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_6(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_7(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_8(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_9(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=1.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_10(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"XXtypeXX": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_11(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"TYPE": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_12(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "XXjson_objectXX"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_13(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "JSON_OBJECT"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_14(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning(None, error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_15(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=None, using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_16(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using=None)
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_17(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning(error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_18(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_19(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), )
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_20(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("XXdispatcher_call_failedXX", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_21(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("DISPATCHER_CALL_FAILED", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_22(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(None), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_23(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="XXfallbackXX")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_24(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="FALLBACK")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_25(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text=None, model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_26(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=None, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_27(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=None, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_28(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=None)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_29(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_30(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_31(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_32(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, )

    async def xǁDispatcherǁ_call_dispatcher__mutmut_33(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="XX{}XX", model=self.model, tokens_input=0, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_34(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=1, tokens_output=0)

    async def xǁDispatcherǁ_call_dispatcher__mutmut_35(self, messages: list[dict[str, str]]) -> GatewayResponse:
        try:
            return await self.gateway.complete(
                self.model,
                messages,
                temperature=0.1,
                response_format={"type": "json_object"},
            )
        except GatewayError as exc:
            log.warning("dispatcher_call_failed", error=str(exc), using="fallback")
            return GatewayResponse(text="{}", model=self.model, tokens_input=0, tokens_output=1)

mutants_xǁDispatcherǁ__init____mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ__init____mutmut['xǁDispatcherǁ__init____mutmut_1'] = Dispatcher.xǁDispatcherǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ__init____mutmut['xǁDispatcherǁ__init____mutmut_2'] = Dispatcher.xǁDispatcherǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ__init____mutmut['xǁDispatcherǁ__init____mutmut_3'] = Dispatcher.xǁDispatcherǁ__init____mutmut_3 # type: ignore # mutmut generated

mutants_xǁDispatcherǁdispatch__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_1'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_2'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_3'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_4'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_5'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_6'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_7'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_8'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_9'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_10'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_11'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_12'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_13'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_14'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_15'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_16'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_17'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_18'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_19'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_20'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_21'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_22'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_23'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_24'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_25'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_26'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_27'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_28'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_29'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_30'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_31'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_32'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_33'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_34'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_35'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDispatcherǁdispatch__mutmut['xǁDispatcherǁdispatch__mutmut_36'] = Dispatcher.xǁDispatcherǁdispatch__mutmut_36 # type: ignore # mutmut generated

mutants_xǁDispatcherǁ_resolve_preset__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_1'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_2'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_3'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_4'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_5'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_6'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_7'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_8'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_9'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_10'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_resolve_preset__mutmut['xǁDispatcherǁ_resolve_preset__mutmut_11'] = Dispatcher.xǁDispatcherǁ_resolve_preset__mutmut_11 # type: ignore # mutmut generated

mutants_xǁDispatcherǁ_dispatch_auto__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_1'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_2'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_3'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_4'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_5'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_6'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_7'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_8'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_9'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_10'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_11'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_12'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_13'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_14'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_15'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_16'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_17'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_18'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_19'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_20'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_21'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_auto__mutmut['xǁDispatcherǁ_dispatch_auto__mutmut_22'] = Dispatcher.xǁDispatcherǁ_dispatch_auto__mutmut_22 # type: ignore # mutmut generated

mutants_xǁDispatcherǁ_dispatch_custom__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_1'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_2'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_3'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_4'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_5'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_6'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_7'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_8'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_9'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_10'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_11'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_12'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_13'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_14'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_15'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_16'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_17'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_18'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_19'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_20'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_21'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_22'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_23'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_24'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_25'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_custom__mutmut['xǁDispatcherǁ_dispatch_custom__mutmut_26'] = Dispatcher.xǁDispatcherǁ_dispatch_custom__mutmut_26 # type: ignore # mutmut generated

mutants_xǁDispatcherǁ_dispatch_preset__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_1'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_2'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_3'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_4'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_5'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_6'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_7'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_8'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_9'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_10'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_11'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_12'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_13'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_14'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_15'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_16'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_17'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_18'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_19'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_20'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_21'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_22'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_23'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_24'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_25'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_26'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_27'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_28'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_29'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_30'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_31'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_32'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_dispatch_preset__mutmut['xǁDispatcherǁ_dispatch_preset__mutmut_33'] = Dispatcher.xǁDispatcherǁ_dispatch_preset__mutmut_33 # type: ignore # mutmut generated

mutants_xǁDispatcherǁ_call_dispatcher__mutmut['_mutmut_orig'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_1'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_2'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_3'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_4'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_5'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_6'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_7'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_8'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_9'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_10'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_11'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_12'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_13'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_14'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_15'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_16'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_17'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_18'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_19'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_20'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_21'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_22'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_23'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_24'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_25'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_26'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_27'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_28'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_29'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_30'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_31'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_32'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_33'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_34'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDispatcherǁ_call_dispatcher__mutmut['xǁDispatcherǁ_call_dispatcher__mutmut_35'] = Dispatcher.xǁDispatcherǁ_call_dispatcher__mutmut_35 # type: ignore # mutmut generated


__all__ = [
    "DispatchOutcome",
    "DispatchResult",
    "Dispatcher",
    "FormationDAG",
    "Stage",
    "WorkerPrompt",
    "build_dag_from_dict",
    "build_dispatcher_prompt",
    "build_preset_dag",
    "parse_dispatch_result",
]
