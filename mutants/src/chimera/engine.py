"""The formation engine — runs the dispatcher-designed DAG.

Executes stages in dependency waves: all stages whose dependencies are met run
concurrently via :func:`asyncio.gather`. Every call is traced into a
:class:`DeliberationTrace` (prompt, response, tokens, latency, cost).
"""

from __future__ import annotations

import asyncio
import json
import time
import uuid
from typing import Any

import structlog
from pydantic import BaseModel, Field

from chimera.aggregator import Aggregator, StageResult
from chimera.config import ChimeraConfig, DeliberationOverrides
from chimera.dispatcher import (
    Dispatcher,
    DispatchOutcome,
    DispatchResult,
    FormationDAG,
    Stage,
    build_dag_from_dict,
)
from chimera.gateway import Gateway, GatewayError, GatewayResponse
from chimera.exceptions import BudgetExhaustedError
from chimera.observability import get_langfuse

log = structlog.get_logger("chimera.engine")

#: Per-stage wall-clock timeout (seconds). A stage that exceeds this is cancelled
#: and recorded as degraded so one slow worker cannot block the deliberation.
#: Tests monkeypatch this module global to exercise the timeout path quickly.
DEFAULT_STAGE_TIMEOUT_S: float = 120.0


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


class StageSpan(BaseModel):
    """One traced stage execution."""

    stage_id: str
    kind: str
    model: str
    prompt: str
    response: str
    tokens_input: int = 0
    tokens_output: int = 0
    latency_ms: int = 0
    cost: float = 0.0
    depends_on: list[str] = Field(default_factory=list)


class DeliberationTrace(BaseModel):
    """Full trace of a deliberation run."""

    request_id: str
    formation: str
    source: str
    dispatch: StageSpan
    workers: list[StageSpan] = Field(default_factory=list)
    aggregator: StageSpan | None = None
    stages: list[StageSpan] = Field(default_factory=list)
    answer_stage_id: str = ""
    total_duration_ms: int = 0
    total_cost: float = 0.0
    total_tokens: int = 0


class DeliberationResult(BaseModel):
    """The user-facing result of a deliberation."""

    answer: str
    trace: DeliberationTrace
mutants_x__stage_cost__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__stage_cost__mutmut)
def _stage_cost(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_orig(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_1(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = None
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_2(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(None)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_3(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 1.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_4(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0 - entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_5(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input * 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_6(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() / tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_7(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1001.0
        + entry.cost_rate_output() * tokens_output / 1000.0
    )


def x__stage_cost__mutmut_8(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output * 1000.0
    )


def x__stage_cost__mutmut_9(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() / tokens_output / 1000.0
    )


def x__stage_cost__mutmut_10(model: str, config: ChimeraConfig, tokens_input: int, tokens_output: int) -> float:
    try:
        entry = config.get_model(model)
    except KeyError:
        return 0.0
    return (
        entry.cost_rate_input() * tokens_input / 1000.0
        + entry.cost_rate_output() * tokens_output / 1001.0
    )

mutants_x__stage_cost__mutmut['_mutmut_orig'] = x__stage_cost__mutmut_orig # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_1'] = x__stage_cost__mutmut_1 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_2'] = x__stage_cost__mutmut_2 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_3'] = x__stage_cost__mutmut_3 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_4'] = x__stage_cost__mutmut_4 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_5'] = x__stage_cost__mutmut_5 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_6'] = x__stage_cost__mutmut_6 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_7'] = x__stage_cost__mutmut_7 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_8'] = x__stage_cost__mutmut_8 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_9'] = x__stage_cost__mutmut_9 # type: ignore # mutmut generated
mutants_x__stage_cost__mutmut['x__stage_cost__mutmut_10'] = x__stage_cost__mutmut_10 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__last_user_content__mutmut)
def _last_user_content(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_orig(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_1(messages: list[dict[str, str]]) -> str:
    for msg in reversed(None):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_2(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get(None) == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_3(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("XXroleXX") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_4(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("ROLE") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_5(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") != "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_6(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "XXuserXX":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_7(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "USER":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_8(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get(None, "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_9(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", None)
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_10(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_11(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", )
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_12(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("XXcontentXX", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_13(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("CONTENT", "")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_14(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "XXXX")
    return messages[-1].get("content", "") if messages else ""


def x__last_user_content__mutmut_15(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get(None, "") if messages else ""


def x__last_user_content__mutmut_16(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", None) if messages else ""


def x__last_user_content__mutmut_17(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("") if messages else ""


def x__last_user_content__mutmut_18(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", ) if messages else ""


def x__last_user_content__mutmut_19(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[+1].get("content", "") if messages else ""


def x__last_user_content__mutmut_20(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-2].get("content", "") if messages else ""


def x__last_user_content__mutmut_21(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("XXcontentXX", "") if messages else ""


def x__last_user_content__mutmut_22(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("CONTENT", "") if messages else ""


def x__last_user_content__mutmut_23(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "XXXX") if messages else ""


def x__last_user_content__mutmut_24(messages: list[dict[str, str]]) -> str:
    for msg in reversed(messages):
        if msg.get("role") == "user":
            return msg.get("content", "")
    return messages[-1].get("content", "") if messages else "XXXX"

mutants_x__last_user_content__mutmut['_mutmut_orig'] = x__last_user_content__mutmut_orig # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_1'] = x__last_user_content__mutmut_1 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_2'] = x__last_user_content__mutmut_2 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_3'] = x__last_user_content__mutmut_3 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_4'] = x__last_user_content__mutmut_4 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_5'] = x__last_user_content__mutmut_5 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_6'] = x__last_user_content__mutmut_6 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_7'] = x__last_user_content__mutmut_7 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_8'] = x__last_user_content__mutmut_8 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_9'] = x__last_user_content__mutmut_9 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_10'] = x__last_user_content__mutmut_10 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_11'] = x__last_user_content__mutmut_11 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_12'] = x__last_user_content__mutmut_12 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_13'] = x__last_user_content__mutmut_13 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_14'] = x__last_user_content__mutmut_14 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_15'] = x__last_user_content__mutmut_15 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_16'] = x__last_user_content__mutmut_16 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_17'] = x__last_user_content__mutmut_17 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_18'] = x__last_user_content__mutmut_18 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_19'] = x__last_user_content__mutmut_19 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_20'] = x__last_user_content__mutmut_20 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_21'] = x__last_user_content__mutmut_21 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_22'] = x__last_user_content__mutmut_22 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_23'] = x__last_user_content__mutmut_23 # type: ignore # mutmut generated
mutants_x__last_user_content__mutmut['x__last_user_content__mutmut_24'] = x__last_user_content__mutmut_24 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__apply_stage_models__mutmut)
def _apply_stage_models(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_orig(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_1(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_2(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = None
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_3(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(None)
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_4(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_5(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning(None, stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_6(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=None)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_7(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning(stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_8(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", )
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_9(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("XXstage_model_unknown_stageXX", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_10(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("STAGE_MODEL_UNKNOWN_STAGE", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_11(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            break
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_12(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_13(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                None
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_14(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = None
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_15(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(None)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_16(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = None
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_17(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind != "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_18(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "XXworkerXX":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_19(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "WORKER":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_20(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = None
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_21(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(None)
            if wp is not None:
                wp.model = model


def x__apply_stage_models__mutmut_22(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is None:
                wp.model = model


def x__apply_stage_models__mutmut_23(
    dispatch: DispatchResult,
    stage_models: dict[str, str] | None,
    config: ChimeraConfig,
) -> None:
    """Force per-stage models (stage_id → model) onto a dispatch plan.

    * Unknown stage ids are logged as warnings and skipped (never fatal).
    * Unknown model names raise ``ValueError`` (validated against the catalog).
    * Worker prompt model entries are kept in sync with the stage model.
    * Applies to ``auto``, ``preset`` and ``custom`` DAGs uniformly. Per-stage
      overrides take precedence over global request overrides.
    """
    if not stage_models:
        return
    stage_ids = set(dispatch.formation.stage_ids())
    for stage_id, model in stage_models.items():
        if stage_id not in stage_ids:
            log.warning("stage_model_unknown_stage", stage_id=stage_id)
            continue
        if model not in config.models:
            raise ValueError(
                f"stage_models references unknown model for stage "
                f"{stage_id!r}: {model!r}"
            )
        stage = dispatch.formation.stage(stage_id)
        stage.model = model
        if stage.kind == "worker":
            wp = dispatch.worker_prompt_for(stage_id)
            if wp is not None:
                wp.model = None

mutants_x__apply_stage_models__mutmut['_mutmut_orig'] = x__apply_stage_models__mutmut_orig # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_1'] = x__apply_stage_models__mutmut_1 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_2'] = x__apply_stage_models__mutmut_2 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_3'] = x__apply_stage_models__mutmut_3 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_4'] = x__apply_stage_models__mutmut_4 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_5'] = x__apply_stage_models__mutmut_5 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_6'] = x__apply_stage_models__mutmut_6 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_7'] = x__apply_stage_models__mutmut_7 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_8'] = x__apply_stage_models__mutmut_8 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_9'] = x__apply_stage_models__mutmut_9 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_10'] = x__apply_stage_models__mutmut_10 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_11'] = x__apply_stage_models__mutmut_11 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_12'] = x__apply_stage_models__mutmut_12 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_13'] = x__apply_stage_models__mutmut_13 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_14'] = x__apply_stage_models__mutmut_14 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_15'] = x__apply_stage_models__mutmut_15 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_16'] = x__apply_stage_models__mutmut_16 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_17'] = x__apply_stage_models__mutmut_17 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_18'] = x__apply_stage_models__mutmut_18 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_19'] = x__apply_stage_models__mutmut_19 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_20'] = x__apply_stage_models__mutmut_20 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_21'] = x__apply_stage_models__mutmut_21 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_22'] = x__apply_stage_models__mutmut_22 # type: ignore # mutmut generated
mutants_x__apply_stage_models__mutmut['x__apply_stage_models__mutmut_23'] = x__apply_stage_models__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_compute_config_checksum__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_check_config_mutation__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁdeliberate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_resolve_custom_dag__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_run_dag__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_execute_stage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_call_stage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_degraded_stage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_worker_messages__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_build_dispatch_span__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_select_answer__mutmut: MutantDict = {}  # type: ignore
mutants_xǁEngineǁ_assemble_trace__mutmut: MutantDict = {}  # type: ignore


class Engine:
    """Runs the full pipeline: dispatcher → workers → aggregator → (merge/audit).

    On construction the config is deep-copied into an immutable snapshot.
    All in-flight requests read from the snapshot, not the caller's live
    object, so configuration is stable for the lifetime of the Engine instance.

    Mutation checks on the caller's original config object are logged as
    warnings so operators know when a change they made won't take effect.
    """

    # Fast checksum of struct fields to detect external mutations.
    _CONFIG_CHECK_FIELDS = (
        "defaults.dispatcher",
        "defaults.default_worker",
        "defaults.default_aggregator",
        "defaults.lock_dispatcher",
        "defaults.lock_aggregator",
        "models",
        "formations",
        "providers",
        "api_keys",
    )

    @_mutmut_mutated(mutants_xǁEngineǁ__init____mutmut)
    def __init__(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_orig(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_1(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = None
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_2(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = None
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_3(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=None)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_4(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=False)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_5(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = None
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_6(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(None)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_7(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = None
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_8(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = None
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_9(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(None, gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_10(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, None)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_11(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(gateway)
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_12(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, )
        self.aggregator = Aggregator(self._config, gateway)

    def xǁEngineǁ__init____mutmut_13(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = None

    def xǁEngineǁ__init____mutmut_14(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(None, gateway)

    def xǁEngineǁ__init____mutmut_15(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, None)

    def xǁEngineǁ__init____mutmut_16(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(gateway)

    def xǁEngineǁ__init____mutmut_17(self, config: ChimeraConfig, gateway: Gateway) -> None:
        self._config_original = config
        self._config = config.model_copy(deep=True)
        self._config_checksum = self._compute_config_checksum(self._config_original)
        self.gateway = gateway
        self.dispatcher = Dispatcher(self._config, gateway)
        self.aggregator = Aggregator(self._config, )

    @property
    def config(self) -> ChimeraConfig:
        """The frozen config snapshot, safe from external mutation."""
        return self._config

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    @_mutmut_mutated(mutants_xǁEngineǁ_compute_config_checksum__mutmut)
    def _compute_config_checksum(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_orig(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_1(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = None
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_2(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode=None, exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_3(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude=None)
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_4(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_5(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", )
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_6(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="XXpythonXX", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_7(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="PYTHON", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_8(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"XXobservabilityXX", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_9(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"OBSERVABILITY", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_10(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "XXserverXX"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_11(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "SERVER"})
        return hash(json.dumps(raw, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_12(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(None)

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_13(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(None, sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_14(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=None, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_15(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, default=None))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_16(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(sort_keys=True, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_17(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, default=str))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_18(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=True, ))

    # ------------------------------------------------------------------ #
    # Config mutation detection
    # ------------------------------------------------------------------ #

    @staticmethod
    def xǁEngineǁ_compute_config_checksum__mutmut_19(cfg: ChimeraConfig) -> int:
        """Compute a fast structural checksum of key config fields.

        Pydantic model_dump with sort_keys=True produces a stable string
        that changes when any nested field is mutated.
        """
        raw = cfg.model_dump(mode="python", exclude={"observability", "server"})
        return hash(json.dumps(raw, sort_keys=False, default=str))

    @_mutmut_mutated(mutants_xǁEngineǁ_check_config_mutation__mutmut)
    def _check_config_mutation(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_orig(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_1(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = None
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_2(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(None)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_3(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current == self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_4(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                None,
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_5(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=None,
            )

    def xǁEngineǁ_check_config_mutation__mutmut_6(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_7(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                )

    def xǁEngineǁ_check_config_mutation__mutmut_8(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "XXconfig_mutated_after_snapshotXX",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_9(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "CONFIG_MUTATED_AFTER_SNAPSHOT",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_10(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "XXThe ChimeraConfig object passed to Engine() has been mutated XX"
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_11(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "the chimeraconfig object passed to engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_12(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "THE CHIMERACONFIG OBJECT PASSED TO ENGINE() HAS BEEN MUTATED "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_13(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "XXsince the Engine was constructed. In-flight requests use the XX"
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_14(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the engine was constructed. in-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_15(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "SINCE THE ENGINE WAS CONSTRUCTED. IN-FLIGHT REQUESTS USE THE "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_16(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "XXfrozen snapshot, so external mutations will NOT take effect XX"
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_17(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will not take effect "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_18(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "FROZEN SNAPSHOT, SO EXTERNAL MUTATIONS WILL NOT TAKE EFFECT "
                    "until a new Engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_19(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "XXuntil a new Engine instance is created.XX"
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_20(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "until a new engine instance is created."
                ),
            )

    def xǁEngineǁ_check_config_mutation__mutmut_21(self) -> None:
        """Log a warning if the caller's original config was mutated since init."""
        current = self._compute_config_checksum(self._config_original)
        if current != self._config_checksum:
            log.warning(
                "config_mutated_after_snapshot",
                msg=(
                    "The ChimeraConfig object passed to Engine() has been mutated "
                    "since the Engine was constructed. In-flight requests use the "
                    "frozen snapshot, so external mutations will NOT take effect "
                    "UNTIL A NEW ENGINE INSTANCE IS CREATED."
                ),
            )

    @_mutmut_mutated(mutants_xǁEngineǁdeliberate__mutmut)
    async def deliberate(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_orig(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_1(
        self,
        user_prompt: str,
        formation: str = "XXautoXX",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_2(
        self,
        user_prompt: str,
        formation: str = "AUTO",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_3(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = True,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_4(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = None
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_5(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:17]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_6(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=None, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_7(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=None)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_8(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_9(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, )
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_10(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = None

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_11(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = None
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_12(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(None, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_13(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, None)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_14(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_15(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, )
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_16(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = None
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_17(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            None, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_18(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, None, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_19(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=None
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_20(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_21(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_22(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_23(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = None

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_24(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(None, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_25(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, None)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_26(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_27(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, )

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_28(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_29(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(None, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_30(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, None, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_31(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, None)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_32(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_33(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_34(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, )

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_35(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            None,
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_36(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=None,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_37(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=None,
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_38(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_39(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_40(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_41(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "XXengine_dispatchedXX",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_42(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "ENGINE_DISPATCHED",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_43(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = None

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_44(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None) and outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_45(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema and (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_46(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = None

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_47(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            None, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_48(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, None, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_49(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, None, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_50(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, None
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_51(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_52(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_53(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_54(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_55(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = None
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_56(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(None, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_57(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, None)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_58(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_59(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, )
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_60(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = None
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_61(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=None,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_62(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=None,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_63(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=None,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_64(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=None,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_65(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=None,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_66(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=None,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_67(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=None,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_68(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_69(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_70(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_71(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_72(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_73(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_74(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_75(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(None, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_76(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, None)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_77(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_78(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, )
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_79(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars(None, "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_80(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", None)
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_81(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_82(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", )
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_83(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("XXrequest_idXX", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_84(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("REQUEST_ID", "formation")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_85(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "XXformationXX")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_86(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "FORMATION")
        return DeliberationResult(answer=answer, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_87(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=None, trace=trace)

    async def xǁEngineǁdeliberate__mutmut_88(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, trace=None)

    async def xǁEngineǁdeliberate__mutmut_89(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(trace=trace)

    async def xǁEngineǁdeliberate__mutmut_90(
        self,
        user_prompt: str,
        formation: str = "auto",
        overrides: DeliberationOverrides | None = None,
        *,
        output_schema: dict[str, Any] | None = None,
        dag: dict[str, Any] | None = None,
        allow_custom_dag: bool = False,
    ) -> DeliberationResult:
        """Run the full deliberation pipeline and return the merged answer + trace.

        Args:
            user_prompt: The user's query.
            formation: Formation name or 'auto'.
            overrides: Request-level model overrides (allowed/disallowed/force models,
                per-stage model overrides via ``stage_models``).
            output_schema: Optional JSON Schema the final answer must conform to.
                If omitted, the dispatcher provides one. If the client provides one,
                it overrides the dispatcher's.
            dag: Optional client-defined DAG definition (a mapping with ``stages``
                and ``edges``). Requires ``allow_custom_dag=True``; otherwise a
                ``ValueError`` is raised. When enabled, the dispatcher skips the
                DESIGN pass and only fills in per-stage prompts/instructions.
            allow_custom_dag: Must be True for ``dag`` to be accepted.
        """
        request_id = uuid.uuid4().hex[:16]
        structlog.contextvars.bind_contextvars(request_id=request_id, formation=formation)
        started = time.monotonic()

        # Warn if the caller's original config has been mutated since Engine init.
        self._check_config_mutation()

        custom_formation = self._resolve_custom_dag(dag, allow_custom_dag)
        outcome = await self.dispatcher.dispatch(
            user_prompt, formation, custom_dag=custom_formation
        )
        dispatch_span = self._build_dispatch_span(outcome, formation)

        # Apply per-stage model overrides (Feature 2). Unknown stages warn;
        # unknown models raise ValueError. Done after dispatch so it applies to
        # auto, preset, and custom DAGs alike.
        stage_models = overrides.stage_models if overrides else None
        _apply_stage_models(outcome.result, stage_models, self.config)

        log.info(
            "engine_dispatched",
            source=outcome.result.source,
            stages=outcome.result.formation.stage_ids(),
        )

        # Client-provided schema wins; dispatcher's is the fallback.
        effective_schema = (
            output_schema
            or (overrides.output_schema if overrides else None)
            or outcome.result.output_schema
        )

        stage_spans, stage_results = await self._run_dag(
            outcome.result, user_prompt, request_id, effective_schema
        )

        answer, answer_stage_id = self._select_answer(outcome.result.formation, stage_results)
        trace = self._assemble_trace(
            request_id=request_id,
            formation=formation,
            dispatch=outcome.result,
            dispatch_span=dispatch_span,
            stage_spans=stage_spans,
            answer_stage_id=answer_stage_id,
            started=started,
        )
        _maybe_langfuse(trace, user_prompt)
        structlog.contextvars.unbind_contextvars("request_id", "formation")
        return DeliberationResult(answer=answer, )

    @_mutmut_mutated(mutants_xǁEngineǁ_resolve_custom_dag__mutmut)
    def _resolve_custom_dag(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_orig(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_1(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is not None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_2(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_3(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError(None)
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_4(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("XXCustom DAG requires allow_custom_dag=TrueXX")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_5(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("custom dag requires allow_custom_dag=true")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_6(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("CUSTOM DAG REQUIRES ALLOW_CUSTOM_DAG=TRUE")
        return build_dag_from_dict(dag, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_7(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(None, self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_8(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, None)

    def xǁEngineǁ_resolve_custom_dag__mutmut_9(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(self.config)

    def xǁEngineǁ_resolve_custom_dag__mutmut_10(
        self,
        dag: dict[str, Any] | None,
        allow_custom_dag: bool,
    ) -> FormationDAG | None:
        """Validate and build a client-defined DAG, or return None.

        Raises ``ValueError`` if a DAG was supplied without opting in, or if the
        DAG fails validation (unknown model, cycle, missing worker/aggregator, ...).
        """
        if dag is None:
            return None
        if not allow_custom_dag:
            raise ValueError("Custom DAG requires allow_custom_dag=True")
        return build_dag_from_dict(dag, )

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    @_mutmut_mutated(mutants_xǁEngineǁ_run_dag__mutmut)
    async def _run_dag(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_orig(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_1(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = None
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_2(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = None
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_3(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = None
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_4(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = None

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_5(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = None
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_6(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(None)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_7(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = None
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_8(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(None)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_9(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d not in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_10(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_11(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError(None)
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_12(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("XXDAG has unresolvable dependenciesXX")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_13(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("dag has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_14(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG HAS UNRESOLVABLE DEPENDENCIES")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_15(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = None
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_16(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(None, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_17(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, None, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_18(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, None, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_19(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, None, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_20(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, None) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_21(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_22(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_23(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_24(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_25(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, ) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_26(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(None, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_27(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, None, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_28(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=None):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_29(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_30(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_31(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, ):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_32(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=True):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_33(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = None
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_34(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = None
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_35(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    None,
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_36(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=None,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_37(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=None,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_38(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=None,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_39(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=None,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_40(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=None,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_41(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_42(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_43(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_44(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_45(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_46(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_47(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "XXengine_stage_doneXX",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_48(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "ENGINE_STAGE_DONE",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_49(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input - span.tokens_output,
                )
            remaining = [s for s in remaining if s not in ready]

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_50(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = None

        return spans, results

    # ------------------------------------------------------------------ #
    # DAG execution
    # ------------------------------------------------------------------ #

    async def xǁEngineǁ_run_dag__mutmut_51(
        self,
        dispatch: DispatchResult,
        user_prompt: str,
        request_id: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[dict[str, StageSpan], dict[str, StageResult]]:
        dag = dispatch.formation
        topo = dag.topo_order()
        spans: dict[str, StageSpan] = {}
        results: dict[str, StageResult] = {}

        remaining = list(topo)
        while remaining:
            ready = [s for s in remaining if all(d in results for d in s.depends_on)]
            if not ready:
                raise RuntimeError("DAG has unresolvable dependencies")
            outcomes = await asyncio.gather(
                *(self._execute_stage(s, dispatch, results, user_prompt, output_schema) for s in ready)
            )
            for stage, (result, span) in zip(ready, outcomes, strict=False):
                results[stage.id] = result
                spans[stage.id] = span
                log.info(
                    "engine_stage_done",
                    stage=stage.id,
                    kind=stage.kind,
                    model=span.model,
                    latency_ms=span.latency_ms,
                    tokens=span.tokens_input + span.tokens_output,
                )
            remaining = [s for s in remaining if s in ready]

        return spans, results

    @_mutmut_mutated(mutants_xǁEngineǁ_execute_stage__mutmut)
    async def _execute_stage(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_orig(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_1(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = None

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_2(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d not in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_3(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = None
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_4(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = None
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_5(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = None
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_6(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                None,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_7(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=None,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_8(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_9(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_10(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(None, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_11(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, None, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_12(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, None, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_13(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, None, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_14(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, None),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_15(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_16(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_17(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_18(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_19(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, ),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_20(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = None
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_21(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int(None)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_22(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) / 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_23(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() + start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_24(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1001)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_25(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                None,
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_26(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=None,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_27(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=None,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_28(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=None,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_29(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_30(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_31(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_32(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_33(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "XXengine_stage_timeoutXX",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_34(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "ENGINE_STAGE_TIMEOUT",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_35(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                None,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_36(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                None,
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_37(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                None,
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_38(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                None,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_39(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_40(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_41(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_42(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_43(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(None),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_44(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = None
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_45(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int(None)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_46(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) / 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_47(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() + start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_48(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1001)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_49(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(None, stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_50(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=None, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_51(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=None, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_52(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=None)
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_53(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_54(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_55(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_56(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, )
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_57(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("XXengine_stage_failedXX", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_58(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("ENGINE_STAGE_FAILED", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_59(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(None))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_60(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = None
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_61(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} or stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_62(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind not in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_63(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"XXaggregatorXX", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_64(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"AGGREGATOR", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_65(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "XXmergeXX", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_66(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "MERGE", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_67(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "XXauditXX"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_68(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "AUDIT"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_69(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model == fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_70(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = None
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_71(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update=None)
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_72(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"XXmodelXX": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_73(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"MODEL": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_74(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = None
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_75(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        None, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_76(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, None, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_77(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, None, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_78(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, None
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_79(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_80(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_81(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_82(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_83(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        None,
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_84(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=None, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_85(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=None, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_86(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=None,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_87(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_88(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_89(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_90(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_91(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "XXengine_stage_retry_okXX",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_92(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "ENGINE_STAGE_RETRY_OK",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_93(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(None, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_94(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, None, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_95(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, None, latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_96(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], None)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_97(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_98(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_99(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_100(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], )
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_101(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(None, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_102(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, None, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_103(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, None, latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_104(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], None)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_105(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_106(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_107(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_108(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], )

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_109(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = None

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_110(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int(None)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_111(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) / 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_112(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() + start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_113(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1001)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_114(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = None
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_115(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(None) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_116(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            None, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_117(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, None, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_118(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, None
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_119(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_120(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_121(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_122(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = None
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_123(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=None,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_124(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=None,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_125(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=None,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_126(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=None,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_127(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=None,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_128(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=None,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_129(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=None,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_130(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=None,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_131(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=None,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_132(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=None,
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_133(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_134(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_135(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_136(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_137(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_138(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_139(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_140(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_141(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_142(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_143(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(None, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_144(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, None, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_145(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, None, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_146(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, None),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_147(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_148(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_149(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_150(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, ),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_151(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(None),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_152(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = None
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_153(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=None,
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_154(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=None,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_155(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=None,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_156(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            response=None,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_157(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            model=response.model,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_158(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            prompt=prompt_text,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_159(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            response=response,
        )
        return result, span

    async def xǁEngineǁ_execute_stage__mutmut_160(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        results: dict[str, StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[StageResult, StageSpan]:
        dep_results = [results[d] for d in stage.depends_on if d in results]

        start = time.monotonic()
        messages: list[dict[str, str]] = []
        try:
            messages, response = await asyncio.wait_for(
                self._call_stage(stage, dispatch, dep_results, user_prompt, output_schema),
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
        except TimeoutError:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning(
                "engine_stage_timeout",
                stage=stage.id,
                model=stage.model,
                timeout=DEFAULT_STAGE_TIMEOUT_S,
            )
            return self._degraded_stage(
                stage,
                GatewayError(f"timed out after {DEFAULT_STAGE_TIMEOUT_S}s"),
                [],
                latency_ms,
            )
        except (GatewayError, BudgetExhaustedError) as exc:
            latency_ms = int((time.monotonic() - start) * 1000)
            log.warning("engine_stage_failed", stage=stage.id, model=stage.model, error=str(exc))
            # Answer-producing stages retry once with the default aggregator so the user
            # still gets a real answer if the dispatcher picked an unavailable aggregator.
            fallback_model = self.config.defaults.default_aggregator
            if stage.kind in {"aggregator", "merge", "audit"} and stage.model != fallback_model:
                retry_stage = stage.model_copy(update={"model": fallback_model})
                try:
                    messages, response = await self._call_stage(
                        retry_stage, dispatch, dep_results, user_prompt
                    )
                    log.info(
                        "engine_stage_retry_ok",
                        stage=stage.id, original=stage.model, fallback=fallback_model,
                    )
                except GatewayError as exc2:
                    return self._degraded_stage(stage, exc2, [], latency_ms)
            else:
                return self._degraded_stage(stage, exc, [], latency_ms)

        latency_ms = int((time.monotonic() - start) * 1000)

        prompt_text = _last_user_content(messages) if messages else _aggregator_prompt_summary(
            stage, dispatch, dep_results
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=response.model,
            prompt=prompt_text,
            response=response.text,
            tokens_input=response.tokens_input,
            tokens_output=response.tokens_output,
            latency_ms=latency_ms,
            cost=_stage_cost(response.model, self.config, response.tokens_input, response.tokens_output),
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=response.model,
            prompt=prompt_text,
            )
        return result, span

    @_mutmut_mutated(mutants_xǁEngineǁ_call_stage__mutmut)
    async def _call_stage(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_orig(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_1(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind != "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_2(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "XXworkerXX":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_3(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "WORKER":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_4(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = None
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_5(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(None, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_6(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, None, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_7(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, None)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_8(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_9(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_10(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, )
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_11(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = None
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_12(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(None, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_13(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, None, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_14(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=None)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_15(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_16(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_17(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, )
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_18(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=1.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_19(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = None
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_20(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            None, dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_21(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, None, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_22(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, None, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_23(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, None, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_24(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, output_schema=None,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_25(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            dispatch, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_26(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dep_results, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_27(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, user_prompt, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_28(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, output_schema=output_schema,
        )
        return [], response

    async def xǁEngineǁ_call_stage__mutmut_29(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        dep_results: list[StageResult],
        user_prompt: str,
        output_schema: dict[str, Any] | None = None,
    ) -> tuple[list[dict[str, str]], GatewayResponse]:
        """Make the actual model call for a stage; returns (messages, response)."""
        if stage.kind == "worker":
            messages = self._worker_messages(stage, dispatch, user_prompt)
            response = await self.gateway.complete(stage.model, messages, temperature=0.3)
            return messages, response
        response = await self.aggregator.execute(
            stage, dispatch, dep_results, user_prompt, )
        return [], response

    @_mutmut_mutated(mutants_xǁEngineǁ_degraded_stage__mutmut)
    def _degraded_stage(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_orig(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_1(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = None
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_2(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(None)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_3(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = None
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_4(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=None,
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_5(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=None,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_6(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=None,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_7(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=None,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_8(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_9(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_10(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_11(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_12(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=1,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_13(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=1,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_14(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = None
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_15(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=None,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_16(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=None,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_17(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=None,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_18(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=None,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_19(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=None,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_20(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=None,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_21(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=None,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_22(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=None,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_23(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=None,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_24(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=None,
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_25(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_26(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_27(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_28(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_29(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_30(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_31(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_32(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_33(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_34(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_35(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=1,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_36(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=1,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_37(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=1.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_38(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(None),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_39(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = None
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_40(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=None,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_41(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=None,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_42(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=None,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_43(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=None,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_44(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=None,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_45(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_46(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            prompt=prompt_text,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_47(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            response=degraded_response,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_48(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            degraded=True,
        )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_49(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            )
        return result, span

    def xǁEngineǁ_degraded_stage__mutmut_50(
        self,
        stage: Stage,
        error: Exception,
        messages: list[dict[str, str]],
        latency_ms: int,
    ) -> tuple[StageResult, StageSpan]:
        prompt_text = (
            _last_user_content(messages)
            if messages
            else f"(degraded {stage.kind} stage '{stage.id}')"
        )
        degraded_response = GatewayResponse(
            text=f"[stage {stage.id} ({stage.model}) unavailable: {error}]",
            model=stage.model,
            tokens_input=0,
            tokens_output=0,
        )
        span = StageSpan(
            stage_id=stage.id,
            kind=stage.kind,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response.text,
            tokens_input=0,
            tokens_output=0,
            latency_ms=latency_ms,
            cost=0.0,
            depends_on=list(stage.depends_on),
        )
        result = StageResult(
            stage_id=stage.id,
            model=stage.model,
            prompt=prompt_text,
            response=degraded_response,
            degraded=False,
        )
        return result, span

    @_mutmut_mutated(mutants_xǁEngineǁ_worker_messages__mutmut)
    def _worker_messages(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_orig(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_1(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = None
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_2(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(None)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_3(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = None
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_4(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp or wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_5(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "XXSolve the user's request.XX"
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_6(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "solve the user's request."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_7(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "SOLVE THE USER'S REQUEST."
        return [
            {
                "role": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_8(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "XXroleXX": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_9(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "ROLE": "user",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_10(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "XXuserXX",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_11(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "USER",
                "content": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_12(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "XXcontentXX": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    def xǁEngineǁ_worker_messages__mutmut_13(
        self,
        stage: Stage,
        dispatch: DispatchResult,
        user_prompt: str,
    ) -> list[dict[str, str]]:
        wp = dispatch.worker_prompt_for(stage.id)
        task = wp.prompt if wp and wp.prompt else "Solve the user's request."
        return [
            {
                "role": "user",
                "CONTENT": f"## User request\n{user_prompt}\n\n## Your assigned task\n{task}",
            }
        ]

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    @_mutmut_mutated(mutants_xǁEngineǁ_build_dispatch_span__mutmut)
    def _build_dispatch_span(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_orig(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_1(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = None
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_2(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = None
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_3(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(None)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_4(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id=None,
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_5(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind=None,
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_6(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=None,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_7(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=None,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_8(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=None,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_9(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=None,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_10(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=None,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_11(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=None,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_12(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=None,
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_13(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=None,
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_14(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_15(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_16(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_17(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_18(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_19(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_20(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_21(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_22(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_23(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_24(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="XXdispatchXX",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_25(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="DISPATCH",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_26(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="XXdispatchXX",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_27(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="DISPATCH",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_28(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                None,
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_29(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                None,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_30(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                None,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_31(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                None,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_32(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_33(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                resp.tokens_input,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_34(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_output,
            ),
            depends_on=[],
        )

    # ------------------------------------------------------------------ #
    # Trace assembly
    # ------------------------------------------------------------------ #

    def xǁEngineǁ_build_dispatch_span__mutmut_35(self, outcome: DispatchOutcome, formation: str) -> StageSpan:
        resp = outcome.response
        prompt_text = _last_user_content(outcome.messages)
        return StageSpan(
            stage_id="dispatch",
            kind="dispatch",
            model=self.config.defaults.dispatcher,
            prompt=prompt_text,
            response=resp.text,
            tokens_input=resp.tokens_input,
            tokens_output=resp.tokens_output,
            latency_ms=outcome.latency_ms,
            cost=_stage_cost(
                self.config.defaults.dispatcher,
                self.config,
                resp.tokens_input,
                ),
            depends_on=[],
        )

    @_mutmut_mutated(mutants_xǁEngineǁ_select_answer__mutmut)
    def _select_answer(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_orig(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_1(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = None
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_2(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_3(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = None
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_4(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[+1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_5(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-2]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_6(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) != 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_7(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 2:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_8(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = None
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_9(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[1]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_10(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = None
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_11(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id not in results
        ]
        return "\n\n".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_12(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(None), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_13(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "XX\n\nXX".join(parts), terminals[0].id

    def xǁEngineǁ_select_answer__mutmut_14(
        self, dag: FormationDAG, results: dict[str, StageResult]
    ) -> tuple[str, str]:
        terminals = dag.terminals()
        if not terminals:
            # fall back to last stage in topo order
            last = dag.topo_order()[-1]
            return results[last.id].response.text, last.id
        if len(terminals) == 1:
            t = terminals[0]
            return results[t.id].response.text, t.id
        parts = [
            f"### {t.id} ({results[t.id].model})\n{results[t.id].response.text}"
            for t in terminals
            if t.id in results
        ]
        return "\n\n".join(parts), terminals[1].id

    @_mutmut_mutated(mutants_xǁEngineǁ_assemble_trace__mutmut)
    def _assemble_trace(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_orig(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_1(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = None
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_2(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind != "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_3(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "XXworkerXX"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_4(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "WORKER"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_5(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = None
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_6(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            None,
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_7(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_8(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_9(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind not in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_10(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"XXaggregatorXX", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_11(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"AGGREGATOR", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_12(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "XXmergeXX", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_13(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "MERGE", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_14(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "XXauditXX"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_15(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "AUDIT"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_16(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = None
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_17(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = None
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_18(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(None, 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_19(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), None)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_20(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_21(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), )
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_22(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(None), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_23(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 7)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_24(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = None
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_25(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(None)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_26(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input - s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_27(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=None,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_28(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=None,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_29(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=None,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_30(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=None,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_31(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=None,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_32(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=None,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_33(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=None,
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_34(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=None,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_35(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=None,
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_36(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=None,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_37(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=None,
        )

    def xǁEngineǁ_assemble_trace__mutmut_38(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_39(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_40(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_41(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_42(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_43(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_44(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_45(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_46(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_47(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_48(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            )

    def xǁEngineǁ_assemble_trace__mutmut_49(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(None),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_50(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int(None),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_51(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) / 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_52(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() + started) * 1000),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

    def xǁEngineǁ_assemble_trace__mutmut_53(
        self,
        *,
        request_id: str,
        formation: str,
        dispatch: DispatchResult,
        dispatch_span: StageSpan,
        stage_spans: dict[str, StageSpan],
        answer_stage_id: str,
        started: float,
    ) -> DeliberationTrace:
        workers = [s for s in stage_spans.values() if s.kind == "worker"]
        primary_aggregator = next(
            (s for s in stage_spans.values() if s.kind in {"aggregator", "merge", "audit"}),
            None,
        )
        all_spans: list[StageSpan] = [dispatch_span, *stage_spans.values()]
        total_cost = round(sum(s.cost for s in all_spans), 6)
        total_tokens = sum(s.tokens_input + s.tokens_output for s in all_spans)
        return DeliberationTrace(
            request_id=request_id,
            formation=formation,
            source=dispatch.source,
            dispatch=dispatch_span,
            workers=workers,
            aggregator=primary_aggregator,
            stages=list(stage_spans.values()),
            answer_stage_id=answer_stage_id,
            total_duration_ms=int((time.monotonic() - started) * 1001),
            total_cost=total_cost,
            total_tokens=total_tokens,
        )

mutants_xǁEngineǁ__init____mutmut['_mutmut_orig'] = Engine.xǁEngineǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_1'] = Engine.xǁEngineǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_2'] = Engine.xǁEngineǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_3'] = Engine.xǁEngineǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_4'] = Engine.xǁEngineǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_5'] = Engine.xǁEngineǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_6'] = Engine.xǁEngineǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_7'] = Engine.xǁEngineǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_8'] = Engine.xǁEngineǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_9'] = Engine.xǁEngineǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_10'] = Engine.xǁEngineǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_11'] = Engine.xǁEngineǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_12'] = Engine.xǁEngineǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_13'] = Engine.xǁEngineǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_14'] = Engine.xǁEngineǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_15'] = Engine.xǁEngineǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_16'] = Engine.xǁEngineǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ__init____mutmut['xǁEngineǁ__init____mutmut_17'] = Engine.xǁEngineǁ__init____mutmut_17 # type: ignore # mutmut generated

mutants_xǁEngineǁ_compute_config_checksum__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_1'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_2'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_3'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_4'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_5'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_6'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_7'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_8'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_9'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_10'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_11'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_12'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_13'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_14'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_15'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_16'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_17'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_18'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_compute_config_checksum__mutmut['xǁEngineǁ_compute_config_checksum__mutmut_19'] = Engine.xǁEngineǁ_compute_config_checksum__mutmut_19 # type: ignore # mutmut generated

mutants_xǁEngineǁ_check_config_mutation__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_1'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_2'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_3'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_4'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_5'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_6'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_7'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_8'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_9'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_10'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_11'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_12'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_13'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_14'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_15'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_16'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_17'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_18'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_19'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_20'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_check_config_mutation__mutmut['xǁEngineǁ_check_config_mutation__mutmut_21'] = Engine.xǁEngineǁ_check_config_mutation__mutmut_21 # type: ignore # mutmut generated

mutants_xǁEngineǁdeliberate__mutmut['_mutmut_orig'] = Engine.xǁEngineǁdeliberate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_1'] = Engine.xǁEngineǁdeliberate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_2'] = Engine.xǁEngineǁdeliberate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_3'] = Engine.xǁEngineǁdeliberate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_4'] = Engine.xǁEngineǁdeliberate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_5'] = Engine.xǁEngineǁdeliberate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_6'] = Engine.xǁEngineǁdeliberate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_7'] = Engine.xǁEngineǁdeliberate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_8'] = Engine.xǁEngineǁdeliberate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_9'] = Engine.xǁEngineǁdeliberate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_10'] = Engine.xǁEngineǁdeliberate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_11'] = Engine.xǁEngineǁdeliberate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_12'] = Engine.xǁEngineǁdeliberate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_13'] = Engine.xǁEngineǁdeliberate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_14'] = Engine.xǁEngineǁdeliberate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_15'] = Engine.xǁEngineǁdeliberate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_16'] = Engine.xǁEngineǁdeliberate__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_17'] = Engine.xǁEngineǁdeliberate__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_18'] = Engine.xǁEngineǁdeliberate__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_19'] = Engine.xǁEngineǁdeliberate__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_20'] = Engine.xǁEngineǁdeliberate__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_21'] = Engine.xǁEngineǁdeliberate__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_22'] = Engine.xǁEngineǁdeliberate__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_23'] = Engine.xǁEngineǁdeliberate__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_24'] = Engine.xǁEngineǁdeliberate__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_25'] = Engine.xǁEngineǁdeliberate__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_26'] = Engine.xǁEngineǁdeliberate__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_27'] = Engine.xǁEngineǁdeliberate__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_28'] = Engine.xǁEngineǁdeliberate__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_29'] = Engine.xǁEngineǁdeliberate__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_30'] = Engine.xǁEngineǁdeliberate__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_31'] = Engine.xǁEngineǁdeliberate__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_32'] = Engine.xǁEngineǁdeliberate__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_33'] = Engine.xǁEngineǁdeliberate__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_34'] = Engine.xǁEngineǁdeliberate__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_35'] = Engine.xǁEngineǁdeliberate__mutmut_35 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_36'] = Engine.xǁEngineǁdeliberate__mutmut_36 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_37'] = Engine.xǁEngineǁdeliberate__mutmut_37 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_38'] = Engine.xǁEngineǁdeliberate__mutmut_38 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_39'] = Engine.xǁEngineǁdeliberate__mutmut_39 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_40'] = Engine.xǁEngineǁdeliberate__mutmut_40 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_41'] = Engine.xǁEngineǁdeliberate__mutmut_41 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_42'] = Engine.xǁEngineǁdeliberate__mutmut_42 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_43'] = Engine.xǁEngineǁdeliberate__mutmut_43 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_44'] = Engine.xǁEngineǁdeliberate__mutmut_44 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_45'] = Engine.xǁEngineǁdeliberate__mutmut_45 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_46'] = Engine.xǁEngineǁdeliberate__mutmut_46 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_47'] = Engine.xǁEngineǁdeliberate__mutmut_47 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_48'] = Engine.xǁEngineǁdeliberate__mutmut_48 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_49'] = Engine.xǁEngineǁdeliberate__mutmut_49 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_50'] = Engine.xǁEngineǁdeliberate__mutmut_50 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_51'] = Engine.xǁEngineǁdeliberate__mutmut_51 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_52'] = Engine.xǁEngineǁdeliberate__mutmut_52 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_53'] = Engine.xǁEngineǁdeliberate__mutmut_53 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_54'] = Engine.xǁEngineǁdeliberate__mutmut_54 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_55'] = Engine.xǁEngineǁdeliberate__mutmut_55 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_56'] = Engine.xǁEngineǁdeliberate__mutmut_56 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_57'] = Engine.xǁEngineǁdeliberate__mutmut_57 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_58'] = Engine.xǁEngineǁdeliberate__mutmut_58 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_59'] = Engine.xǁEngineǁdeliberate__mutmut_59 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_60'] = Engine.xǁEngineǁdeliberate__mutmut_60 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_61'] = Engine.xǁEngineǁdeliberate__mutmut_61 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_62'] = Engine.xǁEngineǁdeliberate__mutmut_62 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_63'] = Engine.xǁEngineǁdeliberate__mutmut_63 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_64'] = Engine.xǁEngineǁdeliberate__mutmut_64 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_65'] = Engine.xǁEngineǁdeliberate__mutmut_65 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_66'] = Engine.xǁEngineǁdeliberate__mutmut_66 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_67'] = Engine.xǁEngineǁdeliberate__mutmut_67 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_68'] = Engine.xǁEngineǁdeliberate__mutmut_68 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_69'] = Engine.xǁEngineǁdeliberate__mutmut_69 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_70'] = Engine.xǁEngineǁdeliberate__mutmut_70 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_71'] = Engine.xǁEngineǁdeliberate__mutmut_71 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_72'] = Engine.xǁEngineǁdeliberate__mutmut_72 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_73'] = Engine.xǁEngineǁdeliberate__mutmut_73 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_74'] = Engine.xǁEngineǁdeliberate__mutmut_74 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_75'] = Engine.xǁEngineǁdeliberate__mutmut_75 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_76'] = Engine.xǁEngineǁdeliberate__mutmut_76 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_77'] = Engine.xǁEngineǁdeliberate__mutmut_77 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_78'] = Engine.xǁEngineǁdeliberate__mutmut_78 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_79'] = Engine.xǁEngineǁdeliberate__mutmut_79 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_80'] = Engine.xǁEngineǁdeliberate__mutmut_80 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_81'] = Engine.xǁEngineǁdeliberate__mutmut_81 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_82'] = Engine.xǁEngineǁdeliberate__mutmut_82 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_83'] = Engine.xǁEngineǁdeliberate__mutmut_83 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_84'] = Engine.xǁEngineǁdeliberate__mutmut_84 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_85'] = Engine.xǁEngineǁdeliberate__mutmut_85 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_86'] = Engine.xǁEngineǁdeliberate__mutmut_86 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_87'] = Engine.xǁEngineǁdeliberate__mutmut_87 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_88'] = Engine.xǁEngineǁdeliberate__mutmut_88 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_89'] = Engine.xǁEngineǁdeliberate__mutmut_89 # type: ignore # mutmut generated
mutants_xǁEngineǁdeliberate__mutmut['xǁEngineǁdeliberate__mutmut_90'] = Engine.xǁEngineǁdeliberate__mutmut_90 # type: ignore # mutmut generated

mutants_xǁEngineǁ_resolve_custom_dag__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_1'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_2'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_3'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_4'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_5'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_6'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_7'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_8'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_9'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_resolve_custom_dag__mutmut['xǁEngineǁ_resolve_custom_dag__mutmut_10'] = Engine.xǁEngineǁ_resolve_custom_dag__mutmut_10 # type: ignore # mutmut generated

mutants_xǁEngineǁ_run_dag__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_run_dag__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_1'] = Engine.xǁEngineǁ_run_dag__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_2'] = Engine.xǁEngineǁ_run_dag__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_3'] = Engine.xǁEngineǁ_run_dag__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_4'] = Engine.xǁEngineǁ_run_dag__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_5'] = Engine.xǁEngineǁ_run_dag__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_6'] = Engine.xǁEngineǁ_run_dag__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_7'] = Engine.xǁEngineǁ_run_dag__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_8'] = Engine.xǁEngineǁ_run_dag__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_9'] = Engine.xǁEngineǁ_run_dag__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_10'] = Engine.xǁEngineǁ_run_dag__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_11'] = Engine.xǁEngineǁ_run_dag__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_12'] = Engine.xǁEngineǁ_run_dag__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_13'] = Engine.xǁEngineǁ_run_dag__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_14'] = Engine.xǁEngineǁ_run_dag__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_15'] = Engine.xǁEngineǁ_run_dag__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_16'] = Engine.xǁEngineǁ_run_dag__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_17'] = Engine.xǁEngineǁ_run_dag__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_18'] = Engine.xǁEngineǁ_run_dag__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_19'] = Engine.xǁEngineǁ_run_dag__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_20'] = Engine.xǁEngineǁ_run_dag__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_21'] = Engine.xǁEngineǁ_run_dag__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_22'] = Engine.xǁEngineǁ_run_dag__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_23'] = Engine.xǁEngineǁ_run_dag__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_24'] = Engine.xǁEngineǁ_run_dag__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_25'] = Engine.xǁEngineǁ_run_dag__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_26'] = Engine.xǁEngineǁ_run_dag__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_27'] = Engine.xǁEngineǁ_run_dag__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_28'] = Engine.xǁEngineǁ_run_dag__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_29'] = Engine.xǁEngineǁ_run_dag__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_30'] = Engine.xǁEngineǁ_run_dag__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_31'] = Engine.xǁEngineǁ_run_dag__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_32'] = Engine.xǁEngineǁ_run_dag__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_33'] = Engine.xǁEngineǁ_run_dag__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_34'] = Engine.xǁEngineǁ_run_dag__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_35'] = Engine.xǁEngineǁ_run_dag__mutmut_35 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_36'] = Engine.xǁEngineǁ_run_dag__mutmut_36 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_37'] = Engine.xǁEngineǁ_run_dag__mutmut_37 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_38'] = Engine.xǁEngineǁ_run_dag__mutmut_38 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_39'] = Engine.xǁEngineǁ_run_dag__mutmut_39 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_40'] = Engine.xǁEngineǁ_run_dag__mutmut_40 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_41'] = Engine.xǁEngineǁ_run_dag__mutmut_41 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_42'] = Engine.xǁEngineǁ_run_dag__mutmut_42 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_43'] = Engine.xǁEngineǁ_run_dag__mutmut_43 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_44'] = Engine.xǁEngineǁ_run_dag__mutmut_44 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_45'] = Engine.xǁEngineǁ_run_dag__mutmut_45 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_46'] = Engine.xǁEngineǁ_run_dag__mutmut_46 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_47'] = Engine.xǁEngineǁ_run_dag__mutmut_47 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_48'] = Engine.xǁEngineǁ_run_dag__mutmut_48 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_49'] = Engine.xǁEngineǁ_run_dag__mutmut_49 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_50'] = Engine.xǁEngineǁ_run_dag__mutmut_50 # type: ignore # mutmut generated
mutants_xǁEngineǁ_run_dag__mutmut['xǁEngineǁ_run_dag__mutmut_51'] = Engine.xǁEngineǁ_run_dag__mutmut_51 # type: ignore # mutmut generated

mutants_xǁEngineǁ_execute_stage__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_execute_stage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_1'] = Engine.xǁEngineǁ_execute_stage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_2'] = Engine.xǁEngineǁ_execute_stage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_3'] = Engine.xǁEngineǁ_execute_stage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_4'] = Engine.xǁEngineǁ_execute_stage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_5'] = Engine.xǁEngineǁ_execute_stage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_6'] = Engine.xǁEngineǁ_execute_stage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_7'] = Engine.xǁEngineǁ_execute_stage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_8'] = Engine.xǁEngineǁ_execute_stage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_9'] = Engine.xǁEngineǁ_execute_stage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_10'] = Engine.xǁEngineǁ_execute_stage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_11'] = Engine.xǁEngineǁ_execute_stage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_12'] = Engine.xǁEngineǁ_execute_stage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_13'] = Engine.xǁEngineǁ_execute_stage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_14'] = Engine.xǁEngineǁ_execute_stage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_15'] = Engine.xǁEngineǁ_execute_stage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_16'] = Engine.xǁEngineǁ_execute_stage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_17'] = Engine.xǁEngineǁ_execute_stage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_18'] = Engine.xǁEngineǁ_execute_stage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_19'] = Engine.xǁEngineǁ_execute_stage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_20'] = Engine.xǁEngineǁ_execute_stage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_21'] = Engine.xǁEngineǁ_execute_stage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_22'] = Engine.xǁEngineǁ_execute_stage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_23'] = Engine.xǁEngineǁ_execute_stage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_24'] = Engine.xǁEngineǁ_execute_stage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_25'] = Engine.xǁEngineǁ_execute_stage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_26'] = Engine.xǁEngineǁ_execute_stage__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_27'] = Engine.xǁEngineǁ_execute_stage__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_28'] = Engine.xǁEngineǁ_execute_stage__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_29'] = Engine.xǁEngineǁ_execute_stage__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_30'] = Engine.xǁEngineǁ_execute_stage__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_31'] = Engine.xǁEngineǁ_execute_stage__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_32'] = Engine.xǁEngineǁ_execute_stage__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_33'] = Engine.xǁEngineǁ_execute_stage__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_34'] = Engine.xǁEngineǁ_execute_stage__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_35'] = Engine.xǁEngineǁ_execute_stage__mutmut_35 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_36'] = Engine.xǁEngineǁ_execute_stage__mutmut_36 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_37'] = Engine.xǁEngineǁ_execute_stage__mutmut_37 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_38'] = Engine.xǁEngineǁ_execute_stage__mutmut_38 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_39'] = Engine.xǁEngineǁ_execute_stage__mutmut_39 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_40'] = Engine.xǁEngineǁ_execute_stage__mutmut_40 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_41'] = Engine.xǁEngineǁ_execute_stage__mutmut_41 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_42'] = Engine.xǁEngineǁ_execute_stage__mutmut_42 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_43'] = Engine.xǁEngineǁ_execute_stage__mutmut_43 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_44'] = Engine.xǁEngineǁ_execute_stage__mutmut_44 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_45'] = Engine.xǁEngineǁ_execute_stage__mutmut_45 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_46'] = Engine.xǁEngineǁ_execute_stage__mutmut_46 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_47'] = Engine.xǁEngineǁ_execute_stage__mutmut_47 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_48'] = Engine.xǁEngineǁ_execute_stage__mutmut_48 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_49'] = Engine.xǁEngineǁ_execute_stage__mutmut_49 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_50'] = Engine.xǁEngineǁ_execute_stage__mutmut_50 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_51'] = Engine.xǁEngineǁ_execute_stage__mutmut_51 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_52'] = Engine.xǁEngineǁ_execute_stage__mutmut_52 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_53'] = Engine.xǁEngineǁ_execute_stage__mutmut_53 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_54'] = Engine.xǁEngineǁ_execute_stage__mutmut_54 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_55'] = Engine.xǁEngineǁ_execute_stage__mutmut_55 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_56'] = Engine.xǁEngineǁ_execute_stage__mutmut_56 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_57'] = Engine.xǁEngineǁ_execute_stage__mutmut_57 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_58'] = Engine.xǁEngineǁ_execute_stage__mutmut_58 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_59'] = Engine.xǁEngineǁ_execute_stage__mutmut_59 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_60'] = Engine.xǁEngineǁ_execute_stage__mutmut_60 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_61'] = Engine.xǁEngineǁ_execute_stage__mutmut_61 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_62'] = Engine.xǁEngineǁ_execute_stage__mutmut_62 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_63'] = Engine.xǁEngineǁ_execute_stage__mutmut_63 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_64'] = Engine.xǁEngineǁ_execute_stage__mutmut_64 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_65'] = Engine.xǁEngineǁ_execute_stage__mutmut_65 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_66'] = Engine.xǁEngineǁ_execute_stage__mutmut_66 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_67'] = Engine.xǁEngineǁ_execute_stage__mutmut_67 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_68'] = Engine.xǁEngineǁ_execute_stage__mutmut_68 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_69'] = Engine.xǁEngineǁ_execute_stage__mutmut_69 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_70'] = Engine.xǁEngineǁ_execute_stage__mutmut_70 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_71'] = Engine.xǁEngineǁ_execute_stage__mutmut_71 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_72'] = Engine.xǁEngineǁ_execute_stage__mutmut_72 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_73'] = Engine.xǁEngineǁ_execute_stage__mutmut_73 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_74'] = Engine.xǁEngineǁ_execute_stage__mutmut_74 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_75'] = Engine.xǁEngineǁ_execute_stage__mutmut_75 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_76'] = Engine.xǁEngineǁ_execute_stage__mutmut_76 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_77'] = Engine.xǁEngineǁ_execute_stage__mutmut_77 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_78'] = Engine.xǁEngineǁ_execute_stage__mutmut_78 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_79'] = Engine.xǁEngineǁ_execute_stage__mutmut_79 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_80'] = Engine.xǁEngineǁ_execute_stage__mutmut_80 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_81'] = Engine.xǁEngineǁ_execute_stage__mutmut_81 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_82'] = Engine.xǁEngineǁ_execute_stage__mutmut_82 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_83'] = Engine.xǁEngineǁ_execute_stage__mutmut_83 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_84'] = Engine.xǁEngineǁ_execute_stage__mutmut_84 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_85'] = Engine.xǁEngineǁ_execute_stage__mutmut_85 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_86'] = Engine.xǁEngineǁ_execute_stage__mutmut_86 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_87'] = Engine.xǁEngineǁ_execute_stage__mutmut_87 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_88'] = Engine.xǁEngineǁ_execute_stage__mutmut_88 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_89'] = Engine.xǁEngineǁ_execute_stage__mutmut_89 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_90'] = Engine.xǁEngineǁ_execute_stage__mutmut_90 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_91'] = Engine.xǁEngineǁ_execute_stage__mutmut_91 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_92'] = Engine.xǁEngineǁ_execute_stage__mutmut_92 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_93'] = Engine.xǁEngineǁ_execute_stage__mutmut_93 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_94'] = Engine.xǁEngineǁ_execute_stage__mutmut_94 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_95'] = Engine.xǁEngineǁ_execute_stage__mutmut_95 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_96'] = Engine.xǁEngineǁ_execute_stage__mutmut_96 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_97'] = Engine.xǁEngineǁ_execute_stage__mutmut_97 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_98'] = Engine.xǁEngineǁ_execute_stage__mutmut_98 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_99'] = Engine.xǁEngineǁ_execute_stage__mutmut_99 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_100'] = Engine.xǁEngineǁ_execute_stage__mutmut_100 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_101'] = Engine.xǁEngineǁ_execute_stage__mutmut_101 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_102'] = Engine.xǁEngineǁ_execute_stage__mutmut_102 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_103'] = Engine.xǁEngineǁ_execute_stage__mutmut_103 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_104'] = Engine.xǁEngineǁ_execute_stage__mutmut_104 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_105'] = Engine.xǁEngineǁ_execute_stage__mutmut_105 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_106'] = Engine.xǁEngineǁ_execute_stage__mutmut_106 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_107'] = Engine.xǁEngineǁ_execute_stage__mutmut_107 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_108'] = Engine.xǁEngineǁ_execute_stage__mutmut_108 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_109'] = Engine.xǁEngineǁ_execute_stage__mutmut_109 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_110'] = Engine.xǁEngineǁ_execute_stage__mutmut_110 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_111'] = Engine.xǁEngineǁ_execute_stage__mutmut_111 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_112'] = Engine.xǁEngineǁ_execute_stage__mutmut_112 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_113'] = Engine.xǁEngineǁ_execute_stage__mutmut_113 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_114'] = Engine.xǁEngineǁ_execute_stage__mutmut_114 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_115'] = Engine.xǁEngineǁ_execute_stage__mutmut_115 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_116'] = Engine.xǁEngineǁ_execute_stage__mutmut_116 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_117'] = Engine.xǁEngineǁ_execute_stage__mutmut_117 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_118'] = Engine.xǁEngineǁ_execute_stage__mutmut_118 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_119'] = Engine.xǁEngineǁ_execute_stage__mutmut_119 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_120'] = Engine.xǁEngineǁ_execute_stage__mutmut_120 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_121'] = Engine.xǁEngineǁ_execute_stage__mutmut_121 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_122'] = Engine.xǁEngineǁ_execute_stage__mutmut_122 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_123'] = Engine.xǁEngineǁ_execute_stage__mutmut_123 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_124'] = Engine.xǁEngineǁ_execute_stage__mutmut_124 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_125'] = Engine.xǁEngineǁ_execute_stage__mutmut_125 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_126'] = Engine.xǁEngineǁ_execute_stage__mutmut_126 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_127'] = Engine.xǁEngineǁ_execute_stage__mutmut_127 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_128'] = Engine.xǁEngineǁ_execute_stage__mutmut_128 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_129'] = Engine.xǁEngineǁ_execute_stage__mutmut_129 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_130'] = Engine.xǁEngineǁ_execute_stage__mutmut_130 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_131'] = Engine.xǁEngineǁ_execute_stage__mutmut_131 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_132'] = Engine.xǁEngineǁ_execute_stage__mutmut_132 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_133'] = Engine.xǁEngineǁ_execute_stage__mutmut_133 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_134'] = Engine.xǁEngineǁ_execute_stage__mutmut_134 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_135'] = Engine.xǁEngineǁ_execute_stage__mutmut_135 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_136'] = Engine.xǁEngineǁ_execute_stage__mutmut_136 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_137'] = Engine.xǁEngineǁ_execute_stage__mutmut_137 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_138'] = Engine.xǁEngineǁ_execute_stage__mutmut_138 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_139'] = Engine.xǁEngineǁ_execute_stage__mutmut_139 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_140'] = Engine.xǁEngineǁ_execute_stage__mutmut_140 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_141'] = Engine.xǁEngineǁ_execute_stage__mutmut_141 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_142'] = Engine.xǁEngineǁ_execute_stage__mutmut_142 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_143'] = Engine.xǁEngineǁ_execute_stage__mutmut_143 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_144'] = Engine.xǁEngineǁ_execute_stage__mutmut_144 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_145'] = Engine.xǁEngineǁ_execute_stage__mutmut_145 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_146'] = Engine.xǁEngineǁ_execute_stage__mutmut_146 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_147'] = Engine.xǁEngineǁ_execute_stage__mutmut_147 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_148'] = Engine.xǁEngineǁ_execute_stage__mutmut_148 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_149'] = Engine.xǁEngineǁ_execute_stage__mutmut_149 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_150'] = Engine.xǁEngineǁ_execute_stage__mutmut_150 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_151'] = Engine.xǁEngineǁ_execute_stage__mutmut_151 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_152'] = Engine.xǁEngineǁ_execute_stage__mutmut_152 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_153'] = Engine.xǁEngineǁ_execute_stage__mutmut_153 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_154'] = Engine.xǁEngineǁ_execute_stage__mutmut_154 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_155'] = Engine.xǁEngineǁ_execute_stage__mutmut_155 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_156'] = Engine.xǁEngineǁ_execute_stage__mutmut_156 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_157'] = Engine.xǁEngineǁ_execute_stage__mutmut_157 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_158'] = Engine.xǁEngineǁ_execute_stage__mutmut_158 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_159'] = Engine.xǁEngineǁ_execute_stage__mutmut_159 # type: ignore # mutmut generated
mutants_xǁEngineǁ_execute_stage__mutmut['xǁEngineǁ_execute_stage__mutmut_160'] = Engine.xǁEngineǁ_execute_stage__mutmut_160 # type: ignore # mutmut generated

mutants_xǁEngineǁ_call_stage__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_call_stage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_1'] = Engine.xǁEngineǁ_call_stage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_2'] = Engine.xǁEngineǁ_call_stage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_3'] = Engine.xǁEngineǁ_call_stage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_4'] = Engine.xǁEngineǁ_call_stage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_5'] = Engine.xǁEngineǁ_call_stage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_6'] = Engine.xǁEngineǁ_call_stage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_7'] = Engine.xǁEngineǁ_call_stage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_8'] = Engine.xǁEngineǁ_call_stage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_9'] = Engine.xǁEngineǁ_call_stage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_10'] = Engine.xǁEngineǁ_call_stage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_11'] = Engine.xǁEngineǁ_call_stage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_12'] = Engine.xǁEngineǁ_call_stage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_13'] = Engine.xǁEngineǁ_call_stage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_14'] = Engine.xǁEngineǁ_call_stage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_15'] = Engine.xǁEngineǁ_call_stage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_16'] = Engine.xǁEngineǁ_call_stage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_17'] = Engine.xǁEngineǁ_call_stage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_18'] = Engine.xǁEngineǁ_call_stage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_19'] = Engine.xǁEngineǁ_call_stage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_20'] = Engine.xǁEngineǁ_call_stage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_21'] = Engine.xǁEngineǁ_call_stage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_22'] = Engine.xǁEngineǁ_call_stage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_23'] = Engine.xǁEngineǁ_call_stage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_24'] = Engine.xǁEngineǁ_call_stage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_25'] = Engine.xǁEngineǁ_call_stage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_26'] = Engine.xǁEngineǁ_call_stage__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_27'] = Engine.xǁEngineǁ_call_stage__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_28'] = Engine.xǁEngineǁ_call_stage__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_call_stage__mutmut['xǁEngineǁ_call_stage__mutmut_29'] = Engine.xǁEngineǁ_call_stage__mutmut_29 # type: ignore # mutmut generated

mutants_xǁEngineǁ_degraded_stage__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_degraded_stage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_1'] = Engine.xǁEngineǁ_degraded_stage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_2'] = Engine.xǁEngineǁ_degraded_stage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_3'] = Engine.xǁEngineǁ_degraded_stage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_4'] = Engine.xǁEngineǁ_degraded_stage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_5'] = Engine.xǁEngineǁ_degraded_stage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_6'] = Engine.xǁEngineǁ_degraded_stage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_7'] = Engine.xǁEngineǁ_degraded_stage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_8'] = Engine.xǁEngineǁ_degraded_stage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_9'] = Engine.xǁEngineǁ_degraded_stage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_10'] = Engine.xǁEngineǁ_degraded_stage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_11'] = Engine.xǁEngineǁ_degraded_stage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_12'] = Engine.xǁEngineǁ_degraded_stage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_13'] = Engine.xǁEngineǁ_degraded_stage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_14'] = Engine.xǁEngineǁ_degraded_stage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_15'] = Engine.xǁEngineǁ_degraded_stage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_16'] = Engine.xǁEngineǁ_degraded_stage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_17'] = Engine.xǁEngineǁ_degraded_stage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_18'] = Engine.xǁEngineǁ_degraded_stage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_19'] = Engine.xǁEngineǁ_degraded_stage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_20'] = Engine.xǁEngineǁ_degraded_stage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_21'] = Engine.xǁEngineǁ_degraded_stage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_22'] = Engine.xǁEngineǁ_degraded_stage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_23'] = Engine.xǁEngineǁ_degraded_stage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_24'] = Engine.xǁEngineǁ_degraded_stage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_25'] = Engine.xǁEngineǁ_degraded_stage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_26'] = Engine.xǁEngineǁ_degraded_stage__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_27'] = Engine.xǁEngineǁ_degraded_stage__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_28'] = Engine.xǁEngineǁ_degraded_stage__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_29'] = Engine.xǁEngineǁ_degraded_stage__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_30'] = Engine.xǁEngineǁ_degraded_stage__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_31'] = Engine.xǁEngineǁ_degraded_stage__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_32'] = Engine.xǁEngineǁ_degraded_stage__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_33'] = Engine.xǁEngineǁ_degraded_stage__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_34'] = Engine.xǁEngineǁ_degraded_stage__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_35'] = Engine.xǁEngineǁ_degraded_stage__mutmut_35 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_36'] = Engine.xǁEngineǁ_degraded_stage__mutmut_36 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_37'] = Engine.xǁEngineǁ_degraded_stage__mutmut_37 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_38'] = Engine.xǁEngineǁ_degraded_stage__mutmut_38 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_39'] = Engine.xǁEngineǁ_degraded_stage__mutmut_39 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_40'] = Engine.xǁEngineǁ_degraded_stage__mutmut_40 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_41'] = Engine.xǁEngineǁ_degraded_stage__mutmut_41 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_42'] = Engine.xǁEngineǁ_degraded_stage__mutmut_42 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_43'] = Engine.xǁEngineǁ_degraded_stage__mutmut_43 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_44'] = Engine.xǁEngineǁ_degraded_stage__mutmut_44 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_45'] = Engine.xǁEngineǁ_degraded_stage__mutmut_45 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_46'] = Engine.xǁEngineǁ_degraded_stage__mutmut_46 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_47'] = Engine.xǁEngineǁ_degraded_stage__mutmut_47 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_48'] = Engine.xǁEngineǁ_degraded_stage__mutmut_48 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_49'] = Engine.xǁEngineǁ_degraded_stage__mutmut_49 # type: ignore # mutmut generated
mutants_xǁEngineǁ_degraded_stage__mutmut['xǁEngineǁ_degraded_stage__mutmut_50'] = Engine.xǁEngineǁ_degraded_stage__mutmut_50 # type: ignore # mutmut generated

mutants_xǁEngineǁ_worker_messages__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_worker_messages__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_1'] = Engine.xǁEngineǁ_worker_messages__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_2'] = Engine.xǁEngineǁ_worker_messages__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_3'] = Engine.xǁEngineǁ_worker_messages__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_4'] = Engine.xǁEngineǁ_worker_messages__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_5'] = Engine.xǁEngineǁ_worker_messages__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_6'] = Engine.xǁEngineǁ_worker_messages__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_7'] = Engine.xǁEngineǁ_worker_messages__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_8'] = Engine.xǁEngineǁ_worker_messages__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_9'] = Engine.xǁEngineǁ_worker_messages__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_10'] = Engine.xǁEngineǁ_worker_messages__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_11'] = Engine.xǁEngineǁ_worker_messages__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_12'] = Engine.xǁEngineǁ_worker_messages__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_worker_messages__mutmut['xǁEngineǁ_worker_messages__mutmut_13'] = Engine.xǁEngineǁ_worker_messages__mutmut_13 # type: ignore # mutmut generated

mutants_xǁEngineǁ_build_dispatch_span__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_1'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_2'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_3'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_4'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_5'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_6'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_7'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_8'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_9'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_10'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_11'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_12'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_13'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_14'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_15'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_16'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_17'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_18'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_19'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_20'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_21'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_22'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_23'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_24'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_25'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_26'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_27'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_28'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_29'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_30'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_31'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_32'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_33'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_34'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁ_build_dispatch_span__mutmut['xǁEngineǁ_build_dispatch_span__mutmut_35'] = Engine.xǁEngineǁ_build_dispatch_span__mutmut_35 # type: ignore # mutmut generated

mutants_xǁEngineǁ_select_answer__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_select_answer__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_1'] = Engine.xǁEngineǁ_select_answer__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_2'] = Engine.xǁEngineǁ_select_answer__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_3'] = Engine.xǁEngineǁ_select_answer__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_4'] = Engine.xǁEngineǁ_select_answer__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_5'] = Engine.xǁEngineǁ_select_answer__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_6'] = Engine.xǁEngineǁ_select_answer__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_7'] = Engine.xǁEngineǁ_select_answer__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_8'] = Engine.xǁEngineǁ_select_answer__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_9'] = Engine.xǁEngineǁ_select_answer__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_10'] = Engine.xǁEngineǁ_select_answer__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_11'] = Engine.xǁEngineǁ_select_answer__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_12'] = Engine.xǁEngineǁ_select_answer__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_13'] = Engine.xǁEngineǁ_select_answer__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_select_answer__mutmut['xǁEngineǁ_select_answer__mutmut_14'] = Engine.xǁEngineǁ_select_answer__mutmut_14 # type: ignore # mutmut generated

mutants_xǁEngineǁ_assemble_trace__mutmut['_mutmut_orig'] = Engine.xǁEngineǁ_assemble_trace__mutmut_orig # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_1'] = Engine.xǁEngineǁ_assemble_trace__mutmut_1 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_2'] = Engine.xǁEngineǁ_assemble_trace__mutmut_2 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_3'] = Engine.xǁEngineǁ_assemble_trace__mutmut_3 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_4'] = Engine.xǁEngineǁ_assemble_trace__mutmut_4 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_5'] = Engine.xǁEngineǁ_assemble_trace__mutmut_5 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_6'] = Engine.xǁEngineǁ_assemble_trace__mutmut_6 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_7'] = Engine.xǁEngineǁ_assemble_trace__mutmut_7 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_8'] = Engine.xǁEngineǁ_assemble_trace__mutmut_8 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_9'] = Engine.xǁEngineǁ_assemble_trace__mutmut_9 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_10'] = Engine.xǁEngineǁ_assemble_trace__mutmut_10 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_11'] = Engine.xǁEngineǁ_assemble_trace__mutmut_11 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_12'] = Engine.xǁEngineǁ_assemble_trace__mutmut_12 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_13'] = Engine.xǁEngineǁ_assemble_trace__mutmut_13 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_14'] = Engine.xǁEngineǁ_assemble_trace__mutmut_14 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_15'] = Engine.xǁEngineǁ_assemble_trace__mutmut_15 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_16'] = Engine.xǁEngineǁ_assemble_trace__mutmut_16 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_17'] = Engine.xǁEngineǁ_assemble_trace__mutmut_17 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_18'] = Engine.xǁEngineǁ_assemble_trace__mutmut_18 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_19'] = Engine.xǁEngineǁ_assemble_trace__mutmut_19 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_20'] = Engine.xǁEngineǁ_assemble_trace__mutmut_20 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_21'] = Engine.xǁEngineǁ_assemble_trace__mutmut_21 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_22'] = Engine.xǁEngineǁ_assemble_trace__mutmut_22 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_23'] = Engine.xǁEngineǁ_assemble_trace__mutmut_23 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_24'] = Engine.xǁEngineǁ_assemble_trace__mutmut_24 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_25'] = Engine.xǁEngineǁ_assemble_trace__mutmut_25 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_26'] = Engine.xǁEngineǁ_assemble_trace__mutmut_26 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_27'] = Engine.xǁEngineǁ_assemble_trace__mutmut_27 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_28'] = Engine.xǁEngineǁ_assemble_trace__mutmut_28 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_29'] = Engine.xǁEngineǁ_assemble_trace__mutmut_29 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_30'] = Engine.xǁEngineǁ_assemble_trace__mutmut_30 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_31'] = Engine.xǁEngineǁ_assemble_trace__mutmut_31 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_32'] = Engine.xǁEngineǁ_assemble_trace__mutmut_32 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_33'] = Engine.xǁEngineǁ_assemble_trace__mutmut_33 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_34'] = Engine.xǁEngineǁ_assemble_trace__mutmut_34 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_35'] = Engine.xǁEngineǁ_assemble_trace__mutmut_35 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_36'] = Engine.xǁEngineǁ_assemble_trace__mutmut_36 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_37'] = Engine.xǁEngineǁ_assemble_trace__mutmut_37 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_38'] = Engine.xǁEngineǁ_assemble_trace__mutmut_38 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_39'] = Engine.xǁEngineǁ_assemble_trace__mutmut_39 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_40'] = Engine.xǁEngineǁ_assemble_trace__mutmut_40 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_41'] = Engine.xǁEngineǁ_assemble_trace__mutmut_41 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_42'] = Engine.xǁEngineǁ_assemble_trace__mutmut_42 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_43'] = Engine.xǁEngineǁ_assemble_trace__mutmut_43 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_44'] = Engine.xǁEngineǁ_assemble_trace__mutmut_44 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_45'] = Engine.xǁEngineǁ_assemble_trace__mutmut_45 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_46'] = Engine.xǁEngineǁ_assemble_trace__mutmut_46 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_47'] = Engine.xǁEngineǁ_assemble_trace__mutmut_47 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_48'] = Engine.xǁEngineǁ_assemble_trace__mutmut_48 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_49'] = Engine.xǁEngineǁ_assemble_trace__mutmut_49 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_50'] = Engine.xǁEngineǁ_assemble_trace__mutmut_50 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_51'] = Engine.xǁEngineǁ_assemble_trace__mutmut_51 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_52'] = Engine.xǁEngineǁ_assemble_trace__mutmut_52 # type: ignore # mutmut generated
mutants_xǁEngineǁ_assemble_trace__mutmut['xǁEngineǁ_assemble_trace__mutmut_53'] = Engine.xǁEngineǁ_assemble_trace__mutmut_53 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__aggregator_prompt_summary__mutmut)
def _aggregator_prompt_summary(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(d.stage_id for d in deps) or "(none)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_orig(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(d.stage_id for d in deps) or "(none)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_1(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = None
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_2(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(d.stage_id for d in deps) and "(none)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_3(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(None) or "(none)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_4(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = "XX, XX".join(d.stage_id for d in deps) or "(none)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_5(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(d.stage_id for d in deps) or "XX(none)XX"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"


def x__aggregator_prompt_summary__mutmut_6(
    stage: Stage, dispatch: DispatchResult, deps: list[StageResult]
) -> str:
    """Compact prompt string for aggregator spans (whose messages are built internally)."""
    deps_desc = ", ".join(d.stage_id for d in deps) or "(NONE)"
    return f"merge stage '{stage.id}' over upstream: {deps_desc}"

mutants_x__aggregator_prompt_summary__mutmut['_mutmut_orig'] = x__aggregator_prompt_summary__mutmut_orig # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_1'] = x__aggregator_prompt_summary__mutmut_1 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_2'] = x__aggregator_prompt_summary__mutmut_2 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_3'] = x__aggregator_prompt_summary__mutmut_3 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_4'] = x__aggregator_prompt_summary__mutmut_4 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_5'] = x__aggregator_prompt_summary__mutmut_5 # type: ignore # mutmut generated
mutants_x__aggregator_prompt_summary__mutmut['x__aggregator_prompt_summary__mutmut_6'] = x__aggregator_prompt_summary__mutmut_6 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__maybe_langfuse__mutmut)
def _maybe_langfuse(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_orig(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_1(trace: DeliberationTrace, user_prompt: str) -> None:
    client = None
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_2(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is not None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_3(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = None
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_4(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=None, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_5(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name=None)
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_6(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_7(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, )
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_8(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="XXchimera.deliberationXX")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_9(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="CHIMERA.DELIBERATION")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_10(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name=None,
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_11(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=None,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_12(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=None,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_13(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=None,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_14(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage=None,
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_15(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_16(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_17(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_18(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_19(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_20(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="XXdispatchXX",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_21(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="DISPATCH",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_22(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "XXpromptXX": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_23(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "PROMPT": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_24(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "XXcompletionXX": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_25(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "COMPLETION": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_26(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=None,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_27(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=None,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_28(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=None,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_29(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=None,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_30(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage=None,
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_31(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_32(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_33(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_34(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_35(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_36(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"XXpromptXX": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_37(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"PROMPT": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_38(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "XXcompletionXX": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_39(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "COMPLETION": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(exc))


def x__maybe_langfuse__mutmut_40(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning(None, error=str(exc))


def x__maybe_langfuse__mutmut_41(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=None)


def x__maybe_langfuse__mutmut_42(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning(error=str(exc))


def x__maybe_langfuse__mutmut_43(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", )


def x__maybe_langfuse__mutmut_44(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("XXlangfuse_trace_failedXX", error=str(exc))


def x__maybe_langfuse__mutmut_45(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("LANGFUSE_TRACE_FAILED", error=str(exc))


def x__maybe_langfuse__mutmut_46(trace: DeliberationTrace, user_prompt: str) -> None:
    client = get_langfuse()
    if client is None:
        return
    # Langfuse is best-effort telemetry: it must never break a deliberation.
    try:
        trace_obj = client.trace(id=trace.request_id, name="chimera.deliberation")
        trace_obj.generation(
            name="dispatch",
            model=trace.dispatch.model,
            input=user_prompt,
            output=trace.dispatch.response,
            usage={
                "prompt": trace.dispatch.tokens_input,
                "completion": trace.dispatch.tokens_output,
            },
        )
        for span in trace.stages:
            trace_obj.generation(
                name=span.stage_id,
                model=span.model,
                input=span.prompt,
                output=span.response,
                usage={"prompt": span.tokens_input, "completion": span.tokens_output},
            )
    except (
        ConnectionError,
        RuntimeError,
        ValueError,
        TypeError,
        KeyError,
        AttributeError,
    ) as exc:
        log.warning("langfuse_trace_failed", error=str(None))

mutants_x__maybe_langfuse__mutmut['_mutmut_orig'] = x__maybe_langfuse__mutmut_orig # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_1'] = x__maybe_langfuse__mutmut_1 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_2'] = x__maybe_langfuse__mutmut_2 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_3'] = x__maybe_langfuse__mutmut_3 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_4'] = x__maybe_langfuse__mutmut_4 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_5'] = x__maybe_langfuse__mutmut_5 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_6'] = x__maybe_langfuse__mutmut_6 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_7'] = x__maybe_langfuse__mutmut_7 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_8'] = x__maybe_langfuse__mutmut_8 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_9'] = x__maybe_langfuse__mutmut_9 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_10'] = x__maybe_langfuse__mutmut_10 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_11'] = x__maybe_langfuse__mutmut_11 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_12'] = x__maybe_langfuse__mutmut_12 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_13'] = x__maybe_langfuse__mutmut_13 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_14'] = x__maybe_langfuse__mutmut_14 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_15'] = x__maybe_langfuse__mutmut_15 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_16'] = x__maybe_langfuse__mutmut_16 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_17'] = x__maybe_langfuse__mutmut_17 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_18'] = x__maybe_langfuse__mutmut_18 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_19'] = x__maybe_langfuse__mutmut_19 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_20'] = x__maybe_langfuse__mutmut_20 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_21'] = x__maybe_langfuse__mutmut_21 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_22'] = x__maybe_langfuse__mutmut_22 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_23'] = x__maybe_langfuse__mutmut_23 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_24'] = x__maybe_langfuse__mutmut_24 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_25'] = x__maybe_langfuse__mutmut_25 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_26'] = x__maybe_langfuse__mutmut_26 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_27'] = x__maybe_langfuse__mutmut_27 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_28'] = x__maybe_langfuse__mutmut_28 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_29'] = x__maybe_langfuse__mutmut_29 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_30'] = x__maybe_langfuse__mutmut_30 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_31'] = x__maybe_langfuse__mutmut_31 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_32'] = x__maybe_langfuse__mutmut_32 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_33'] = x__maybe_langfuse__mutmut_33 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_34'] = x__maybe_langfuse__mutmut_34 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_35'] = x__maybe_langfuse__mutmut_35 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_36'] = x__maybe_langfuse__mutmut_36 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_37'] = x__maybe_langfuse__mutmut_37 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_38'] = x__maybe_langfuse__mutmut_38 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_39'] = x__maybe_langfuse__mutmut_39 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_40'] = x__maybe_langfuse__mutmut_40 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_41'] = x__maybe_langfuse__mutmut_41 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_42'] = x__maybe_langfuse__mutmut_42 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_43'] = x__maybe_langfuse__mutmut_43 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_44'] = x__maybe_langfuse__mutmut_44 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_45'] = x__maybe_langfuse__mutmut_45 # type: ignore # mutmut generated
mutants_x__maybe_langfuse__mutmut['x__maybe_langfuse__mutmut_46'] = x__maybe_langfuse__mutmut_46 # type: ignore # mutmut generated


__all__ = [
    "DeliberationResult",
    "DeliberationTrace",
    "Engine",
    "StageSpan",
]
