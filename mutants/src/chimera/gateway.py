"""Provider gateway — a uniform async interface over LiteLLM.

The :class:`Gateway` protocol lets the rest of Chimera stay provider-agnostic.
:class:`LiteLLMGateway` is the production implementation; tests inject fakes.

Resilience features (F5–F8 audit):
* F7 – Exponential backoff retry for transient failures (429/5xx/network).
* F6 – Provider-aware response_format negotiation (json_schema → json_object → text).
* C3 – Token limit detection (finish_reason="length").
* C4 – Empty/null response handling (is_empty flag).
* C7 – Budget exhaustion detection (BudgetExhaustedError).
"""

from __future__ import annotations

import asyncio
import os
from dataclasses import dataclass
from enum import Enum
from typing import Any, Protocol, runtime_checkable

import structlog

from chimera.config import ChimeraConfig, ModelEntry
from chimera.circuit_breaker import ProviderCircuitBreaker, fast_fail_response
from chimera.exceptions import BudgetExhaustedError

log = structlog.get_logger("chimera.gateway")


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


class GatewayError(Exception):
    """Raised when a provider call fails (auth, network, bad request, ...)."""


@dataclass(slots=True)
class GatewayResponse:
    """A normalized completion response."""

    text: str
    model: str
    tokens_input: int
    tokens_output: int
    raw: Any = None
    finish_reason: str = ""
    is_empty: bool = False

    @property
    def total_tokens(self) -> int:
        return self.tokens_input + self.tokens_output


@runtime_checkable
class Gateway(Protocol):
    """Anything that can complete a prompt asynchronously."""

    async def complete(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse: ...


# --------------------------------------------------------------------------- #
# F6: Provider-aware format negotiation
# --------------------------------------------------------------------------- #

class FormatCapability(Enum):
    """What level of structured output a provider supports."""
    JSON_SCHEMA = "json_schema"          # Full JSON Schema (openai, anthropic, google, zai)
    JSON_OBJECT = "json_object"           # Generic {} only (deepseek, moonshot)
    NONE = "none"                         # Plain text only


# Providers that support full json_schema
_JSON_SCHEMA_PROVIDERS: frozenset[str] = frozenset({
    "openai", "anthropic", "google", "zai",
})

# Providers that support json_object but not json_schema
_JSON_OBJECT_PROVIDERS: frozenset[str] = frozenset({
    "deepseek", "moonshot",
})
mutants_x__get_format_capability__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_format_capability__mutmut)
def _get_format_capability(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = provider.lower()
    if p in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE


def x__get_format_capability__mutmut_orig(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = provider.lower()
    if p in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE


def x__get_format_capability__mutmut_1(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = None
    if p in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE


def x__get_format_capability__mutmut_2(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = provider.upper()
    if p in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE


def x__get_format_capability__mutmut_3(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = provider.lower()
    if p not in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE


def x__get_format_capability__mutmut_4(provider: str) -> FormatCapability:
    """Return the structured-output capability level for a provider."""
    p = provider.lower()
    if p in _JSON_SCHEMA_PROVIDERS:
        return FormatCapability.JSON_SCHEMA
    if p not in _JSON_OBJECT_PROVIDERS:
        return FormatCapability.JSON_OBJECT
    return FormatCapability.NONE

mutants_x__get_format_capability__mutmut['_mutmut_orig'] = x__get_format_capability__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_format_capability__mutmut['x__get_format_capability__mutmut_1'] = x__get_format_capability__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_format_capability__mutmut['x__get_format_capability__mutmut_2'] = x__get_format_capability__mutmut_2 # type: ignore # mutmut generated
mutants_x__get_format_capability__mutmut['x__get_format_capability__mutmut_3'] = x__get_format_capability__mutmut_3 # type: ignore # mutmut generated
mutants_x__get_format_capability__mutmut['x__get_format_capability__mutmut_4'] = x__get_format_capability__mutmut_4 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_negotiate_response_format__mutmut)
def negotiate_response_format(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_orig(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_1(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is not None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_2(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = None
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_3(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(None)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_4(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = None

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_5(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get(None)

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_6(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("XXtypeXX")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_7(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("TYPE")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_8(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability != FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_9(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability != FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_10(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type != "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_11(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "XXjson_schemaXX":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_12(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "JSON_SCHEMA":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_13(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                None,
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_14(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format=None,
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_15(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format=None,
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_16(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=None,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_17(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_18(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_19(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_20(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_21(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "XXgateway_format_downgradeXX",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_22(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "GATEWAY_FORMAT_DOWNGRADE",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_23(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="XXjson_schemaXX",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_24(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="JSON_SCHEMA",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_25(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="XXjson_objectXX",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_26(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="JSON_OBJECT",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_27(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"XXtypeXX": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_28(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"TYPE": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_29(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "XXjson_objectXX"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_30(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "JSON_OBJECT"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_31(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type != "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_32(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "XXjson_objectXX":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_33(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "JSON_OBJECT":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_34(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"XXtypeXX": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_35(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"TYPE": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_36(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "XXjson_objectXX"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_37(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "JSON_OBJECT"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_38(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_39(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            None,
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_40(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=None,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_41(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            provider=None,
        )
    return None


def x_negotiate_response_format__mutmut_42(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_43(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_44(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "gateway_format_removed",
            requested_type=requested_type,
            )
    return None


def x_negotiate_response_format__mutmut_45(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "XXgateway_format_removedXX",
            requested_type=requested_type,
            provider=provider,
        )
    return None


def x_negotiate_response_format__mutmut_46(
    requested: dict[str, Any] | None,
    provider: str,
) -> dict[str, Any] | None:
    """Downgrade response_format to what the provider actually supports (F6).

    * json_schema → json_object for providers that don't support schema.
    * Any format → None for text-only providers.
    * Returns None if the provider can't handle any structured format.
    """
    if requested is None:
        return None

    capability = _get_format_capability(provider)
    requested_type = requested.get("type")

    if capability == FormatCapability.JSON_SCHEMA:
        return requested  # full support

    if capability == FormatCapability.JSON_OBJECT:
        if requested_type == "json_schema":
            # Downgrade: strip schema detail, keep json_object
            log.info(
                "gateway_format_downgrade",
                from_format="json_schema",
                to_format="json_object",
                provider=provider,
            )
            return {"type": "json_object"}
        if requested_type == "json_object":
            return requested
        # unknown type — try json_object as a safe bet
        return {"type": "json_object"}

    # capability == NONE
    if requested_type is not None:
        log.info(
            "GATEWAY_FORMAT_REMOVED",
            requested_type=requested_type,
            provider=provider,
        )
    return None

mutants_x_negotiate_response_format__mutmut['_mutmut_orig'] = x_negotiate_response_format__mutmut_orig # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_1'] = x_negotiate_response_format__mutmut_1 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_2'] = x_negotiate_response_format__mutmut_2 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_3'] = x_negotiate_response_format__mutmut_3 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_4'] = x_negotiate_response_format__mutmut_4 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_5'] = x_negotiate_response_format__mutmut_5 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_6'] = x_negotiate_response_format__mutmut_6 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_7'] = x_negotiate_response_format__mutmut_7 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_8'] = x_negotiate_response_format__mutmut_8 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_9'] = x_negotiate_response_format__mutmut_9 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_10'] = x_negotiate_response_format__mutmut_10 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_11'] = x_negotiate_response_format__mutmut_11 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_12'] = x_negotiate_response_format__mutmut_12 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_13'] = x_negotiate_response_format__mutmut_13 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_14'] = x_negotiate_response_format__mutmut_14 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_15'] = x_negotiate_response_format__mutmut_15 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_16'] = x_negotiate_response_format__mutmut_16 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_17'] = x_negotiate_response_format__mutmut_17 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_18'] = x_negotiate_response_format__mutmut_18 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_19'] = x_negotiate_response_format__mutmut_19 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_20'] = x_negotiate_response_format__mutmut_20 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_21'] = x_negotiate_response_format__mutmut_21 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_22'] = x_negotiate_response_format__mutmut_22 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_23'] = x_negotiate_response_format__mutmut_23 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_24'] = x_negotiate_response_format__mutmut_24 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_25'] = x_negotiate_response_format__mutmut_25 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_26'] = x_negotiate_response_format__mutmut_26 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_27'] = x_negotiate_response_format__mutmut_27 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_28'] = x_negotiate_response_format__mutmut_28 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_29'] = x_negotiate_response_format__mutmut_29 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_30'] = x_negotiate_response_format__mutmut_30 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_31'] = x_negotiate_response_format__mutmut_31 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_32'] = x_negotiate_response_format__mutmut_32 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_33'] = x_negotiate_response_format__mutmut_33 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_34'] = x_negotiate_response_format__mutmut_34 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_35'] = x_negotiate_response_format__mutmut_35 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_36'] = x_negotiate_response_format__mutmut_36 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_37'] = x_negotiate_response_format__mutmut_37 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_38'] = x_negotiate_response_format__mutmut_38 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_39'] = x_negotiate_response_format__mutmut_39 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_40'] = x_negotiate_response_format__mutmut_40 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_41'] = x_negotiate_response_format__mutmut_41 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_42'] = x_negotiate_response_format__mutmut_42 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_43'] = x_negotiate_response_format__mutmut_43 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_44'] = x_negotiate_response_format__mutmut_44 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_45'] = x_negotiate_response_format__mutmut_45 # type: ignore # mutmut generated
mutants_x_negotiate_response_format__mutmut['x_negotiate_response_format__mutmut_46'] = x_negotiate_response_format__mutmut_46 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


@_mutmut_mutated(mutants_x_resolve_litellm_model__mutmut)
def resolve_litellm_model(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_orig(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_1(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = None
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_2(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = None
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_3(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["XXapi_keyXX"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_4(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["API_KEY"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_5(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = None
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_6(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.upper()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_7(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider != "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_8(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "XXzaiXX":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_9(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "ZAI":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_10(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = None
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_11(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit(None, 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_12(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", None)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_13(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit(1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_14(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", )[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_15(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.split("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_16(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("XX/XX", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_17(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 2)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_18(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[+1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_19(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-2]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_20(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = None
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_21(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = None
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_22(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["XXapi_baseXX"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_23(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["API_BASE"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_24(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "XXhttps://api.z.ai/api/coding/paas/v4XX"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_25(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "HTTPS://API.Z.AI/API/CODING/PAAS/V4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_26(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = None
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_27(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["XXcustom_llm_providerXX"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_28(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["CUSTOM_LLM_PROVIDER"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_29(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "XXopenaiXX"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_30(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "OPENAI"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_31(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "XXapi_keyXX" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_32(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "API_KEY" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_33(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_34(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = None
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_35(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["XXapi_keyXX"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_36(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["API_KEY"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_37(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get(None)
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_38(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("XXZAI_API_KEYXX")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_39(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("zai_api_key")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_40(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider != "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_41(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "XXopenrouterXX":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_42(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "OPENROUTER":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_43(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith(None):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_44(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("XXopenrouter/XX"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_45(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("OPENROUTER/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_46(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider != "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_47(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "XXanthropicXX":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_48(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "ANTHROPIC":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_49(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith(None):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_50(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("XXanthropic/XX"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_51(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("ANTHROPIC/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_52(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider != "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_53(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "XXdeepseekXX":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_54(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "DEEPSEEK":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_55(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = None
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_56(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit(None, 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_57(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", None)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_58(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit(1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_59(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", )[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_60(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.split("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_61(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("XX/XX", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_62(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 2)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_63(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[+1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_64(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-2]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_65(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = None
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_66(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = None
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_67(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["XXapi_baseXX"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_68(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["API_BASE"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_69(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "XXhttps://api.deepseek.com/v1XX"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_70(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "HTTPS://API.DEEPSEEK.COM/V1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_71(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = None
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_72(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["XXcustom_llm_providerXX"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_73(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["CUSTOM_LLM_PROVIDER"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_74(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "XXopenaiXX"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_75(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "OPENAI"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_76(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "XXapi_keyXX" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_77(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "API_KEY" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_78(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_79(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = None
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_80(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["XXapi_keyXX"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_81(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["API_KEY"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_82(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get(None)
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_83(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("XXDEEPSEEK_API_KEYXX")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_84(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("deepseek_api_key")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_85(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider != "openai":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_86(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "XXopenaiXX":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_87(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "OPENAI":
        if model_name.startswith("openai/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_88(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith(None):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_89(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("XXopenai/XX"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs


# --------------------------------------------------------------------------- #
# Model resolution
# --------------------------------------------------------------------------- #


def x_resolve_litellm_model__mutmut_90(
    model_name: str, entry: ModelEntry, api_key: str | None = None,
) -> tuple[str, dict[str, Any]]:
    """Map a Chimera model to a LiteLLM model string + extra kwargs.

    Rules:
    * If the config supplies ``litellm_model`` explicitly, use it verbatim.
    * ``zai`` provider → OpenAI-compatible call against the ZAI base URL.
    * ``openrouter`` provider → ensure the ``openrouter/`` prefix.
    * ``anthropic`` / ``deepseek`` → use the native LiteLLM provider prefix.
    * When ``api_key`` is supplied, it is passed as ``api_key`` so LiteLLM
      authenticates — otherwise it falls back to env vars.
    """
    kwargs: dict[str, Any] = {}
    if api_key:
        kwargs["api_key"] = api_key
    if entry.litellm_model:
        return entry.litellm_model, kwargs

    provider = entry.provider.lower()
    if provider == "zai":
        # The z.ai coding endpoint expects the bare model name (e.g. "glm-5.2"),
        # not the full catalog id ("zai-coding-plan/glm-5.2").
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.z.ai/api/coding/paas/v4"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("ZAI_API_KEY")
        return lm_model, kwargs

    if provider == "openrouter":
        if model_name.startswith("openrouter/"):
            return model_name, kwargs
        return f"openrouter/{model_name}", kwargs

    if provider == "anthropic":
        if model_name.startswith("anthropic/"):
            return model_name, kwargs
        return f"anthropic/{model_name}", kwargs

    if provider == "deepseek":
        # Route through LiteLLM's OpenAI provider to avoid DeepSeek-specific
        # cost calculator that doesn't know about v4-flash/v4-pro model names.
        api_model = model_name.rsplit("/", 1)[-1]
        lm_model = f"openai/{api_model}"
        kwargs["api_base"] = "https://api.deepseek.com/v1"
        kwargs["custom_llm_provider"] = "openai"
        if "api_key" not in kwargs:
            kwargs["api_key"] = os.environ.get("DEEPSEEK_API_KEY")
        return lm_model, kwargs

    if provider == "openai":
        if model_name.startswith("OPENAI/"):
            return model_name, kwargs
        return f"openai/{model_name}", kwargs

    return model_name, kwargs

mutants_x_resolve_litellm_model__mutmut['_mutmut_orig'] = x_resolve_litellm_model__mutmut_orig # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_1'] = x_resolve_litellm_model__mutmut_1 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_2'] = x_resolve_litellm_model__mutmut_2 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_3'] = x_resolve_litellm_model__mutmut_3 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_4'] = x_resolve_litellm_model__mutmut_4 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_5'] = x_resolve_litellm_model__mutmut_5 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_6'] = x_resolve_litellm_model__mutmut_6 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_7'] = x_resolve_litellm_model__mutmut_7 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_8'] = x_resolve_litellm_model__mutmut_8 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_9'] = x_resolve_litellm_model__mutmut_9 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_10'] = x_resolve_litellm_model__mutmut_10 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_11'] = x_resolve_litellm_model__mutmut_11 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_12'] = x_resolve_litellm_model__mutmut_12 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_13'] = x_resolve_litellm_model__mutmut_13 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_14'] = x_resolve_litellm_model__mutmut_14 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_15'] = x_resolve_litellm_model__mutmut_15 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_16'] = x_resolve_litellm_model__mutmut_16 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_17'] = x_resolve_litellm_model__mutmut_17 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_18'] = x_resolve_litellm_model__mutmut_18 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_19'] = x_resolve_litellm_model__mutmut_19 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_20'] = x_resolve_litellm_model__mutmut_20 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_21'] = x_resolve_litellm_model__mutmut_21 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_22'] = x_resolve_litellm_model__mutmut_22 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_23'] = x_resolve_litellm_model__mutmut_23 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_24'] = x_resolve_litellm_model__mutmut_24 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_25'] = x_resolve_litellm_model__mutmut_25 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_26'] = x_resolve_litellm_model__mutmut_26 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_27'] = x_resolve_litellm_model__mutmut_27 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_28'] = x_resolve_litellm_model__mutmut_28 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_29'] = x_resolve_litellm_model__mutmut_29 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_30'] = x_resolve_litellm_model__mutmut_30 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_31'] = x_resolve_litellm_model__mutmut_31 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_32'] = x_resolve_litellm_model__mutmut_32 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_33'] = x_resolve_litellm_model__mutmut_33 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_34'] = x_resolve_litellm_model__mutmut_34 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_35'] = x_resolve_litellm_model__mutmut_35 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_36'] = x_resolve_litellm_model__mutmut_36 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_37'] = x_resolve_litellm_model__mutmut_37 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_38'] = x_resolve_litellm_model__mutmut_38 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_39'] = x_resolve_litellm_model__mutmut_39 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_40'] = x_resolve_litellm_model__mutmut_40 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_41'] = x_resolve_litellm_model__mutmut_41 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_42'] = x_resolve_litellm_model__mutmut_42 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_43'] = x_resolve_litellm_model__mutmut_43 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_44'] = x_resolve_litellm_model__mutmut_44 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_45'] = x_resolve_litellm_model__mutmut_45 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_46'] = x_resolve_litellm_model__mutmut_46 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_47'] = x_resolve_litellm_model__mutmut_47 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_48'] = x_resolve_litellm_model__mutmut_48 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_49'] = x_resolve_litellm_model__mutmut_49 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_50'] = x_resolve_litellm_model__mutmut_50 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_51'] = x_resolve_litellm_model__mutmut_51 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_52'] = x_resolve_litellm_model__mutmut_52 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_53'] = x_resolve_litellm_model__mutmut_53 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_54'] = x_resolve_litellm_model__mutmut_54 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_55'] = x_resolve_litellm_model__mutmut_55 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_56'] = x_resolve_litellm_model__mutmut_56 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_57'] = x_resolve_litellm_model__mutmut_57 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_58'] = x_resolve_litellm_model__mutmut_58 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_59'] = x_resolve_litellm_model__mutmut_59 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_60'] = x_resolve_litellm_model__mutmut_60 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_61'] = x_resolve_litellm_model__mutmut_61 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_62'] = x_resolve_litellm_model__mutmut_62 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_63'] = x_resolve_litellm_model__mutmut_63 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_64'] = x_resolve_litellm_model__mutmut_64 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_65'] = x_resolve_litellm_model__mutmut_65 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_66'] = x_resolve_litellm_model__mutmut_66 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_67'] = x_resolve_litellm_model__mutmut_67 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_68'] = x_resolve_litellm_model__mutmut_68 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_69'] = x_resolve_litellm_model__mutmut_69 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_70'] = x_resolve_litellm_model__mutmut_70 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_71'] = x_resolve_litellm_model__mutmut_71 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_72'] = x_resolve_litellm_model__mutmut_72 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_73'] = x_resolve_litellm_model__mutmut_73 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_74'] = x_resolve_litellm_model__mutmut_74 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_75'] = x_resolve_litellm_model__mutmut_75 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_76'] = x_resolve_litellm_model__mutmut_76 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_77'] = x_resolve_litellm_model__mutmut_77 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_78'] = x_resolve_litellm_model__mutmut_78 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_79'] = x_resolve_litellm_model__mutmut_79 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_80'] = x_resolve_litellm_model__mutmut_80 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_81'] = x_resolve_litellm_model__mutmut_81 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_82'] = x_resolve_litellm_model__mutmut_82 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_83'] = x_resolve_litellm_model__mutmut_83 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_84'] = x_resolve_litellm_model__mutmut_84 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_85'] = x_resolve_litellm_model__mutmut_85 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_86'] = x_resolve_litellm_model__mutmut_86 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_87'] = x_resolve_litellm_model__mutmut_87 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_88'] = x_resolve_litellm_model__mutmut_88 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_89'] = x_resolve_litellm_model__mutmut_89 # type: ignore # mutmut generated
mutants_x_resolve_litellm_model__mutmut['x_resolve_litellm_model__mutmut_90'] = x_resolve_litellm_model__mutmut_90 # type: ignore # mutmut generated


# --------------------------------------------------------------------------- #
# F7: Retry with exponential backoff
# --------------------------------------------------------------------------- #

# HTTP status codes that are retryable (transient failures)
_RETRYABLE_STATUS_CODES: frozenset[int] = frozenset({
    429,                      # Rate limit
    500, 502, 503, 504,       # Server errors
})
mutants_x__is_retryable__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__is_retryable__mutmut)
def _is_retryable(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_orig(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_1(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = None
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_2(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code not in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_3(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return False

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_4(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(None, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_5(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, None):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_6(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr("status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_7(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, ):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_8(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "XXstatus_codeXX"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_9(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "STATUS_CODE"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_10(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = None
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_11(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(None, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_12(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, None, 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_13(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", None)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_14(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr("status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_15(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_16(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", )
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_17(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "XXstatus_codeXX", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_18(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "STATUS_CODE", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_19(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 1)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_20(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code not in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_21(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return False
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_22(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return False
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_23(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return True
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_24(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(None, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_25(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, None):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_26(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr("status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_27(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, ):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_28(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "XXstatus_codeXX"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_29(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "STATUS_CODE"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_30(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(None, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_31(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, None, 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_32(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", None) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_33(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr("status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_34(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_35(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", ) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_36(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "XXstatus_codeXX", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_37(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "STATUS_CODE", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_38(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 1) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_39(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) not in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_40(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return False
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_41(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return False
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_42(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return False
    except ImportError:
        pass

    return False


def x__is_retryable__mutmut_43(exc: BaseException) -> bool:
    """Check whether an exception indicates a transient failure worth retrying.

    Retry on: rate limit (429), server errors (5xx), network errors.
    Do NOT retry on: auth errors (401/403), bad request (400), budget exhausted.
    """
    import httpx

    # httpx.HTTPStatusError carries an HTTP response
    if isinstance(exc, httpx.HTTPStatusError):
        code = exc.response.status_code
        return code in _RETRYABLE_STATUS_CODES

    # Network-level errors (timeout, connection refused, DNS, ...)
    if isinstance(exc, (httpx.TimeoutException, httpx.NetworkError,
                         httpx.ConnectError, httpx.ReadError,
                         httpx.WriteError, httpx.PoolTimeout)):
        return True

    # openai.OpenAIError – check for retryable HTTP subclasses
    try:
        import openai
        if isinstance(exc, openai.OpenAIError):
            # OpenAI's APIStatusError has a status_code attribute
            if hasattr(exc, "status_code"):
                code = getattr(exc, "status_code", 0)
                if code in _RETRYABLE_STATUS_CODES:
                    return True
            # APIConnectionError and APITimeoutError are transient
            if isinstance(exc, (openai.APIConnectionError, openai.APITimeoutError)):
                return True
            # 401/403/400 are NOT retryable
            return False
    except ImportError:
        pass

    # litellm exceptions can wrap httpx/openai
    try:
        import litellm
        if isinstance(exc, litellm.exceptions.APIError):
            if hasattr(exc, "status_code"):
                return getattr(exc, "status_code", 0) in _RETRYABLE_STATUS_CODES
        if isinstance(exc, litellm.exceptions.APIConnectionError):
            return True
        if isinstance(exc, litellm.exceptions.RateLimitError):
            return True
        if isinstance(exc, litellm.exceptions.Timeout):
            return True
    except ImportError:
        pass

    return True

mutants_x__is_retryable__mutmut['_mutmut_orig'] = x__is_retryable__mutmut_orig # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_1'] = x__is_retryable__mutmut_1 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_2'] = x__is_retryable__mutmut_2 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_3'] = x__is_retryable__mutmut_3 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_4'] = x__is_retryable__mutmut_4 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_5'] = x__is_retryable__mutmut_5 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_6'] = x__is_retryable__mutmut_6 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_7'] = x__is_retryable__mutmut_7 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_8'] = x__is_retryable__mutmut_8 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_9'] = x__is_retryable__mutmut_9 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_10'] = x__is_retryable__mutmut_10 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_11'] = x__is_retryable__mutmut_11 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_12'] = x__is_retryable__mutmut_12 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_13'] = x__is_retryable__mutmut_13 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_14'] = x__is_retryable__mutmut_14 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_15'] = x__is_retryable__mutmut_15 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_16'] = x__is_retryable__mutmut_16 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_17'] = x__is_retryable__mutmut_17 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_18'] = x__is_retryable__mutmut_18 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_19'] = x__is_retryable__mutmut_19 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_20'] = x__is_retryable__mutmut_20 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_21'] = x__is_retryable__mutmut_21 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_22'] = x__is_retryable__mutmut_22 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_23'] = x__is_retryable__mutmut_23 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_24'] = x__is_retryable__mutmut_24 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_25'] = x__is_retryable__mutmut_25 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_26'] = x__is_retryable__mutmut_26 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_27'] = x__is_retryable__mutmut_27 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_28'] = x__is_retryable__mutmut_28 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_29'] = x__is_retryable__mutmut_29 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_30'] = x__is_retryable__mutmut_30 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_31'] = x__is_retryable__mutmut_31 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_32'] = x__is_retryable__mutmut_32 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_33'] = x__is_retryable__mutmut_33 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_34'] = x__is_retryable__mutmut_34 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_35'] = x__is_retryable__mutmut_35 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_36'] = x__is_retryable__mutmut_36 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_37'] = x__is_retryable__mutmut_37 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_38'] = x__is_retryable__mutmut_38 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_39'] = x__is_retryable__mutmut_39 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_40'] = x__is_retryable__mutmut_40 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_41'] = x__is_retryable__mutmut_41 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_42'] = x__is_retryable__mutmut_42 # type: ignore # mutmut generated
mutants_x__is_retryable__mutmut['x__is_retryable__mutmut_43'] = x__is_retryable__mutmut_43 # type: ignore # mutmut generated
mutants_x__sleep_ms__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__sleep_ms__mutmut)
async def _sleep_ms(ms: float) -> None:
    """Small async sleep helper."""
    await asyncio.sleep(ms / 1000.0)


async def x__sleep_ms__mutmut_orig(ms: float) -> None:
    """Small async sleep helper."""
    await asyncio.sleep(ms / 1000.0)


async def x__sleep_ms__mutmut_1(ms: float) -> None:
    """Small async sleep helper."""
    await asyncio.sleep(None)


async def x__sleep_ms__mutmut_2(ms: float) -> None:
    """Small async sleep helper."""
    await asyncio.sleep(ms * 1000.0)


async def x__sleep_ms__mutmut_3(ms: float) -> None:
    """Small async sleep helper."""
    await asyncio.sleep(ms / 1001.0)

mutants_x__sleep_ms__mutmut['_mutmut_orig'] = x__sleep_ms__mutmut_orig # type: ignore # mutmut generated
mutants_x__sleep_ms__mutmut['x__sleep_ms__mutmut_1'] = x__sleep_ms__mutmut_1 # type: ignore # mutmut generated
mutants_x__sleep_ms__mutmut['x__sleep_ms__mutmut_2'] = x__sleep_ms__mutmut_2 # type: ignore # mutmut generated
mutants_x__sleep_ms__mutmut['x__sleep_ms__mutmut_3'] = x__sleep_ms__mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁLiteLLMGatewayǁcomplete__mutmut: MutantDict = {}  # type: ignore
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut: MutantDict = {}  # type: ignore
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut: MutantDict = {}  # type: ignore
mutants_xǁLiteLLMGatewayǁ_build_response__mutmut: MutantDict = {}  # type: ignore


class LiteLLMGateway:
    """Production gateway backed by ``litellm.acompletion``.

    Resilience features:
    * F7 – Exponential backoff retry (configurable via RetryConfig).
    * F6 – Provider-aware response_format negotiation (auto-downgrade).
    * C3 – Token limit detection with WARNING log.
    * C4 – Empty/null response detection via is_empty flag.
    * C7 – Budget exhaustion detection (BudgetExhaustedError + CRITICAL log).
    """

    @_mutmut_mutated(mutants_xǁLiteLLMGatewayǁ__init____mutmut)
    def __init__(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_orig(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_1(self, config: ChimeraConfig) -> None:
        self.config = None
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_2(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = None
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_3(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = None
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_4(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = None

    def xǁLiteLLMGatewayǁ__init____mutmut_5(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=None, config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_6(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, config=None,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_7(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                config=cb_cfg,
            )

    def xǁLiteLLMGatewayǁ__init____mutmut_8(self, config: ChimeraConfig) -> None:
        self.config = config
        # Track provider for format negotiation
        self._model_provider_map: dict[str, str] = {}
        # F3: Per-provider circuit breakers (lazy-init on first call)
        self._circuit_breakers: dict[str, ProviderCircuitBreaker] = {}
        # Auto-initialize breakers for configured providers
        for provider_name, cb_cfg in config.circuit_breakers.items():
            self._circuit_breakers[provider_name] = ProviderCircuitBreaker(
                name=provider_name, )

    @_mutmut_mutated(mutants_xǁLiteLLMGatewayǁcomplete__mutmut)
    async def complete(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_orig(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_1(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 1.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_2(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = None
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_3(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(None)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_4(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = None
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_5(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(None)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_6(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = None

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_7(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(None, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_8(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, None, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_9(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=None)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_10(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_11(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_12(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, )

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_13(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = None

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_14(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            None, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_15(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, None
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_16(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_17(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_18(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = None
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_19(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "XXmodelXX": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_20(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "MODEL": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_21(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "XXmessagesXX": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_22(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "MESSAGES": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_23(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "XXtemperatureXX": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_24(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "TEMPERATURE": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_25(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_26(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = None

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_27(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["XXresponse_formatXX"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_28(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["RESPONSE_FORMAT"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_29(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            None,
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_30(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=None,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_31(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=None,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_32(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=None,
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_33(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_34(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_35(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_36(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_37(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "XXgateway_callXX",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_38(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "GATEWAY_CALL",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_39(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get(None) if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_40(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("XXtypeXX") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_41(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("TYPE") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_42(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "XXnoneXX"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_43(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "NONE"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_44(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = None
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_45(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(None)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_46(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None or not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_47(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_48(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_49(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning(None, provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_50(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=None, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_51(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=None)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_52(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning(provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_53(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_54(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, )
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_55(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("XXcircuit_breaker_openXX", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_56(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("CIRCUIT_BREAKER_OPEN", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_57(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(None)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_58(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = None
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_59(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(None, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_60(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, None)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_61(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_62(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, )
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_63(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is not None:
                breaker.on_failure()
            raise

    async def xǁLiteLLMGatewayǁcomplete__mutmut_64(
        self,
        model: str,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.2,
        response_format: dict[str, Any] | None = None,
        **kwargs: Any,
    ) -> GatewayResponse:
        entry = self.config.get_model(model)
        api_key = self.config.api_keys.get(entry.provider)
        lm_model, extra = resolve_litellm_model(model, entry, api_key=api_key)

        # F6: Provider-aware format negotiation
        negotiated_format = negotiate_response_format(
            response_format, entry.provider
        )

        call_kwargs: dict[str, Any] = {
            "model": lm_model,
            "messages": messages,
            "temperature": temperature,
            **extra,
            **kwargs,
        }
        if negotiated_format is not None:
            call_kwargs["response_format"] = negotiated_format

        log.debug(
            "gateway_call",
            model=model,
            litellm_model=lm_model,
            response_format_type=(
                negotiated_format.get("type") if negotiated_format else "none"
            ),
        )

        # F3: Circuit breaker check
        breaker = self._get_circuit_breaker(entry.provider)
        if breaker is not None and not breaker.before_call():
            log.warning("circuit_breaker_open", provider=entry.provider, model=model)
            return fast_fail_response(entry.provider)

        try:
            response = await self._complete_with_retry(call_kwargs, model)
            if breaker is not None:
                breaker.on_success()
            return response
        except (GatewayError, BudgetExhaustedError):
            if breaker is None:
                breaker.on_failure()
            raise

    @_mutmut_mutated(mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut)
    def _get_circuit_breaker(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_orig(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_1(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = None
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_2(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(None)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_3(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_4(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = None
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_5(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get(None)
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_6(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("XXdefaultXX")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_7(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("DEFAULT")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_8(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_9(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = None
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_10(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=None, config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_11(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=None)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_12(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(config=default_cfg)
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_13(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, )
            self._circuit_breakers[provider] = breaker
            return breaker
        return None

    def xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_14(self, provider: str) -> ProviderCircuitBreaker | None:
        """Get the circuit breaker for a provider, creating one from defaults if needed."""
        breaker = self._circuit_breakers.get(provider)
        if breaker is not None:
            return breaker
        # Try "default" config
        default_cfg = self.config.circuit_breakers.get("default")
        if default_cfg is not None:
            breaker = ProviderCircuitBreaker(name=provider, config=default_cfg)
            self._circuit_breakers[provider] = None
            return breaker
        return None

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    @_mutmut_mutated(mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut)
    async def _complete_with_retry(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_orig(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_1(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = None
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_2(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = ""

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_3(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(None, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_4(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, None):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_5(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_6(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, ):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_7(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(2, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_8(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts - 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_9(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 2):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_10(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = None
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_11(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    None, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_12(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, None
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_13(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_14(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_15(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(None, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_16(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, None)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_17(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_18(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, )
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_19(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = None

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_20(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = None
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_21(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).upper()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_22(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(None).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_23(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(None):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_24(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = None
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_25(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(None)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_26(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        None,
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_27(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=None,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_28(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=None,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_29(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=None,
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_30(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_31(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_32(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_33(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_34(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "XXbudget_exhaustedXX",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_35(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "BUDGET_EXHAUSTED",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_36(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(None),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_37(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=None,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_38(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=None,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_39(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=None,
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_40(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_41(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_42(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_43(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(None),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_44(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_45(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(None):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_46(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        None,
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_47(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=None,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_48(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=None,
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_49(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=None,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_50(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_51(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_52(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_53(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_54(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "XXgateway_non_retryableXX",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_55(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "GATEWAY_NON_RETRYABLE",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_56(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(None),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_57(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        None
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_58(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt > retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_59(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        None,
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_60(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=None,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_61(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=None,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_62(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=None,
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_63(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_64(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_65(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_66(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_67(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "XXgateway_retry_exhaustedXX",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_68(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "GATEWAY_RETRY_EXHAUSTED",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_69(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(None),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_70(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    return

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_71(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = None
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_72(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    None,
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_73(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    None,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_74(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_75(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_76(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms / (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_77(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier * (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_78(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt + 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_79(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 2)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_80(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = None
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_81(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 / (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_82(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay / 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_83(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 1.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_84(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 + 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_85(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) * 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_86(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 / (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_87(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (3 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_88(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) / 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_89(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(None) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_90(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(None)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_91(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 101) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_92(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 101.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_93(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 2)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_94(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = None

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_95(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay - jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_96(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    None,
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_97(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=None,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_98(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=None,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_99(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=None,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_100(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=None,
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_101(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=None,
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_102(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_103(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_104(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_105(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_106(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_107(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_108(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "XXgateway_retryXX",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_109(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "GATEWAY_RETRY",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_110(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt - 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_111(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 2,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_112(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(None),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_113(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(None)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_114(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:201],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_115(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(None)

        raise GatewayError(
            f"{model} call failed after {retry_cfg.max_attempts} attempts: {last_error}"
        ) from last_error

    # ------------------------------------------------------------------ #
    # F7: Exponential backoff retry + C7: budget exhaustion detection
    # ------------------------------------------------------------------ #

    async def xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_116(
        self, call_kwargs: dict[str, Any], model: str
    ) -> GatewayResponse:
        """Call LiteLLM with exponential backoff retry for transient failures.

        Retry policy (from RetryConfig):
        * max_attempts: max total attempts (default 3)
        * base_delay_ms: initial delay (default 500)
        * max_delay_ms: cap on delay (default 10000)
        * backoff_multiplier: multiplier each attempt (default 2.0)

        Retryable: 429, 5xx, network errors (timeout, connection refused).
        Non-retryable: 401/403, 400, budget exhausted.
        Budget exhaustion (C7): detected and raised as BudgetExhaustedError.
        """
        retry_cfg = self.config.retry
        last_error: BaseException | None = None

        for attempt in range(1, retry_cfg.max_attempts + 1):
            try:
                result = await asyncio.to_thread(
                    _litellm_sync_complete, call_kwargs
                )
                return self._build_response(result, model)
            except Exception as exc:
                last_error = exc

                # C7: Detect budget/quota exhaustion before retry logic
                exc_str = str(exc).lower()
                if _is_budget_exhausted(exc_str):
                    entry = self.config.get_model(model)
                    log.critical(
                        "budget_exhausted",
                        model=model,
                        provider=entry.provider,
                        error=str(exc),
                    )
                    raise BudgetExhaustedError(
                        model=model,
                        provider=entry.provider,
                        details=str(exc),
                    ) from exc

                # Check if this is retryable
                if not _is_retryable(exc):
                    log.debug(
                        "gateway_non_retryable",
                        model=model,
                        error=str(exc),
                        attempt=attempt,
                    )
                    raise GatewayError(
                        f"{model} call failed: {exc}"
                    ) from exc

                if attempt >= retry_cfg.max_attempts:
                    log.warning(
                        "gateway_retry_exhausted",
                        model=model,
                        attempts=attempt,
                        error=str(exc),
                    )
                    break

                # Calculate exponential backoff with jitter
                delay = min(
                    retry_cfg.base_delay_ms * (retry_cfg.backoff_multiplier ** (attempt - 1)),
                    retry_cfg.max_delay_ms,
                )
                # Add ±25% jitter
                jitter = delay * 0.25 * (2 * (hash(str(attempt)) % 100) / 100.0 - 1)
                delay = delay + jitter

                log.info(
                    "gateway_retry",
                    model=model,
                    attempt=attempt,
                    next_attempt=attempt + 1,
                    delay_ms=int(delay),
                    error=str(exc)[:200],
                )
                await _sleep_ms(delay)

        raise GatewayError(
            None
        ) from last_error

    @_mutmut_mutated(mutants_xǁLiteLLMGatewayǁ_build_response__mutmut)
    def _build_response(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(result, model)

    def xǁLiteLLMGatewayǁ_build_response__mutmut_orig(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(result, model)

    def xǁLiteLLMGatewayǁ_build_response__mutmut_1(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(None, model)

    def xǁLiteLLMGatewayǁ_build_response__mutmut_2(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(result, None)

    def xǁLiteLLMGatewayǁ_build_response__mutmut_3(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(model)

    def xǁLiteLLMGatewayǁ_build_response__mutmut_4(self, result: Any, model: str) -> GatewayResponse:
        """Build a GatewayResponse from a LiteLLM result, with token limit
        and empty response detection (C3, C4)."""
        return _build_response(result, )

mutants_xǁLiteLLMGatewayǁ__init____mutmut['_mutmut_orig'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_1'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_2'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_3'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_4'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_5'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_6'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_7'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ__init____mutmut['xǁLiteLLMGatewayǁ__init____mutmut_8'] = LiteLLMGateway.xǁLiteLLMGatewayǁ__init____mutmut_8 # type: ignore # mutmut generated

mutants_xǁLiteLLMGatewayǁcomplete__mutmut['_mutmut_orig'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_orig # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_1'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_1 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_2'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_2 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_3'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_4'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_4 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_5'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_5 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_6'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_6 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_7'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_7 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_8'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_8 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_9'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_9 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_10'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_10 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_11'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_11 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_12'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_12 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_13'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_13 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_14'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_14 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_15'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_15 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_16'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_16 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_17'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_17 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_18'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_18 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_19'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_19 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_20'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_20 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_21'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_21 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_22'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_22 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_23'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_23 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_24'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_24 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_25'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_25 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_26'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_26 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_27'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_27 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_28'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_28 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_29'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_29 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_30'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_30 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_31'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_31 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_32'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_32 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_33'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_33 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_34'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_34 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_35'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_35 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_36'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_36 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_37'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_37 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_38'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_38 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_39'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_39 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_40'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_40 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_41'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_41 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_42'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_42 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_43'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_43 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_44'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_44 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_45'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_45 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_46'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_46 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_47'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_47 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_48'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_48 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_49'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_49 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_50'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_50 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_51'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_51 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_52'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_52 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_53'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_53 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_54'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_54 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_55'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_55 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_56'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_56 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_57'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_57 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_58'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_58 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_59'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_59 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_60'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_60 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_61'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_61 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_62'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_62 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_63'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_63 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁcomplete__mutmut['xǁLiteLLMGatewayǁcomplete__mutmut_64'] = LiteLLMGateway.xǁLiteLLMGatewayǁcomplete__mutmut_64 # type: ignore # mutmut generated

mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['_mutmut_orig'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_orig # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_1'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_1 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_2'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_2 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_3'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_4'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_4 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_5'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_5 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_6'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_6 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_7'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_7 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_8'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_8 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_9'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_9 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_10'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_10 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_11'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_11 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_12'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_12 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_13'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_13 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut['xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_14'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_get_circuit_breaker__mutmut_14 # type: ignore # mutmut generated

mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['_mutmut_orig'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_orig # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_1'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_1 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_2'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_2 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_3'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_4'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_4 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_5'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_5 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_6'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_6 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_7'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_7 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_8'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_8 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_9'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_9 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_10'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_10 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_11'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_11 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_12'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_12 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_13'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_13 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_14'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_14 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_15'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_15 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_16'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_16 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_17'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_17 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_18'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_18 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_19'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_19 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_20'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_20 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_21'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_21 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_22'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_22 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_23'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_23 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_24'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_24 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_25'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_25 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_26'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_26 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_27'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_27 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_28'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_28 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_29'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_29 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_30'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_30 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_31'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_31 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_32'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_32 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_33'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_33 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_34'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_34 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_35'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_35 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_36'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_36 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_37'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_37 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_38'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_38 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_39'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_39 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_40'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_40 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_41'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_41 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_42'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_42 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_43'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_43 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_44'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_44 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_45'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_45 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_46'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_46 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_47'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_47 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_48'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_48 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_49'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_49 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_50'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_50 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_51'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_51 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_52'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_52 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_53'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_53 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_54'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_54 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_55'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_55 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_56'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_56 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_57'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_57 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_58'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_58 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_59'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_59 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_60'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_60 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_61'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_61 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_62'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_62 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_63'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_63 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_64'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_64 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_65'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_65 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_66'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_66 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_67'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_67 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_68'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_68 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_69'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_69 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_70'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_70 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_71'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_71 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_72'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_72 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_73'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_73 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_74'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_74 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_75'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_75 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_76'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_76 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_77'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_77 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_78'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_78 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_79'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_79 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_80'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_80 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_81'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_81 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_82'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_82 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_83'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_83 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_84'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_84 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_85'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_85 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_86'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_86 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_87'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_87 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_88'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_88 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_89'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_89 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_90'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_90 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_91'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_91 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_92'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_92 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_93'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_93 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_94'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_94 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_95'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_95 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_96'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_96 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_97'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_97 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_98'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_98 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_99'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_99 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_100'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_100 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_101'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_101 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_102'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_102 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_103'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_103 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_104'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_104 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_105'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_105 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_106'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_106 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_107'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_107 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_108'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_108 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_109'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_109 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_110'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_110 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_111'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_111 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_112'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_112 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_113'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_113 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_114'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_114 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_115'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_115 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_complete_with_retry__mutmut['xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_116'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_complete_with_retry__mutmut_116 # type: ignore # mutmut generated

mutants_xǁLiteLLMGatewayǁ_build_response__mutmut['_mutmut_orig'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_build_response__mutmut_orig # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_build_response__mutmut['xǁLiteLLMGatewayǁ_build_response__mutmut_1'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_build_response__mutmut_1 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_build_response__mutmut['xǁLiteLLMGatewayǁ_build_response__mutmut_2'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_build_response__mutmut_2 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_build_response__mutmut['xǁLiteLLMGatewayǁ_build_response__mutmut_3'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_build_response__mutmut_3 # type: ignore # mutmut generated
mutants_xǁLiteLLMGatewayǁ_build_response__mutmut['xǁLiteLLMGatewayǁ_build_response__mutmut_4'] = LiteLLMGateway.xǁLiteLLMGatewayǁ_build_response__mutmut_4 # type: ignore # mutmut generated
mutants_x__build_response__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__build_response__mutmut)
def _build_response(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_orig(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_1(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = None
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_2(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(None)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_3(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = None
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_4(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(None, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_5(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, None, None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_6(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr("usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_7(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_8(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", )
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_9(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "XXusageXX", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_10(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "USAGE", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_11(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = None
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_12(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(None)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_13(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) and 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_14(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(None, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_15(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, None, 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_16(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", None) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_17(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr("prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_18(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_19(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", ) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_20(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "XXprompt_tokensXX", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_21(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "PROMPT_TOKENS", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_22(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 1) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_23(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 1)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_24(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = None

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_25(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(None)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_26(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) and 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_27(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(None, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_28(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, None, 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_29(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", None) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_30(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr("completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_31(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_32(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", ) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_33(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "XXcompletion_tokensXX", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_34(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "COMPLETION_TOKENS", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_35(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 1) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_36(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 1)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_37(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = None
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_38(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = "XXXX"
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_39(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = None
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_40(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) and []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_41(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(None, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_42(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, None, None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_43(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr("choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_44(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_45(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", ) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_46(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "XXchoicesXX", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_47(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "CHOICES", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_48(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = None

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_49(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") and ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_50(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(None, "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_51(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], None, "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_52(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", None) or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_53(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr("finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_54(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_55(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", ) or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_56(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[1], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_57(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "XXfinish_reasonXX", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_58(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "FINISH_REASON", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_59(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "XXXX") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_60(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or "XXXX"

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_61(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = None

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_62(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_63(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(None)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_64(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason != "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_65(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "XXlengthXX":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_66(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "LENGTH":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_67(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            None,
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_68(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=None,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_69(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=None,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_70(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=None,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_71(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_72(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_73(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_74(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_75(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "XXtoken_limit_reachedXX",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_76(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "TOKEN_LIMIT_REACHED",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_77(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=None,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_78(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=None,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_79(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=None,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_80(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=None,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_81(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=None,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_82(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=None,
        is_empty=is_empty,
    )


def x__build_response__mutmut_83(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=None,
    )


def x__build_response__mutmut_84(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_85(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_86(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_87(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        raw=result,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_88(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        finish_reason=finish_reason,
        is_empty=is_empty,
    )


def x__build_response__mutmut_89(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        is_empty=is_empty,
    )


def x__build_response__mutmut_90(result: Any, model: str) -> GatewayResponse:
    """Build a GatewayResponse from a LiteLLM result (standalone, testable).

    Detects token limit (C3) and empty responses (C4).
    """
    text = _extract_text(result)
    usage = getattr(result, "usage", None)
    tok_in = int(getattr(usage, "prompt_tokens", 0) or 0)
    tok_out = int(getattr(usage, "completion_tokens", 0) or 0)

    # C3: Check finish_reason for token limit
    finish_reason = ""
    choices = getattr(result, "choices", None) or []
    if choices:
        finish_reason = getattr(choices[0], "finish_reason", "") or ""

    # C4: Detect empty response
    is_empty = not bool(text)

    if finish_reason == "length":
        log.warning(
            "token_limit_reached",
            model=model,
            tokens_input=tok_in,
            tokens_output=tok_out,
        )

    return GatewayResponse(
        text=text,
        model=model,
        tokens_input=tok_in,
        tokens_output=tok_out,
        raw=result,
        finish_reason=finish_reason,
        )

mutants_x__build_response__mutmut['_mutmut_orig'] = x__build_response__mutmut_orig # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_1'] = x__build_response__mutmut_1 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_2'] = x__build_response__mutmut_2 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_3'] = x__build_response__mutmut_3 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_4'] = x__build_response__mutmut_4 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_5'] = x__build_response__mutmut_5 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_6'] = x__build_response__mutmut_6 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_7'] = x__build_response__mutmut_7 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_8'] = x__build_response__mutmut_8 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_9'] = x__build_response__mutmut_9 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_10'] = x__build_response__mutmut_10 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_11'] = x__build_response__mutmut_11 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_12'] = x__build_response__mutmut_12 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_13'] = x__build_response__mutmut_13 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_14'] = x__build_response__mutmut_14 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_15'] = x__build_response__mutmut_15 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_16'] = x__build_response__mutmut_16 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_17'] = x__build_response__mutmut_17 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_18'] = x__build_response__mutmut_18 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_19'] = x__build_response__mutmut_19 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_20'] = x__build_response__mutmut_20 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_21'] = x__build_response__mutmut_21 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_22'] = x__build_response__mutmut_22 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_23'] = x__build_response__mutmut_23 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_24'] = x__build_response__mutmut_24 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_25'] = x__build_response__mutmut_25 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_26'] = x__build_response__mutmut_26 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_27'] = x__build_response__mutmut_27 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_28'] = x__build_response__mutmut_28 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_29'] = x__build_response__mutmut_29 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_30'] = x__build_response__mutmut_30 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_31'] = x__build_response__mutmut_31 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_32'] = x__build_response__mutmut_32 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_33'] = x__build_response__mutmut_33 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_34'] = x__build_response__mutmut_34 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_35'] = x__build_response__mutmut_35 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_36'] = x__build_response__mutmut_36 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_37'] = x__build_response__mutmut_37 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_38'] = x__build_response__mutmut_38 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_39'] = x__build_response__mutmut_39 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_40'] = x__build_response__mutmut_40 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_41'] = x__build_response__mutmut_41 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_42'] = x__build_response__mutmut_42 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_43'] = x__build_response__mutmut_43 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_44'] = x__build_response__mutmut_44 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_45'] = x__build_response__mutmut_45 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_46'] = x__build_response__mutmut_46 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_47'] = x__build_response__mutmut_47 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_48'] = x__build_response__mutmut_48 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_49'] = x__build_response__mutmut_49 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_50'] = x__build_response__mutmut_50 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_51'] = x__build_response__mutmut_51 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_52'] = x__build_response__mutmut_52 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_53'] = x__build_response__mutmut_53 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_54'] = x__build_response__mutmut_54 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_55'] = x__build_response__mutmut_55 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_56'] = x__build_response__mutmut_56 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_57'] = x__build_response__mutmut_57 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_58'] = x__build_response__mutmut_58 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_59'] = x__build_response__mutmut_59 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_60'] = x__build_response__mutmut_60 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_61'] = x__build_response__mutmut_61 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_62'] = x__build_response__mutmut_62 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_63'] = x__build_response__mutmut_63 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_64'] = x__build_response__mutmut_64 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_65'] = x__build_response__mutmut_65 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_66'] = x__build_response__mutmut_66 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_67'] = x__build_response__mutmut_67 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_68'] = x__build_response__mutmut_68 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_69'] = x__build_response__mutmut_69 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_70'] = x__build_response__mutmut_70 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_71'] = x__build_response__mutmut_71 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_72'] = x__build_response__mutmut_72 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_73'] = x__build_response__mutmut_73 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_74'] = x__build_response__mutmut_74 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_75'] = x__build_response__mutmut_75 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_76'] = x__build_response__mutmut_76 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_77'] = x__build_response__mutmut_77 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_78'] = x__build_response__mutmut_78 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_79'] = x__build_response__mutmut_79 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_80'] = x__build_response__mutmut_80 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_81'] = x__build_response__mutmut_81 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_82'] = x__build_response__mutmut_82 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_83'] = x__build_response__mutmut_83 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_84'] = x__build_response__mutmut_84 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_85'] = x__build_response__mutmut_85 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_86'] = x__build_response__mutmut_86 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_87'] = x__build_response__mutmut_87 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_88'] = x__build_response__mutmut_88 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_89'] = x__build_response__mutmut_89 # type: ignore # mutmut generated
mutants_x__build_response__mutmut['x__build_response__mutmut_90'] = x__build_response__mutmut_90 # type: ignore # mutmut generated


def _litellm_sync_complete(call_kwargs: dict[str, Any]) -> Any:
    import litellm

    return litellm.completion(**call_kwargs)
mutants_x__extract_text__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

@_mutmut_mutated(mutants_x__extract_text__mutmut)
def _extract_text(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_orig(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_1(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = None
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_2(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(None, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_3(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, None, None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_4(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr("choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_5(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_6(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", )
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_7(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "XXchoicesXX", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_8(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "CHOICES", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_9(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_10(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return "XXXX"
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_11(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_12(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return "XXXX"
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_13(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) != 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_14(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 1:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_15(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return "XXXX"

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_16(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = None
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_17(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[1]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_18(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is not None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_19(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return "XXXX"

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_20(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = None
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_21(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(None, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_22(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, None, None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_23(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr("message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_24(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_25(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", )
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_26(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "XXmessageXX", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_27(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "MESSAGE", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_28(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is not None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_29(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return "XXXX"

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_30(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = None
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_31(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(None, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_32(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, None, None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_33(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr("content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_34(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_35(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", )
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_36(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "XXcontentXX", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_37(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "CONTENT", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_38(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) or content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_39(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content or isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_40(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("XXreasoning_contentXX", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_41(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("REASONING_CONTENT", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_42(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "XXreasoningXX"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_43(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "REASONING"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_44(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = None
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_45(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(None, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_46(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, None, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_47(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_48(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_49(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, )
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_50(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) or val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_51(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val or isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_52(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is None:
        return content if isinstance(content, str) else str(content)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_53(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(None)

    return ""


# --------------------------------------------------------------------------- #
# C4: Empty/null response extraction
# --------------------------------------------------------------------------- #

def x__extract_text__mutmut_54(result: Any) -> str:
    """Extract text content from a LiteLLM completion result.

    Handles every known edge case without crashing (C4):
    * Empty choices array, null choices, missing choices attribute
    * Choices with no message, message with null/empty content
    * ``finish_reason="stop"`` with empty content (model stopped early)
    * Reasoning models that return text in non-standard fields
      (``reasoning_content`` for DeepSeek V4, ``reasoning`` for MiniMax/Kimi)
    """
    choices = getattr(result, "choices", None)
    if not choices:
        return ""
    if not isinstance(choices, (list, tuple)):
        return ""
    if len(choices) == 0:
        return ""

    first = choices[0]
    if first is None:
        return ""

    message = getattr(first, "message", None)
    if message is None:
        return ""

    # Primary content field
    content = getattr(message, "content", None)
    if content and isinstance(content, str) and content.strip():
        return content

    # Reasoning models put their response in non-standard fields:
    #   DeepSeek V4 series → reasoning_content
    #   MiniMax M3, Kimi K2 → reasoning
    for attr in ("reasoning_content", "reasoning"):
        val = getattr(message, attr, None)
        if val and isinstance(val, str) and val.strip():
            return val

    # If content exists but is empty/whitespace-only, return it as-is
    # so is_empty can be detected by the caller
    if content is not None:
        return content if isinstance(content, str) else str(content)

    return "XXXX"

mutants_x__extract_text__mutmut['_mutmut_orig'] = x__extract_text__mutmut_orig # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_1'] = x__extract_text__mutmut_1 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_2'] = x__extract_text__mutmut_2 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_3'] = x__extract_text__mutmut_3 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_4'] = x__extract_text__mutmut_4 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_5'] = x__extract_text__mutmut_5 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_6'] = x__extract_text__mutmut_6 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_7'] = x__extract_text__mutmut_7 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_8'] = x__extract_text__mutmut_8 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_9'] = x__extract_text__mutmut_9 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_10'] = x__extract_text__mutmut_10 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_11'] = x__extract_text__mutmut_11 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_12'] = x__extract_text__mutmut_12 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_13'] = x__extract_text__mutmut_13 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_14'] = x__extract_text__mutmut_14 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_15'] = x__extract_text__mutmut_15 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_16'] = x__extract_text__mutmut_16 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_17'] = x__extract_text__mutmut_17 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_18'] = x__extract_text__mutmut_18 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_19'] = x__extract_text__mutmut_19 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_20'] = x__extract_text__mutmut_20 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_21'] = x__extract_text__mutmut_21 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_22'] = x__extract_text__mutmut_22 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_23'] = x__extract_text__mutmut_23 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_24'] = x__extract_text__mutmut_24 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_25'] = x__extract_text__mutmut_25 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_26'] = x__extract_text__mutmut_26 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_27'] = x__extract_text__mutmut_27 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_28'] = x__extract_text__mutmut_28 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_29'] = x__extract_text__mutmut_29 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_30'] = x__extract_text__mutmut_30 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_31'] = x__extract_text__mutmut_31 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_32'] = x__extract_text__mutmut_32 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_33'] = x__extract_text__mutmut_33 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_34'] = x__extract_text__mutmut_34 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_35'] = x__extract_text__mutmut_35 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_36'] = x__extract_text__mutmut_36 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_37'] = x__extract_text__mutmut_37 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_38'] = x__extract_text__mutmut_38 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_39'] = x__extract_text__mutmut_39 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_40'] = x__extract_text__mutmut_40 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_41'] = x__extract_text__mutmut_41 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_42'] = x__extract_text__mutmut_42 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_43'] = x__extract_text__mutmut_43 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_44'] = x__extract_text__mutmut_44 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_45'] = x__extract_text__mutmut_45 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_46'] = x__extract_text__mutmut_46 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_47'] = x__extract_text__mutmut_47 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_48'] = x__extract_text__mutmut_48 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_49'] = x__extract_text__mutmut_49 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_50'] = x__extract_text__mutmut_50 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_51'] = x__extract_text__mutmut_51 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_52'] = x__extract_text__mutmut_52 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_53'] = x__extract_text__mutmut_53 # type: ignore # mutmut generated
mutants_x__extract_text__mutmut['x__extract_text__mutmut_54'] = x__extract_text__mutmut_54 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut: MutantDict = {}  # type: ignore


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

@_mutmut_mutated(mutants_x__is_budget_exhausted__mutmut)
def _is_budget_exhausted(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_orig(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_1(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = None
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_2(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "XXinsufficient_quotaXX",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_3(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "INSUFFICIENT_QUOTA",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_4(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "XXbillingXX",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_5(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "BILLING",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_6(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "XXpayment requiredXX",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_7(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "PAYMENT REQUIRED",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_8(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "XXquota exceededXX",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_9(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "QUOTA EXCEEDED",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_10(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "XXrate limit exceededXX",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_11(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "RATE LIMIT EXCEEDED",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_12(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "XXyou exceeded your current quotaXX",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_13(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "YOU EXCEEDED YOUR CURRENT QUOTA",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_14(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "XXinsufficient_creditsXX",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_15(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "INSUFFICIENT_CREDITS",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_16(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "XXaccount balanceXX",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_17(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "ACCOUNT BALANCE",
        "billing issue",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_18(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "XXbilling issueXX",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_19(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "BILLING ISSUE",
        "spending limit",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_20(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "XXspending limitXX",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_21(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "SPENDING LIMIT",
    )
    return any(kw in error_str for kw in keywords)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_22(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(None)


# --------------------------------------------------------------------------- #
# C7: Budget exhaustion detection
# --------------------------------------------------------------------------- #

def x__is_budget_exhausted__mutmut_23(error_str: str) -> bool:
    """Detect provider errors indicating quota/budget exhaustion (C7)."""
    keywords = (
        "insufficient_quota",
        "billing",
        "payment required",
        "quota exceeded",
        "rate limit exceeded",
        "you exceeded your current quota",
        "insufficient_credits",
        "account balance",
        "billing issue",
        "spending limit",
    )
    return any(kw not in error_str for kw in keywords)

mutants_x__is_budget_exhausted__mutmut['_mutmut_orig'] = x__is_budget_exhausted__mutmut_orig # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_1'] = x__is_budget_exhausted__mutmut_1 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_2'] = x__is_budget_exhausted__mutmut_2 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_3'] = x__is_budget_exhausted__mutmut_3 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_4'] = x__is_budget_exhausted__mutmut_4 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_5'] = x__is_budget_exhausted__mutmut_5 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_6'] = x__is_budget_exhausted__mutmut_6 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_7'] = x__is_budget_exhausted__mutmut_7 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_8'] = x__is_budget_exhausted__mutmut_8 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_9'] = x__is_budget_exhausted__mutmut_9 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_10'] = x__is_budget_exhausted__mutmut_10 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_11'] = x__is_budget_exhausted__mutmut_11 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_12'] = x__is_budget_exhausted__mutmut_12 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_13'] = x__is_budget_exhausted__mutmut_13 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_14'] = x__is_budget_exhausted__mutmut_14 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_15'] = x__is_budget_exhausted__mutmut_15 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_16'] = x__is_budget_exhausted__mutmut_16 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_17'] = x__is_budget_exhausted__mutmut_17 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_18'] = x__is_budget_exhausted__mutmut_18 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_19'] = x__is_budget_exhausted__mutmut_19 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_20'] = x__is_budget_exhausted__mutmut_20 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_21'] = x__is_budget_exhausted__mutmut_21 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_22'] = x__is_budget_exhausted__mutmut_22 # type: ignore # mutmut generated
mutants_x__is_budget_exhausted__mutmut['x__is_budget_exhausted__mutmut_23'] = x__is_budget_exhausted__mutmut_23 # type: ignore # mutmut generated


__all__ = [
    "FormatCapability",
    "Gateway",
    "GatewayError",
    "GatewayResponse",
    "LiteLLMGateway",
    "BudgetExhaustedError",
    "_get_format_capability",
    "_is_retryable",
    "negotiate_response_format",
    "resolve_litellm_model",
]
